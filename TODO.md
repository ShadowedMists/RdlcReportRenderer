# TODO

## Project Status Summary

**Overall Progress:** Infrastructure complete, Excel Phases 4-5 complete, Chart/Gauge GDI+ abstraction substantially complete (Chart) / complete (Gauge), PDF cross-platform not started.

### Current Priorities

| Priority | Phase | Status | Risk |
|----------|-------|--------|------|
| 🔴 **HIGH** | Excel Phase 4: ImageFormatType Enum | ✅ COMPLETE | LOW |
| 🔴 **HIGH** | Excel Phase 5: IImageProvider Abstraction | ✅ COMPLETE | MEDIUM |
| 🟡 **HIGH** | Chart engine: GDI+ → interface abstraction | 🔄 Substantially complete (Milestones A-C, D2 done; D3/E1/E2 open) | HIGH |
| 🟡 **HIGH** | Gauge engine: GDI+ → interface abstraction | ✅ COMPLETE (all milestones and open items closed) | HIGH |
| 🔵 **LOW** | PDF Phase 1: SkiaSharp Migration | 📋 NOT STARTED | VERY HIGH |
| 🔵 **LOW** | Map engine: GDI+ → interface abstraction | 📋 NOT STARTED — deferred until after PDF Phase 1 | HIGH |

> The prior "Chart Library Migration (OxyPlot)" decision was retracted — see `docs/decisions.md`. Charts/gauges are rendered by vendored GDI+ engines this repo owns, not external libraries; the plan re-targets their existing rendering seams to SkiaSharp.

---

## Excel Phase 4: ImageFormatType Enum (✅ COMPLETE)

Replaced `System.Drawing.Imaging.ImageFormat` with a cross-platform `ImageFormatType` enum across the Excel renderer (internal API only, no public breaking changes). Reference: `docs/rendering-abstractions.md`.

## Excel Phase 5: IImageProvider Abstraction (✅ COMPLETE)

Added `IImageProvider`/`WindowsImageProvider`/`CrossPlatformImageProvider`/`ImageProviderFactory`, injected into `ChartMapper`/`GaugeMapper`. Reference: `docs/rendering-abstractions.md`.

## Chart & Gauge Cross-Platform Rendering

**Approach:** re-target the existing vendored GDI+ Chart/Gauge engines to SkiaSharp behind their existing rendering seams (Ports & Adapters — `IDrawingResourceFactory`/`IGaugeDrawingResourceFactory` port, `Gdi`/`Skia` adapters). Full design: `docs/rendering-abstractions.md`. Key decisions and the Phase 0 spike findings (GDI+ cannot construct at all on Linux, independent of backend): `docs/decisions.md`, `docs/platform-support.md`.

**Chart engine** (`tasks/chart-gdi-type-abstraction.md`): Milestones A (foundation), B (chokepoint migration), C1-C8 (per-type ports + call-site conversion) are done. D2 (backend selection) is **essentially done** (2026-07-22): scoped its real blocker to ~49 call sites reading `.Graphics` as raw `System.Drawing.Graphics` and converted ~47 of them across three sub-increments — (1) `ImageLoader.GetAdjustedImageSize`'s dead `Graphics` param removed, DPI/`CompositingQuality`/`InterpolationMode` abstracted onto `IChartRenderingEngine`; (2) `ChartPicture.Paint`'s `Graphics`-typed entry point given an `IRenderSurface`-typed dual-overload sibling (via a new `IChartRenderingEngine.BindSurface(IRenderSurface)` that moves the Gdi-surface downcast into the backend adapters), all 4 real production callers converted, `Graphics`-typed overload kept permanently for `ChartImage.SaveIntoMetafile`'s EMF-only path; (3) `TextAnnotation.cs`'s standalone `Bitmap`/`Graphics` construction routed through `IRenderSurfaceFactory`, and a provably-redundant `Graphics` reassignment removed from `SelectionManager.cs`. Only 2 already-documented permanent holdouts remain (`FunnelChart` 3D fill, `SaveIntoMetafile` EMF), both by design. D3 (remove temporary concrete overloads — blocked on `ChartGraphics3D.cs`, `MapAreasCollection.cs`, and the entire Gauge engine). E1 (Skia backend) is **started** (2026-07-22): scoped its real blocker — `ChartGraphics.cs`/painter files still allocate concrete GDI+ resources feeding GDI+-typed engine calls, ~100-150 sites for a non-trivial 2D chart, independent of and much smaller than D3's everywhere-scope. Converted the two highest-leverage chokepoint helpers (`ChartGraphics.FillRectangleShadowAbs`, `DrawMarkerAbs`'s shape-marker branch) plus the Pie/Doughnut chokepoint (`DrawPieRel` + its `DrawPieGradientEffects`/`DrawPieSoftShadow` helpers — all retyped in place, no signature change) since ~70 painter files draw through the first two and `PieChart`/`SunburstChart` through the third. Along the way, added `Blend`/`InterpolationColors` to the shared `IPathGradientBrush` interface (a gap from the original brush count) and implemented them on both the Chart and Gauge engines' `GdiPathGradientBrush`. `PieChart.cs`'s 3 label call sites converted too (bridging `Font`/`StringFormat` to `IChartFont`/`ITextFormat`, same pattern as `BarChart.cs`) — `PieChart.cs` is now fully converted apart from its hit-testing-only `CreateMapAreaPath`. Every `ChartTypes/*.cs` file from the original E1 estimate has now been converted or scoped out: `PieChart`/`AreaChart`/`StackedAreaChart`/`RangeChart`/`RadarChart`/`LineChart`/`RangeColumnChart`/`TreeMapChart`/`StackedBarChart`/`StackedColumnChart` are fully converted (2D paths — 3D-only branches and a few named hit-testing/`SmartLabels`-coupled residuals excepted); `KagiChart`/`StepLineChart` needed no changes (their concrete allocations are 3D-only). (A later pass, 2026-07-23, found the earlier "`StackedBarChart`/`StackedColumnChart` blocked on `SmartLabels.AdjustSmartLabelPosition`" framing overstated — that shared helper only ever mutates `StringFormat.Alignment`/`.LineAlignment` and never needed to change; the existing bridge-at-sink pattern, already proven in `PointChart.cs`, was simply applied to the 3 remaining concrete `DrawPointLabelStringRel` call sites in `RadarChart.cs`/`StackedBarChart.cs`/`StackedColumnChart.cs`. Verified: `dotnet build --no-incremental` 0 errors, `VisualRegressionTests` 80/80, `Chart.Rdl.Tests` 1/1.) `Legend.cs`/`Axis.cs`/`Title.cs`/`StripLine.cs` were also found to already be fully converted (stale estimates). A second `ChartGraphics.cs` scoping+conversion pass (2026-07-22) converted several more real chokepoints — `DrawPointLabelBackground` (shared by both `DrawPointLabelStringRel` overloads), `DrawRectangleBarStyle` (2D bar-bevel effects, not 3D-only), `DrawArrowRel`, `DrawLabelBackground`, `MapCategoryNodeLabel`, plus a new `GetPolygonCirclePathResource` sibling that let two `Axis.cs` call sites drop their manual bridge — while confirming most of the file's other concrete allocations (`GetHatchBrush`/`GetGradientBrush`/`CreateBrush`/`DrawCircleAbs`/etc.) are deliberate, already-sibling'd holdouts, not oversights. A third pass (2026-07-22) closed out the three previously-named residuals: **`Label.cs`/`DrawLabelStringRel`** got a full `IChartFont`/`IBrush`/`ITextFormat`-typed sibling (`ITextFormat` has no `Clone()`, so the leading clone-and-mutate became an explicit 4-property copy); **`SunburstChart`'s label-fitting residual** was resolved by replacing GDI+'s raster-based `Region.IsSuperSetOf` with an equivalent, backend-agnostic set-identity check via `IClipRegion.Complement` (verified against its visual-regression baseline, zero pixel diff); and the **named Skia adapter stubs** (`SkiaClipRegion`'s region algebra via `SKPath.Op`, brush-based pens, `SetTransparentColor`/`SetWrapMode`, `WrapImage`/`CreateTextureBrush`) are now real, with 12 new functional smoke tests (`SkiaResourceFactoryTests.cs`) since there's still no visual baseline for the Skia backend. This surfaced a new residual: several `SkiaChartGraphics` GDI+-typed members marked `NotReachable()` (`DrawPie`/`FillPie`/`DrawArc`/`DrawImage(IChartImage,...)`/etc.) are now genuinely reachable from real E1-converted call paths, though not yet hit in practice since real `Chart` rendering still routes through `ChartPicture.Paint`'s GDI+-typed entry point (D2). A fourth pass (2026-07-22) closed out that residual: traced each flagged member's real callers and found `DrawPie(IPen,...)`/`FillPie(IBrush,...)` (from `DrawPieRel`'s interface overload) and `DrawLines(IPen,...)`/`DrawImage(IChartImage,...)` (from `DrawLabelStringRel`'s interface overload) genuinely reachable — implemented for real via `SKCanvas.DrawArc`/`DrawBitmap`. `DrawArc(IPen,...)`/`DrawCurve(IPen,...)`/`FillRegion(IBrush,IClipRegion)` turned out *not* actually reachable from the Chart engine (their only real callers are the out-of-scope Gauge/Map engines) but were implemented anyway since the cost was low (`DrawCurve` via a hand-rolled cardinal-spline-to-Bezier conversion, since SkiaSharp has no native cardinal-spline primitive; `FillRegion` via a new `SkiaClipRegion.ToDrawablePath()`). `SetClip(IGraphicsPath,CombineMode)` was confirmed unreachable too and left a no-op, mirroring `SvgChartGraphics`'s identical no-op for the same overload. Added 8 more functional Skia tests (20 total). A fifth pass (2026-07-22) audited the 4 annotation files previously flagged as "not yet scoped" (`Annotation.cs`/`CalloutAnnotation.cs`/`PolylineAnnotation.cs`/`AnnotationPathPointCollection.cs`) and found all already fully converted or correctly scoped from an earlier pass — `CalloutAnnotation.cs`'s `GraphicsPathIterator` substitute and cloud-geometry bridge-at-sink were already in place, `PolylineAnnotation.cs`'s `Paint()` hot path was already fully `IGraphicsPath`/`IPen`-typed, and the remaining concrete fields/locals in all 4 files (`PolylineAnnotation.path`, `Annotation.startMovePathRel`, `Annotation.PaintSelectionHandles`'s concrete overload, 2 `StringFormat` locals feeding the shared `SmartLabels` holdout) either never cross into `IChartRenderingEngine` or are already-covered dual-overload/systemic holdouts — no code changes needed, just documentation correction. A sixth pass (2026-07-23) completed **B1b**, previously deferred as "superseded": `ChartGraphics`'s shared `pen` field retyped `Pen`→`IPen` (its 2 direct consumers, `DrawLineAbs`/the concrete `FillRectangleRel`, needed no body changes — overload resolution picked the existing `IPen`-typed sinks automatically); the concrete `FillRectangleAbs`/`DrawPathAbs(GraphicsPath,...)` became one-line forwards to their interface-typed siblings (`FillRectangleAbsResource` directly, `DrawPathAbs(IGraphicsPath,...)` via a `PathPoints`/`PathTypes` bridge) instead of duplicating logic, which let `solidBrush` — now readerless — be deleted outright rather than kept as dead weight. Fixed one real gap surfaced along the way: 3 files (`LineAnnotation.cs`/`PolylineAnnotation.cs`/`CalloutAnnotation.cs`) read the field directly for `CustomStartCap`/`CustomEndCap` (the documented C3 `AdjustableArrowCap` permanent holdout) — added those as real `IPen` members (Gdi-backed on both engines' `GdiPen`, `NotSupportedException` on `SkiaPen`, same precedent as other unsupported Skia pen/brush combinations) rather than reversing the holdout. Verified: `dotnet build --no-incremental` 0 errors, `VisualRegressionTests` 80/80, `Chart.Rdl.Tests` 1/1. A seventh pass (2026-07-23) found `ChartGraphics.FillRectangleRel`'s main 18-arg overload had the exact same shape as B1b's `FillRectangleAbs` — an identical-signature interface-typed sibling (`FillRectangleRelResource`, already added in an earlier E1 pass) that was already verified functionally equivalent — so it collapsed to a one-line forward the same way, removing another ~180 lines of duplicate concrete Brush/Pen/ImageAttributes logic. This is a real chokepoint (nearly every bar/column-style 2D fill routes through it), unlike most of the remaining `ChartGraphics.cs` allocations which are self-contained and lower priority. Verified: `dotnet build --no-incremental` 0 errors, `VisualRegressionTests` 80/80, `Chart.Rdl.Tests` 1/1. Remaining: Skia gradient/hatch brushes (no consumer needs them yet) — see task doc for exact detail.

**Gauge engine** (`tasks/gauge-gdi-type-abstraction.md`): Milestones A-A4 (foundation + clip-region), B1 (factory injection), B3 (the "atomic rewrite" of `KnobStyleAttrib`/`NeedleStyleAttrib`/`MarkerStyleAttrib`/`BarStyleAttrib`, 2026-07-22) are done, verified pixel-exact (55/55 tests, zero baseline diffs). B2 (real call-site conversion) is substantially done. A re-check (2026-07-22) found one of its named open items — `ScaleBase.GetLightBrush`'s `Circle` branch needing `IPathGradientBrush.Blend` — already stale (that gap closed as a side effect of Chart's E1 work), which unblocked converting `GetLightBrush`/`DrawTickMark` for real: both retyped to `IBrush`/`IGraphicsPath`/`IPen`, plus a new interface-typed `GaugeGraphics.DrawPathShadowAbs` sibling. The one non-obvious piece — a wholesale `LinearGradientBrush.Transform = matrix` assignment, which `ILinearGradientBrush` deliberately has no generalized setter for — was resolved by recognizing `matrix` is always a pure rotation about a single point in both real call sites, decomposable back to `SetRotationTransform(angle, center)` via a new `ScaleBase.DecomposeRotation` helper (exact fixed-point math, not an approximation; verified by 4 new focused unit tests since no sample gauge exercises tick-mark gradients). A follow-up pass (2026-07-23) converted `CircularScale.cs`/`LinearScale.cs`'s twin `DrawLabel` methods (identical `Font`/`StringFormat`/`SolidBrush` shape) — `stringFormat`/brushes now `ITextFormat`/`IBrush`, with the resized `Font` bridged to `IChartFont` right at the `DrawString` call sites since it's still needed concrete for measurement. Both sample gauges render visible tick-mark labels through this exact path, so the zero-diff pass result is a genuine pixel-verified conversion. A further pass (2026-07-23) converted `DigitalSegment.cs`/`SegmentsCache.cs` (the seven/fourteen-segment LED digit geometry used by `NumericIndicator`'s `Digital7Segment`/`Digital14Segment` styles) — a `static` utility class with no `GaugeGraphics` instance of its own, so every method now threads an `IGaugeDrawingResourceFactory` parameter down from the caller instead; `SegmentsCache`'s cached-`GraphicsPath.Clone()` semantics were reconstructed via `CreatePath(PointF[], byte[])` since `IGraphicsPath` deliberately has no `Clone()`. Since no sample gauge previously used those indicator styles, a 4th sample gauge and baseline were added specifically to pixel-verify it (baseline generated from the pre-conversion code via the git-stash technique). A sixth pass (2026-07-23) scoped and converted `HotRegionList.SetHotRegion`'s storage side: it now has `IGraphicsPath`-typed overloads (bridging to a fresh concrete `GraphicsPath` per path at the storage sink, same pattern as Chart's already-converted `HotRegionsList.AddHotRegion`), and `CircularPointer`/`Knob`/`LinearPointer`'s private hot-region plumbing — the only real callers already holding an `IGraphicsPath` upstream and needlessly unwrap-and-cloning it back to concrete — was converted end-to-end to use it directly. `GaugeGraphics.DrawRadialSelection` (a draw call, not hit-test storage) and ~10 other `SetHotRegion` call sites that build paths via still-concrete-only legacy methods (`GetFramePath`, `GetCircularRangePath`/`GetLinearRangePath`, etc.) are unconverted — that belongs to converting those path-builders themselves. A seventh pass (2026-07-23) closed that gap: `GaugeGraphics.GetCircularRangePath`/`GetLinearRangePath`/`GetThermometerPath` and `CircularScale`/`LinearScale`'s private `GetBarPath` all now return `IGraphicsPath` (expanding the one real gap found, `AddArc(RectangleF,...)`, to its 4-float form — matching an existing Chart-engine precedent, `ChartGraphics.DrawPieRel` — rather than adding a new interface member), and every real caller (`CircularRange`/`LinearRange`'s `Render`/`GetPath`, `CircularPointer`/`LinearPointer`'s attrib builders, `BackFrame.RenderGlassEffect`, `CircularGauge`/`LinearGauge`'s `RenderStaticShadows`, `CircularScale.GetShadowPath`/`GetSelectionMarkers`) converted end-to-end, dropping now-redundant `WrapPath` bridges. The one deliberate exception: `CircularScale`'s selection-draw method still bridges back to concrete via `UnwrapPath` immediately before `DrawRadialSelection`, which stays concrete-only by design. This closes Open items #8 and #9. An eighth pass (2026-07-23) re-scoped `XamlRenderer.cs`/`XamlLayer.cs`, previously mislabeled "architecturally blocked" — that label didn't survive re-investigation. Two of its three suspected prerequisite gaps were refuted (`ColorBlend` needed no interface change; the "no live `GaugeGraphics`" problem was the same shape as `DigitalSegment.cs`'s already-solved static-threading pattern, since every real caller already had a live `GaugeGraphics` one frame above the parsing entry point), and the third (scale/shear on brush transforms) closed with one new generalized interface member, `ILinearGradientBrush`/`IPathGradientBrush.MultiplyTransform(Matrix3x2, MatrixOrder)`. Two more small gaps surfaced during implementation: a point-to-point `IGaugeDrawingResourceFactory.CreateLinearGradientBrush(PointF, PointF, Color, Color)` overload, and `ISolidBrush.Clone()` (mirroring `IPen.Clone()`) for `XamlLayer.SetSingleBrush`. `XamlRenderer` now takes an `IGaugeDrawingResourceFactory` at construction and builds `IGraphicsPath`/`IBrush`/`IPen` through it; every real caller (`BackFrame`, `CircularPointer`, `StateIndicator`) threads it through. A follow-up pass (2026-07-23) added the durable 5th sample gauge/baseline recommended above (`RenderCircularGaugeWithCustomXamlFrameAndCap`, `BackFrameShape.CustomSemiCircularN1` + `CapStyle.CustomCap1`, chosen specifically because their XAML resources exercise `MultiplyTransform`'s `SkewTransform` branch and `AddBeziers`'s chained-Bezier StreamGeometry, unlike the earlier one-off rotate-only check) — 80/80 `VisualRegressionTests`, zero baseline diffs. This closes Open item #2. A final pass (2026-07-23) investigated the last remaining item, `GaugeCore.Paint`/`PrintPaint`/`SaveTo`/`GetGraphics`, previously thought blocked on a `BufferBitmap`/`IRenderSurface` equivalent — re-reading all four methods end to end found no hard blocker: `GetGraphics` already wraps its raw `Graphics` parameter into `GaugeGraphics` via the established settable-property pattern, every actual draw call already routes through the converted `GaugeGraphics`/`IGaugeRenderingEngine` wrapper, and the only remaining raw-`Graphics` code (`SaveTo`'s background-fill/disabled-state-dimming pre/post-processing and the final `Bitmap.Save`) is the same shape as a residual Chart's own D2 pass deliberately left concrete. The deeper reason a `BufferBitmap`→`IRenderSurface`-equivalent split isn't worth building now: Chart's split only paid off because a second backend (Skia) exists to select between, and the Gauge engine has no non-Gdi adapter at all — introducing the abstraction today would have exactly one implementation and zero call sites that benefit. No code changes made. **This closes Open item #6, the last open item — the entire Gauge GDI+ → interface abstraction effort (`tasks/gauge-gdi-type-abstraction.md`) is now complete.**

**Map engine** (`Microsoft.Reporting.Map.WebForms`) is a third, separate GDI+-coupled rendering engine discovered during this work (347 files, ~22,400 lines) — a real, wired-in RDL `<Map>` feature (`MapMapper.cs` + ~20 sibling mappers), not dead code. **Decision (2026-07-22): deferred, LOW priority, scheduled after PDF Phase 1** — see `docs/decisions.md`. Its built-in Bing Maps tile-layer integration (`Microsoft.Reporting.Map.WebForms.BingMaps`) is independently broken: preliminary research indicates Bing Maps (Free and Enterprise) has been end-of-lifed for RDL/RDLC consumers, so a cross-platform migration wouldn't restore the feature's main value-add on its own. If revisited, the tile-service question needs its own decision first — a Google Maps adapter (commercial API, usage-based billing) or an OpenStreetMap adapter (tile-server-based, no API key, but self-hosting/usage-policy considerations at production volume) are the two candidate replacements — before any GDI+→interface migration work on Map itself would be worth starting.

**Verification convention** (both engines, every increment): `dotnet build` 0 errors + full test suite (`VisualRegressionTests` + `Chart.Rdl.Tests`) passing + zero baseline PNG diffs. See `docs/rendering-abstractions.md` for the git-stash baseline-generation technique used for previously-uncovered render paths.

## PDF Phase 1: SkiaSharp Graphics Migration (LOWER PRIORITY)

**Status:** Analysis complete, not started. **Blocker:** Metafile/EMF generation has no cross-platform equivalent — full graphics-stack replacement required, not incremental fixes.

**Reference:** `tasks/pdf-render-callstack-analysis.md` (5-phase roadmap), `tasks/pdf-quick-reference.md` (cheat sheet).

**Recommendation:** unchanged — lower priority than Chart/Gauge, since Excel/PDF *bodies* already render cross-platform and PDF's blocker is architecturally the deepest of the three.

---

## Completed Analysis & Documentation

| Category | Status | Details |
|----------|--------|---------|
| ✅ **.NET 10 Upgrade** | COMPLETE | All projects migrated, tests passing |
| ✅ **ImageSharp Integration** | COMPLETE | Image metrics cross-platform |
| ✅ **Excel image/format abstraction** | COMPLETE | See `docs/rendering-abstractions.md` |
| ✅ **Chart engine GDI+ abstraction** | Substantially COMPLETE | See `tasks/chart-gdi-type-abstraction.md` |
| ✅ **Gauge engine GDI+ abstraction** | COMPLETE | See `tasks/gauge-gdi-type-abstraction.md` |
| 📋 **PDF cross-platform migration** | NOT STARTED | See `tasks/pdf-render-callstack-analysis.md` |

---

## Documentation index

- `docs/rendering-abstractions.md` — Excel/PDF renderer factory design + Chart/Gauge Ports & Adapters architecture (interfaces, namespaces, recurring patterns)
- `docs/platform-support.md` — current Windows/Linux/macOS support matrix and known gaps
- `docs/decisions.md` — architecture decisions and why (OxyPlot retraction, SkiaSharp re-target, per-method vs. per-type conversion, etc.)
- `docs/troubleshooting.md` — common issues and known quirks found during the migration
- `docs/architecture-map.md` / `docs/build-and-test.md` / `docs/renderer-extension-guide.md` / `docs/examples.md` — supporting reference docs
- `tasks/chart-gdi-type-abstraction.md` / `tasks/gauge-gdi-type-abstraction.md` — active migration progress + open items
- `tasks/chart-cross-platform-implementation.md` — overall Chart/Gauge phase plan
- `tasks/pdf-render-callstack-analysis.md` / `tasks/pdf-quick-reference.md` — PDF migration roadmap (not started)
- `tasks/adapter-layer-refactor.md` — broader adapter-layer scope and README compatibility-gap follow-ups
- `tasks/chart-library-decision.md` / `tasks/chart-image-abstraction-analysis.md` — retraction/superseded pointers

## Notes

- Update this file's status table as milestones complete; keep detailed progress in the linked `tasks/*.md` files, not here.
- Durable architecture facts, decisions, and known gaps belong in `docs/`, not in task-tracking narrative.
