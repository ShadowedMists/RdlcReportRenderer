# Chart Engine: GDI+ Type Abstraction — Task Scope

**Parent plan:** [`chart-cross-platform-implementation.md`](chart-cross-platform-implementation.md) (this doc details Phase 3's "translation strategy")
**Status:** 📋 Scoping — depends on Phase 0 spike to confirm the chosen strategy
**Goal:** Replace concrete GDI+ resource types with backend-agnostic **interfaces + an abstract factory**, so the chart engine can run on GDI+ (Windows), SkiaSharp (Linux/macOS), and any future backend.

---

## 1. Design principle

Today the drawing seam `IChartRenderingEngine` is abstract, but the **types passed through it are concrete GDI+ classes** (`Pen`, `Brush`, `Font`, `GraphicsPath`, …). A backend can only be swapped if those resource types are also abstract. The target is **Ports & Adapters**:

```
Painters (73 files)  ──►  ChartGraphics (chokepoint)  ──►  IChartRenderingEngine
        │                        │                                │
        └── construct resources via ─── IDrawingResourceFactory ──┘   (the PORT)
                                          ▲            ▲
                                   GdiResourceFactory  SkiaResourceFactory   (ADAPTERS)
```

Instead of `new Pen(color, width)`, code calls `factory.CreatePen(color, width)` and receives an `IPen`. Each backend implements the factory and the resource interfaces.

---

## 2. Type inventory (measured usage)

Counts are occurrences / files across `Microsoft.Reporting.Chart.WebForms`. Use them as **complexity indicators**, not time estimates.

### 2.1 KEEP as-is — portable value types (no work)

These live in `System.Drawing.Primitives`, which is **fully cross-platform** (no GDI+). Abstracting them would mean thousands of edits for zero portability gain.

| Type | Occurrences | Disposition |
|------|-------------|-------------|
| `Color` | 1207 | ✅ Keep |
| `PointF` / `Point` | 841 | ✅ Keep |
| `RectangleF` / `Rectangle` | 517 | ✅ Keep |
| `SizeF` / `Size` | 313 | ✅ Keep |

**Total ~2,878 occurrences require no change.** This is the single most important scoping fact: the bulk of "System.Drawing" usage is already portable.

### 2.2 ABSTRACT — GDI+-backed resource types

| Type | Occ | Files | Nature | Abstraction | Size |
|------|-----|-------|--------|-------------|------|
| `GraphicsPath` | 290 | 24 | Mutable builder (AddLine/Arc/Bezier/CloseFigure) | **`IGraphicsPath`** (behavior interface) | 🔴 XL |
| `Font` | 232 | 31 | Descriptor + metrics source | **`IChartFont`** | 🔴 XL (most spread) |
| `StringFormat` | 145 | 19 | Descriptor (alignment/trimming/flags) | **`ITextFormat`** | 🟠 L |
| `Pen` | 127 (43 `new`) | 16 | Descriptor (color/width/dash/cap/align) | **`IPen`** | 🟠 L |
| `SolidBrush` + `Brush` | 164 | ~14 | Descriptor (fill) | **`IBrush`/`ISolidBrush`** | 🟠 L |
| `Matrix` | 58 | 11 | Transform w/ ops | **`ITransform`** or `System.Numerics.Matrix3x2` | 🟡 M |
| `ImageAttributes` | 25 | 6 | Image draw options | **`IImageDrawOptions`** | 🟡 M |
| `Region` | 17 | 6 | Clip region w/ combine ops | **`IClipRegion`** | 🟢 S |
| `HatchBrush` | 10 | 2 | Patterned fill | `IHatchBrush : IBrush` | 🟢 S |
| `LinearGradientBrush` | 15 | 2 | Gradient fill | `IGradientBrush : IBrush` | 🟢 S |
| `TextureBrush` | 12 | 2 | Image fill | `ITextureBrush : IBrush` | 🟢 S |

### 2.3 ABSTRACT — surface & output boundary

| Type | Occ | Files | Disposition |
|------|-----|-------|-------------|
| `new Bitmap` + `Graphics.FromImage` | 8 + 8 | 4 | **`IRenderSurfaceFactory`** → GDI+ `Bitmap` / Skia `SKSurface` |
| `Metafile` | 3 | 2 | ⚠️ **Windows-only** — leave behind a platform guard (no cross-platform EMF) |

### 2.4 Enums (GDI+-namespaced but portable)

`SmoothingMode`, `TextRenderingHint`, `GraphicsUnit`, `LineCap`, `DashStyle`, `FillMode`, `HatchStyle`, `StringAlignment`, `StringFormatFlags`, `CombineMode`, `MatrixOrder`, `PenAlignment`. These are value enums and compile cross-platform. **Decision:** keep initially; mirror into neutral enums only if we want to drop the `System.Drawing.Common` reference entirely (a later cleanup, not a blocker).

---

## 3. Design decision to make in the spike: interfaces vs. descriptor records

The user goal is **interfaces for extensibility**. Two of the resource categories split on how well that fits — call this out explicitly and decide in Phase 0:

| Category | Types | Recommended shape | Why |
|----------|-------|-------------------|-----|
| **Behavior-rich** | `GraphicsPath`, `Region`, `Matrix` | **Interface** (`IGraphicsPath`, `IClipRegion`, `ITransform`) | They accumulate state and have real operations; backends need their own implementations. |
| **Immutable descriptors** | `Pen`, `Brush`, `Font`, `StringFormat` | **Interface OR `readonly record struct`** | They are parameter bags. Interfaces give maximum extensibility (the stated goal); records are simpler, allocation-cheap, and let each backend translate+cache at draw time. |

**Recommendation:** use **interfaces for all** to satisfy the extensibility goal and keep one consistent factory pattern, but allow descriptor `record` implementations *behind* those interfaces where a resource is pure data. This keeps the public shape uniform (everything is an interface + factory) without forcing heavyweight objects for what is really data. Confirm during the spike against one real backend.

---

## 4. Target namespace & core artifacts

Proposed internal namespace: `Microsoft.Reporting.Chart.WebForms.Rendering`.

**Ports (interfaces) to create:**
- `IDrawingResourceFactory` — creates every resource below (the central port)
- `IRenderSurfaceFactory` — creates the draw surface + encodes output
- `IPen`, `IBrush` (+ `ISolidBrush`, `IHatchBrush`, `IGradientBrush`, `ITextureBrush`)
- `IChartFont`, `ITextFormat`
- `IGraphicsPath`, `IClipRegion`, `ITransform`
- `IImageDrawOptions`

**Adapters (implementations):**
- GDI+ set: `GdiResourceFactory`, `GdiPen`, `GdiBrush`, … (wrap existing `System.Drawing` objects — behavior-preserving on Windows)
- Skia set: `SkiaResourceFactory`, `SkiaPen`, … (map to `SKPaint`/`SKPath`/`SKFont`)

**Modified seam:** `IChartRenderingEngine` method signatures change from `System.Drawing` types to the interfaces (e.g. `DrawLine(IPen, PointF, PointF)`). `PointF/RectangleF/Color` stay concrete.

---

## 5. Task breakdown (ordered)

Each task is independently reviewable. Sizes are relative (S/M/L/XL) from §2 counts.

### Milestone A — Foundation (no behavior change)

- [x] **A1. Create the `Rendering` namespace + port interfaces** (`IDrawingResourceFactory`, resource interfaces, `IRenderSurfaceFactory`). Interfaces only, no implementations. 🟢 S
  → **Drafted** in [`Microsoft.Reporting.Chart.WebForms/Rendering/`](../Microsoft.ReportViewer.DataVisualization/Microsoft.Reporting.Chart.WebForms/Rendering/): `IRenderingResource`, `IPen`, `IBrush` (+Solid/Gradient/Texture/Hatch), `IChartFont`, `ITextFormat`, `IGraphicsPath`, `IClipRegion`, `IImageDrawOptions`, `IDrawingResourceFactory`, `IRenderSurface`/`IRenderSurfaceFactory`. Member surfaces taken directly from Appendix A. Builds clean (0 errors). Design decisions baked in: value types/enums kept concrete; resources extend `IDisposable`; `Transform` uses `System.Numerics.Matrix3x2`.
- [x] **A2. Implement the GDI+ adapter set** wrapping existing `System.Drawing` objects (`GdiPen : IPen`, etc.) + `GdiResourceFactory`. Behavior-identical to today. 🟠 L
  **Drafted** in [`Microsoft.Reporting.Chart.WebForms/Rendering/Gdi/`](../Microsoft.ReportViewer.DataVisualization/Microsoft.Reporting.Chart.WebForms/Rendering/Gdi/) — one adapter class per resource interface (`GdiPen`, `GdiSolidBrush`/`GdiLinearGradientBrush`/`GdiTextureBrush`/`GdiHatchBrush`, `GdiChartFont`, `GdiTextFormat`, `GdiGraphicsPath`, `GdiClipRegion`, `GdiImageDrawOptions`, `GdiChartImage`, `GdiRenderSurface`/`GdiRenderSurfaceFactory`) plus `GdiResourceFactory : IDrawingResourceFactory` wiring them together. Each adapter constructs the exact same concrete GDI+ object the engine builds today and exposes it via an internal `Native*` property so sibling adapters (and, later, `ChartGraphics` itself) can reach the underlying object without a second abstraction layer. Builds clean (0 errors, 0 new warnings — confirmed no `Rendering\Gdi` hits in the build's CA1416 output). Not yet wired into `ChartGraphics`/`IChartRenderingEngine` — that's A3/B1. `IRenderSurface.Encode` only handles raster formats (Png/Jpeg/Bmp); Emf/EmfPlus/EmfDual remain out of scope for this surface until the Milestone D metafile path exists.
- [x] **A3. Add `IChartRenderingEngine` overloads that accept the interfaces**, delegating to the existing GDI+ methods (keep old signatures temporarily to avoid a big-bang change). 🟡 M
  **Drafted.** Added ~30 interface-typed overloads to [`IChartRenderingEngine.cs`](../Microsoft.ReportViewer.DataVisualization/Microsoft.Reporting.Chart.WebForms/IChartRenderingEngine.cs) — one per existing GDI+-typed member, plus `GetClipRegion`/`SetClipRegion` and `GetTransform`/`SetTransform` as method-pair equivalents of the `Clip`/`Transform` properties (properties can't be overloaded by type). All three implementers updated: `GdiGraphics` unwraps each `Rendering.*` interface to its wrapped GDI+ object (via private `Native(...)` helpers) and delegates to the pre-existing concrete-typed method — same call, same output. `ChartRenderingEngine` (the Gdi/Svg dispatcher) adds pure passthroughs to `RenderingObject`. `SvgChartGraphics` (legacy, not on the report render path) adds its own `Native(...)` unwrap helpers, since it doesn't share `GdiGraphics`'s. Added `GdiClipRegion(Region existingRegion)` constructor to support wrapping the live `Graphics.Clip` for `GetClipRegion()`. Builds clean (0 errors, 0 new warnings in any of the four changed files). No caller passes the new overloads yet — that's Milestone B.

### Milestone B — Migrate the chokepoint

- [x] **B1a. Inject `IDrawingResourceFactory` into `ChartGraphics`** ([ChartGraphics.cs:11](../Microsoft.ReportViewer.DataVisualization/Microsoft.Reporting.Chart.WebForms/ChartGraphics.cs#L11)) as a constructor-provided field, defaulting to `GdiResourceFactory` when not supplied (both existing call sites — `ChartPicture.cs`, `TextAnnotation.cs` — are unaffected). Infrastructure only; the field isn't consumed yet. 🟢 S
- [ ] **B1b. Retype the `pen`/`solidBrush`/`myMatrix` fields** to `IPen`/`ISolidBrush`/`Matrix3x2`, sourced from `resourceFactory`. **Descoped from B1a — discovered blocked, not merely deferred:**
  - `pen` is drawn together with a still-concrete `GraphicsPath` in `DrawPath(pen, path)` ([ChartGraphics.cs:2295](../Microsoft.ReportViewer.DataVisualization/Microsoft.Reporting.Chart.WebForms/ChartGraphics.cs#L2295)); no `DrawPath(IPen, GraphicsPath)` overload exists (by design — A3 only added matched-shape overloads). Needs **C7** (`GraphicsPath` → `IGraphicsPath`) first.
  - `solidBrush` is interchanged with `GetGradientBrush`/`GetHatchBrush`/`GetTextureBrush` results into one `Brush brush` local ([ChartGraphics.cs:2052-2063](../Microsoft.ReportViewer.DataVisualization/Microsoft.Reporting.Chart.WebForms/ChartGraphics.cs#L2052-L2063), [2202-2224](../Microsoft.ReportViewer.DataVisualization/Microsoft.Reporting.Chart.WebForms/ChartGraphics.cs#L2202-L2224)); those helpers still return concrete `Brush`. Needs **C4** (Brush family → `IBrush`) first, so all of them return `IBrush`-compatible values together.
  - ~~`myMatrix` feeds raw `GraphicsPath.Transform`/`Region.Transform` throughout the file~~ — **resolved by C1+C2**: `myMatrix` is now `Matrix3x2` (see C2 below); the handful of remaining `GraphicsPath.Transform(matrix)` call sites bridge via `Matrix3x2Extensions.ToGdiMatrix()` until C7 lands. `pen`/`solidBrush` remain blocked as described above.
  - **Do not** paper over this with mixed transitional overloads (`DrawPath(IPen, GraphicsPath)` etc.) or backend-specific casts inside `ChartGraphics` — both were considered and rejected as throwaway indirection. Revisit B1b once C4/C7 land, retyping the remaining two fields together in one coherent pass.
- [ ] **B2. Convert `ChartGraphics` high-level methods** (`DrawLineRel`, `FillRectangleAbs`, …) to construct resources via the factory. Depends on B1b. 🟠 L
  **Scope re-measured, materially larger than L.** Correction to an earlier note: the "20+ files" calling `GetGradientBrush`/`GetHatchBrush`/`GetTextureBrush` is the combined count across **three separate GDI+-coupled engines** — the real Chart-engine blast radius is just 6 files (`ChartGraphics.cs` + `AxisScaleSegment.cs` + 4 files under `ChartTypes\`: `AreaChart.cs`, `PieChart.cs`, `RangeChart.cs`, `StackedAreaChart.cs`). The other ~14 files are `GaugeGraphics`/`MapGraphics` call sites — **`GaugeGraphics` (`Microsoft.Reporting.Gauge.WebForms`) and `MapGraphics` (`Microsoft.Reporting.Map.WebForms`) each have their own separate, parallel `GetHatchBrush`/`GetGradientBrush` methods**, i.e. there are three GDI+-coupled rendering engines, not two — the parent plan only tracked gauge. Map's scope needs its own decision (in scope for this initiative, or stays Windows-only?).

  **Attempted and reverted (twice) — precise findings, not just "needs C7":**
  1. Converting `ChartGraphics.GetHatchBrush`/`GetGradientBrush`/`GetPieGradientBrush` to return `IBrush`/`IHatchBrush`/`IPathGradientBrush` compiles fine in isolation, but their sole real caller, `FillRectangleRel` (~180 lines), assigns the result to a `Brush brush` local that also flows from `GetTextureBrush` (still concrete) and gets passed to `FillRectangle`/`DrawCircleAbs` — reverted (confirmed zero net diff via `git diff --stat`).
  2. Converting `DrawCircleAbs(Pen, Brush, ...)` → `DrawCircleAbs(IPen, IBrush, ...)` (plus its own local `GraphicsPath` and `GetSector3DBrush`) **worked** for its `Pen`/`GraphicsPath` half — 3 independent external callers (`Axis.cs`, `PointAndFigureChart.cs` ×2) converted cleanly, no `Widen` coupling this time. But `DrawCircleAbs`'s `Brush brush` parameter is called from `FillRectangleRel`/`FillRectangleAbs` with a value that may have originated from `GetTextureBrush` — which loads images via `Microsoft.Reporting.Chart.WebForms.Utilities.ImageLoader.LoadImage(string) → System.Drawing.Image` (concrete, GDI+-only, and **entirely separate from the existing `IImageProvider` abstraction** in `chart-image-abstraction-analysis.md` — confirmed by reading `ImageLoader.cs`; the two never call each other). Reverted (confirmed zero net diff) rather than leave `ChartGraphics.cs` broken.
  3. Kept one genuinely independent, low-risk addition from this investigation: `ILinearGradientBrush.LinearColors` (GDI+'s `LinearGradientBrush.LinearColors`, needed by `GetSector3DBrush`'s color-extraction logic) — a real gap, harmless to add regardless of when the call sites convert.

  **Conclusion: converting `ChartGraphics`'s brush/circle/path methods requires solving image loading first** (a 4th, previously-unrecognized prerequisite alongside C4/C7) **or converting a very large, coupled block of `ChartGraphics.cs` (`FillRectangleRel`+`FillRectangleAbs`+`DrawCircleAbs`+`GetHatchBrush`+`GetGradientBrush`+`GetTextureBrush`+`GetSector3DBrush`, likely 500+ lines) plus `ImageLoader` all in one atomic pass.** This is not a small-increment task like C1-C3.

  **Attempted the image-loading prerequisite next (per explicit direction to do the full atomic conversion) — scope grew again, work paused here.** Added the additive, harmless groundwork: `IDrawingResourceFactory.LoadImage(Stream) : IChartImage`, implemented in `GdiResourceFactory` (`Image.FromStream`) and a new `SkiaChartImage`/`SkiaResourceFactory` (`SKBitmap.Decode`) — both verified building clean, nothing existing depends on them yet so this is safe to keep regardless of what happens next. But wiring it into `ImageLoader.LoadImage` (`Microsoft.Reporting.Chart.WebForms.Utilities/ImageLoader.cs`) turned out bigger than scoped: its DPI-matching helpers (`GetAdjustedImageSize`/`DoDpisMatch`/`GetScaledImage`, all typed on concrete `Image`+`Graphics`) are called from **16+ sites across 4 files** (`Axis.cs`, `ChartGraphics.cs` ×6, `ImageAnnotation.cs`, `LegendCell.cs` ×4) — not just from `GetTextureBrush` as assumed. Converting `LoadImage`'s return type ripples into all of them, and DPI itself isn't cleanly exposed on `IChartRenderingEngine` yet (same `Graphics`-property gap the spike's finding #4 already flagged for Milestone D). **Decision: paused here rather than keep expanding scope.** Three options were measured: (a) simplify by dropping GDI+'s DPI-mismatch image-rescaling special case (a print/high-DPI-only nuance) and convert all 16+ sites on that simplified basis, (b) leave the DPI-matching helpers Windows-only/concrete permanently and convert only `GetTextureBrush`'s non-DPI-rescaling common path, or (c) tackle Milestone D's `Graphics`-property gap first so DPI has a proper interface-typed home, then this converts more cleanly.

  **Decision (2026-07-18): Option (a) — assume a fixed 96 DPI baseline, drop the mismatch-rescale path entirely.** `ImageLoader`'s DPI-matching exists to compensate when a loaded image's embedded resolution (`Image.HorizontalResolution`/`VerticalResolution`) differs from the target `Graphics.DpiX`/`DpiY` — a print/high-DPI nuance, essentially never true for ordinary screen/web chart images (the vast majority are untagged or already 96 DPI, which is also the only case `IChartImage`/SkiaSharp can cheaply support: `SKBitmap` carries no resolution metadata, so faithfully porting this would require adding resolution properties to `IChartImage` — defaultable but meaningless on the Skia side — plus a resize capability, just to serve an edge case that doesn't apply to this engine's real usage). Rejected (b) because the "DPI-matching only guards `GetTextureBrush`" premise turned out false — `Axis.cs`/`ImageAnnotation.cs`/`LegendCell.cs` call `GetAdjustedImageSize` directly for unrelated image-sizing purposes, so there's no single narrow path to keep GDI+-only. Rejected (c) as solving a much larger problem (a proper interface-typed DPI/Graphics home) than this narrow simplification needs. **Accepted trade-off:** this changes shared code, so it affects Windows too, not just Linux — an image asset explicitly authored at a non-96 DPI (e.g. an old print-oriented logo) will now render at native pixel size instead of being auto-rescaled to compensate. Judged acceptable: narrow, cosmetic, and not a scenario this chart engine's real usage exercises. Implemented: `ImageLoader.GetAdjustedImageSize(Image, Graphics, ref SizeF)` now just returns `image.Width`/`image.Height`; `DoDpisMatch`/`GetScaledImage` (now unreachable) removed; `ChartGraphics.GetTextureBrush`'s DPI-mismatch branch removed. No caller signatures changed (the string-based `GetAdjustedImageSize(string, Graphics, ref SizeF)` overload callers — `Axis.cs`, `BarChart.cs`, etc. — were unaffected, since only the internals of the two static helpers changed). Verified: build 0 errors, all 5 visual regression tests pass, zero baseline changes.

  **Resumed the atomic conversion (2026-07-18) — three more previously-hidden gaps found behind the now-fixed DPI blocker; paused again before another revert.** With DPI resolved, re-attempted converting `GetHatchBrush`/`GetGradientBrush`/`GetPieGradientBrush`/`GetSector3DBrush`/`GetTextureBrush`/`DrawCircleAbs` to `IBrush`-family types together (the full atomic pass, not a partial slice this time). Found the cluster is still not convertible as a whole, for reasons beyond DPI:
  1. **`GetTextureBrush` itself needs three new capabilities, not just a DPI fix:** (a) a bridge from `ImageLoader`'s concrete `Image` (still GDI+-only — `ImageLoader.cs` itself wasn't touched by the DPI simplification, only its DPI math) into `IChartImage`, since nothing today converts an already-loaded `Image` into the interface type; (b) `IDrawingResourceFactory.CreateTextureBrush(IChartImage, WrapMode)` has no overload accepting a draw `RectangleF` + `IImageDrawOptions`, but `GetTextureBrush` constructs via GDI+'s `TextureBrush(image, rect, imageAttributes)` three-argument form; (c) **`IImageDrawOptions.SetColorRemap(from, to)` does not actually replicate the behavior `GetTextureBrush`/`FillRectangleRel`/`FillRectangleAbs`/`DrawPathAbs`/`DrawMarkerAbs` all rely on** — those call GDI+'s `ImageAttributes.SetColorKey(color, color, ColorAdjustType.Default)` (treats a colour *range* as transparent), whereas `SetColorRemap` is implemented via `SetRemapTable` (a straight 1:1 colour substitution, no inherent transparency) — using it as a stand-in would be a silent behavior change, not a faithful port. `IImageDrawOptions` needs its own new member (e.g. `SetTransparentColor(Color)`) before any color-keyed image code can convert correctly.
  2. **Confirmed (independently of DPI) that nothing in the cluster is convertible in isolation without (1):** `GetHatchBrush`/`GetGradientBrush`/`GetPieGradientBrush`'s return values still flow into the same `Brush brush` local in `FillRectangleRel`/`FillRectangleAbs` that also receives `GetTextureBrush`'s result (same shape as reverted attempt #1) — this is unchanged by the DPI fix, since the blocker was never DPI itself, only what stood behind `GetTextureBrush`.
  3. **The shared `pen`/`solidBrush` fields remain correctly left alone (per B1b), but the blast radius if ever touched is now precisely measured, not just "needs C7":** `DrawPathAbs` (`ChartGraphics.cs:2175`) mutates `pen`/`solidBrush` exactly like `FillRectangleRel`/`FillRectangleAbs` do, but also calls `DrawPath(pen, path)` where `path` is `DrawPathAbs`'s own **public, concrete `GraphicsPath` parameter** — retyping the shared fields would force `DrawPathAbs`'s signature to `IGraphicsPath` too, rippling into its 6 real Chart-engine callers (`PolylineAnnotation.cs`, `CalloutAnnotation.cs`, `ArrowAnnotation.cs`, `FunnelChart.cs`, `Borders3D/SunkenBorder.cs`, `Borders3D/EmbossBorder.cs` — confirmed via grep; `Gauge.WebForms`/`Map.WebForms` have their own separate `DrawPathAbs`, not shared with `ChartGraphics`, so they're not additional blast radius here). **This is why the current slice deliberately keeps `DrawCircleAbs`'s `Pen`/`GraphicsPath` parameters concrete** (proven safe in the earlier reverted attempt #2) and only targets its `Brush` parameter — avoiding this ripple entirely for now.
  4. **One real, independent bug found and fixed while investigating:** `GdiGraphics.Native(IBrush)` (the switch that unwraps an `IBrush` to its native GDI+ `Brush` for the interface-typed `IChartRenderingEngine` overloads) was missing a `GdiPathGradientBrush` case — would throw `NotSupportedException` today if any code called e.g. `FillPath(IBrush, IGraphicsPath)` with a path-gradient brush. Fixed (added the missing switch arm) — safe, independent of everything else, verified via build + all 5 tests passing.

  **Decision: paused again rather than push through (1)/(2) speculatively and risk a third revert.** Fixing gap 1(c) in particular (`SetColorKey`-equivalent semantics) is a real design decision (the transparent-range vs. straight-remap distinction affects correctness on both backends, not just Skia), not a mechanical wiring task — worth its own explicit sign-off before implementing, same as the DPI decision above.

  **Implemented (2026-07-18) via a dual-overload strategy, avoiding a third revert.** Fixed gaps 1(a)/(b)/(c): added `IImageDrawOptions.SetTransparentColor(Color)` (Gdi: `ImageAttributes.SetColorKey`; Skia: spike-scope stub) and `SetWrapMode(WrapMode)` (Gdi: `ImageAttributes.SetWrapMode`; Skia: stub); added `IDrawingResourceFactory.WrapImage(Image) : IChartImage` (Gdi: `new GdiChartImage(image)`; Skia: `NotSupportedException`, since `ImageLoader`'s pipeline is GDI+-only and unreachable there anyway) and `CreateTextureBrush(IChartImage, RectangleF, IImageDrawOptions)` (Gdi: new `GdiTextureBrush(Image, RectangleF, ImageAttributes)` constructor wrapping GDI+'s 3-arg `TextureBrush` ctor; Skia: spike-scope stub). With those in place, re-attempted (2) and hit the wall described above (return-type-only conflict, since `GetHatchBrush`/`GetGradientBrush`/`GetPieGradientBrush`/`GetTextureBrush` have no brush-typed parameter to overload on) — resolved it the same way Milestone A3 already established for `IChartRenderingEngine` (dual GDI+-typed/interface-typed overloads coexisting until callers migrate), rather than retrofitting the existing methods and forcing every caller to move at once:
  - Added **new**, separately-named interface-typed siblings, each calling `resourceFactory` instead of `new Xxx(...)`, coexisting with the untouched original concrete methods: `GetHatchBrushResource`, `GetGradientBrushResource` (→ `IBrush`, mixing `ILinearGradientBrush`/`IPathGradientBrush` per branch, matching `GetGradientBrush`'s own mixed-return shape), `GetPieGradientBrushResource` (→ `IPathGradientBrush`), `GetTextureBrushResource` (→ `ITextureBrush`, using the new `WrapImage`/`CreateTextureBrush(rect, options)` groundwork — the Metafile-compositing branch routes its composited `Bitmap` through the same `WrapImage`+`CreateTextureBrush` path rather than constructing a concrete `TextureBrush` directly).
  - `DrawCircleAbs`/`GetSector3DBrush` didn't need new names — both already have a `Pen`/`Brush`-typed parameter, so `DrawCircleAbs(IPen, IBrush, RectangleF, int, bool)`/`GetSector3DBrush(IBrush, float, float)` are true C# overloads of the originals. `DrawCircleAbs`'s own `graphicsPath` local converts to `IGraphicsPath` in the new overload too — safe because it's fully self-contained (built and consumed only within this one method, never handed to a caller), unlike `DrawPathAbs`'s public parameter.
  - **`FillRectangleRel`/`FillRectangleAbs`/`DrawPathAbs` were deliberately left untouched** — they keep calling the original concrete overloads exactly as before. The shared `pen`/`solidBrush` fields and `DrawPathAbs`'s 6-file ripple (see finding 3 above) remain a separate, larger future decision, not bundled into this pass.
  - Converted the 3 external call sites already known (from the earlier reverted attempt #2) to be self-contained: `PointAndFigureChart.cs`'s two inline `DrawCircleAbs` calls now construct via `graph.ResourceFactory.CreatePen(...)` instead of `new Pen(...)`. **`Axis.cs`'s `DrawCircularLine` call site was tried and reverted** — its local `pen` variable turned out not to be self-contained after all: later in the same method it's passed to `ChartGraphics.Widen(graphicsPath, pen)` (the already-documented, no-cross-platform-equivalent gap), which requires a concrete `Pen`. Left as the original concrete overload, confirmed via `git diff` showing only the two `PointAndFigureChart.cs` lines and the new `ChartGraphics.cs` methods changed.
  - Verified: build 0 errors (both incremental and `--no-incremental`), all 5 visual regression tests pass, zero baseline changes. `GdiPen`/`GdiTextureBrush`/etc. wrap the exact same GDI+ constructor calls as the code they replace, so this is provably zero-behavior-change on Windows.
  - **What this unblocks:** any new caller (Chart-engine or otherwise) can now get a fully interface-typed brush/circle-drawing surface without waiting on `FillRectangleRel`/`FillRectangleAbs`/`DrawPathAbs`'s own conversion. **What's still open:** those three methods (and the `pen`/`solidBrush` field retyping + `DrawPathAbs`'s 6-caller ripple) are real, separately-sized future work — not resolved by this pass, just no longer blocked on missing interface surface.

  **Took on the `DrawPathAbs` 6-file ripple next (2026-07-18) — 2 of 6 converted, 4 blocked on a new, systematic finding.** Added `DrawPathAbs(IGraphicsPath, ...)` (both the shadow-aware and plain 13-arg overloads) as a **new, fully self-contained sibling** — deliberately not touching the shared `pen`/`solidBrush` fields at all; it constructs its own local `IPen`/`IBrush` via `resourceFactory` (mirroring `DrawCircleAbs`'s interface-typed overload), so it doesn't need the fields retyped and doesn't reopen the "3 new gaps" investigation. Also added `CreateRoundedRectPathResource` (interface-typed sibling of `CreateRoundedRectPath`, same dual-overload/distinct-name reasoning as the brush getters — return type is the only difference, so it can't be a true C# overload).
  - **Converted cleanly:** `Borders3D/SunkenBorder.cs` and `Borders3D/EmbossBorder.cs` — each has exactly one `DrawPathAbs` call, fed by `CreateRoundedRectPathResource`, and the path is used *only* for that one draw call (a border decoration, never hit-tested). Verified: build 0 errors, all 5 tests pass, zero baseline changes.
  - **Blocked on a new, systematic finding, not per-file quirks:** the remaining 4 callers (`ArrowAnnotation.cs`, `PolylineAnnotation.cs`, `CalloutAnnotation.cs`, `FunnelChart.cs`) all follow the **same shape** — the `GraphicsPath` built for `DrawPathAbs` is *also* handed (directly, or via `.Clone()`/`AddPath()`) to `Chart.chartPicture.common.HotRegionsList.AddHotRegion(...)` for hit-testing/tooltips immediately afterward. Checked: **`HotRegionsList` has zero interface-typed overloads** — every one of its ~13 `AddHotRegion` overloads (`HotRegionsList.cs`) takes a concrete `GraphicsPath`, confirmed via grep across the codebase (same for the Gauge engine's own `AddHotRegion` methods). So the local path in these 4 files isn't "just" feeding `DrawPathAbs` — it's dual-purposed for drawing *and* hit-testing, and only the drawing half has an interface-typed home right now.
  - This is the same category of discovery as `ChartGraphics.Widen` blocking the `Axis.cs` revert earlier: a call site that looked self-contained (like `Borders3D`'s) turns out not to be, once you check every other consumer of the same object, not just the one you're converting.
  - **Not attempted:** duplicating path construction (build the geometry twice — once via `resourceFactory.CreatePath()` for `DrawPathAbs`, once via `new GraphicsPath()` for `AddHotRegion`) would technically work but adds real per-call-site complexity and a small perf cost, and papers over the actual gap (`HotRegionsList` has no interface-typed surface) rather than fixing it. Extending `HotRegionsList` itself with interface-typed overloads is the more honest fix, but is real, separately-sized scope (~13 overloads, likely mirroring the `GraphicsPath`→`IGraphicsPath` pattern, across a class not yet touched by this initiative at all) — not something to start speculatively mid-pass.
  - **Decision: paused here.** 2 of 6 files converted (both genuinely independent); the other 4 need either `HotRegionsList` interface-typed support or accepted duplicate-path-construction, and that choice wasn't made unilaterally.

  **Chose option 1 (extend `HotRegionsList`) — 5 of 6 files now converted; the 6th hit a deeper, unrelated gap.** Added two interface-typed `AddHotRegion` overloads to `HotRegionsList.cs`: `AddHotRegion(IGraphicsPath, bool, ChartGraphics, DataPoint, string, int)` and `AddHotRegion(ChartGraphics, IGraphicsPath, bool, string, string, string, object, ChartElementType)` — both rebuild a concrete `GraphicsPath` from `path.PathPoints`/`path.PathTypes` (already interface-exposed properties) and delegate to the existing concrete overload. This is backend-agnostic (no casting to a `Gdi*`/`Skia*` adapter type) and leaves `HotRegion`'s internal storage and `CheckHotRegions`'s hit-testing (which itself uses `GraphicsPathIterator`, GDI+-only, no interface equivalent) completely untouched. Also added the same interface-typed-counterpart pattern to `Annotation.PaintSelectionHandles` (only reads `PathPoints`, so the bridge is trivial) and `IGraphicsPath.AddClosedCurve(PointF[], float tension)` (a real, previously-missing overload — GDI+'s tensioned closed-curve variant, needed by `FunnelChart.cs`; implemented in `GdiGraphicsPath`, stubbed in `SkiaGraphicsPath`).
  - **Converted:** `ArrowAnnotation.cs` (`GetArrowPath` now returns `IGraphicsPath`, rotation via `Matrix3x2.RotateAt` instead of concrete `Matrix`), `FunnelChart.cs` (`DrawFunnel3DSquareSegment`'s 3 self-contained paths + `DrawFunnelCircularSegment`'s 2 self-contained edge-cap paths — its main shared path stays concrete, see below), `PolylineAnnotation.cs` (the drawn path now `IGraphicsPath`; the non-polygon hit-region path still builds a concrete copy for `ChartGraphics.Widen`, bridged via `new GraphicsPath(graphicsPath.PathPoints, graphicsPath.PathTypes)` rather than `AddPath` on the interface type, since GDI+'s own `GraphicsPath.AddPath` has no interface-typed argument overload). Fixed 6 call sites elsewhere (`AnnotationGroup.cs`, `TextAnnotation.cs`, `LineAnnotation.cs`, `ImageAnnotation.cs`, `CalloutAnnotation.cs`, and `ArrowAnnotation.cs` itself) that called `PaintSelectionHandles(..., null)` — the literal `null` became ambiguous between the two `PaintSelectionHandles` overloads once the interface-typed one existed; disambiguated with an explicit `(GraphicsPath)null` cast, no behavior change.
  - **Not converted, one path per file, for reasons independent of `HotRegionsList`:**
    - `FunnelChart.cs`'s `DrawFunnelCircularSegment` has one shared `graphicsPath` built via `graph.AddEllipseSegment(...)` (a concrete-`GraphicsPath`-only helper) and also consumed directly by `graph.Graphics.FillPath`/`DrawPath` (the real `System.Drawing.Graphics`, not `IChartRenderingEngine`) in its 3D-styled branch — stays concrete; its `DrawPathAbs`/`AddHotRegion` calls stay on the old overloads.
    - `CalloutAnnotation.cs` is blocked on something deeper than `HotRegionsList`: `Paint()` runs every returned callout path through `GraphicsPathIterator` (`new GraphicsPathIterator(graphicsPath)`, `NextMarker`) to split it into per-subpath hot regions — this is GDI+'s compound-path iteration API, with no `IGraphicsPath` equivalent (no `SubpathCount`/marker-walk member exists, or is planned, on the interface). Every one of its 7 `DrawPathAbs` call sites builds a path that either *is* the object walked by `GraphicsPathIterator`, or gets `AddPath`-ed into it (confirmed by reading all 5 `DrawXCallout` helper methods) — so none of them are independently convertible without first solving subpath iteration, a separate and likely substantial gap (would need either a hand-rolled subpath splitter over `IGraphicsPath`'s exposed `PathPoints`/`PathTypes`, or accepting Windows-only precise per-subpath hit-testing for callout shapes specifically). Left entirely untouched, pending its own decision.
  - Verified: build 0 errors (incremental and `--no-incremental`), all 5 visual regression tests pass, zero baseline changes.

  **Closed out the last piece of the original brush/circle/path cluster: `FillRectangleRel`/`FillRectangleAbs` themselves (2026-07-18).** Added `FillRectangleRelResource` (all 4 arities, mirroring the original's delegation chain) and `FillRectangleAbsResource` — both fully interface-typed, using their own local `IPen`/`IBrush` via `resourceFactory` rather than the shared `pen`/`solidBrush` fields (same reasoning as `DrawPathAbs`'s/`DrawCircleAbs`'s interface-typed overloads — this is what avoids ever needing to retype the shared fields, sidestepping the `DrawPathAbs`-field/6-caller ripple entirely). Internally they now call `GetHatchBrushResource`/`GetGradientBrushResource`/`GetTextureBrushResource`/`DrawCircleAbs(IPen,IBrush,...)` — the whole interface-typed brush/circle surface built up over this session. One accepted, documented fidelity nuance: the interface-typed `DrawImage` overload takes `int` source dimensions (GDI+'s int-based overload) vs. the originals' `float`-based one — negligible in practice since these values are already near-integral pixel dimensions when they reach this call. `FillRectangleAbsResource` also fixes a minor asymmetry with the original: since it no longer touches the long-lived shared `solidBrush` field, every brush/pen it creates is unconditionally disposed at the end (the original conditionally skipped disposal specifically to avoid disposing the shared field).
  - **What this completes:** the entire brush/circle/path/rectangle-fill surface identified as blocking B2 (`GetHatchBrush`, `GetGradientBrush`, `GetPieGradientBrush`, `GetSector3DBrush`, `GetTextureBrush`, `DrawCircleAbs`, `DrawPathAbs`, `CreateRoundedRectPath`, `FillRectangleRel`, `FillRectangleAbs`) now has a fully interface-typed sibling, all coexisting with the untouched originals per the dual-overload strategy. `HotRegionsList` and `Annotation.PaintSelectionHandles` gained interface-typed hit-testing support. Real per-caller migration (i.e., switching actual painter call sites from the `*Rel`/`*Abs` originals to the `*Resource` siblings) is a separate, much larger effort — `FillRectangleRel`/`FillRectangleAbs` alone are called from most chart types (bars, areas, pies, legends, chart-area backgrounds, etc.), squarely the "real per-file migration" scope the doc already flagged as future work, not attempted here.
  - **What remains explicitly unconverted, each for a distinct, real reason, not oversight:** `FunnelChart.cs`'s one `AddEllipseSegment`-built path (needs a concrete-`GraphicsPath`-only helper); `CalloutAnnotation.cs` in full (needs `GraphicsPathIterator`, no interface equivalent); the shared `pen`/`solidBrush` fields themselves (would still ripple into `DrawPathAbs`'s original concrete overload's remaining callers — `CalloutAnnotation.cs` and `FunnelChart.cs`'s one holdout — so left alone by design, not by omission).
  - Verified: build 0 errors (incremental and `--no-incremental`), all 5 visual regression tests pass, zero baseline changes.

  **Started the real per-caller migration: `FillRectangleRel`/`FillRectangleAbs` call sites (2026-07-19).** Grepped every `.FillRectangleRel(`/`.FillRectangleAbs(` call in the codebase and found the two `*Resource` siblings are a **pure drop-in rename** for every real Chart-engine caller: all four arities of `FillRectangleRel`/`FillRectangleAbs` take only value types (`Color`, `RectangleF`, chart enums, `string`, `int`, `bool`) — never `Pen`/`Brush`/`GraphicsPath` directly — so no caller-side type changes are needed at all, just the method name. Renamed `.FillRectangleRel(` → `.FillRectangleRelResource(` and `.FillRectangleAbs(` → `.FillRectangleAbsResource(` across all 17 real external callers: `AxisScrollBar.cs`, `CalloutAnnotation.cs`, `ChartArea.cs`, `ChartPicture.cs`, `ImageAnnotation.cs`, `Legend.cs`, `LegendCell.cs`, `RectangleAnnotation.cs`, `StripLine.cs`, `Title.cs`, `BarChart.cs`, `ColumnChart.cs`, `BoxPlotChart.cs`, `StackedColumnChart.cs`, `TreeMapChart.cs`, `StackedBarChart.cs`, `StockChart.cs`. Deliberately left `ChartGraphics.cs`'s own internal delegation between the original overloads (e.g. the 16-arg overload calling the 18-arg one) untouched — that's the original implementation calling itself, not a real caller, and touching it would be pure churn.
  - **Explicitly out of scope, found by the same grep:** `Microsoft.Reporting.Map.WebForms\*` has its own separate `MapGraphics.FillRectangleAbs`/`FillRectangleRel` — a different class entirely (the third GDI+-coupled engine noted in `TODO.md`, not yet scoped for this migration). Not touched.
  - Verified: build 0 errors (`--no-incremental`), all 5 visual regression tests pass, zero baseline changes.
  - **Investigated extending the same rename to `GetHatchBrush`/`GetGradientBrush`/`GetTextureBrush`/`DrawCircleAbs` call sites and confirmed the strategic finding from Milestone C already predicted this correctly: it does NOT generalize.** Unlike `FillRectangleRel`/`FillRectangleAbs`, these brush-getters return a concrete `Brush` that the caller immediately feeds into a concrete `GraphicsPath`-based `FillPath`/`DrawPath` call a few lines later (confirmed by reading `AreaChart.cs:87-114`, plus the same pattern in `RangeChart.cs`, `StackedAreaChart.cs`, `PieChart.cs`, `AxisScaleSegment.cs`) — swapping just the brush-getter to its `*Resource` sibling would produce an `IBrush` with no matching interface-typed `FillPath`/`DrawPath` overload to hand it to, since the path at that call site is still concrete. Converting these requires converting the **whole method's** path/pen/brush usage together (real B2 per-method work, not a caller-side rename) — left untouched, matching the doc's existing guidance that call-site conversion for C3/C4/C7 belongs to B2, not to independent per-type sweeps. `Axis.cs:1231`'s `DrawCircleAbs(pen, null, ...)` similarly can't move — its `pen` is the same one reverted earlier this session because it also feeds `ChartGraphics.Widen(graphicsPath, pen)`.
  - **Read all 5 candidate files in full to scope the next increment precisely — 3 of 5 turned out to hit the same GDI+-only walls already documented for `CalloutAnnotation.cs`/`FunnelChart.cs`, not just "needs interface-typed `FillPath`":**
    - `AreaChart.cs`'s `DrawLine` and `RangeChart.cs`/`StackedAreaChart.cs`'s equivalents all do `new Region(graphicsPath)` (feeding a concrete `GraphicsPath` into `Region`'s constructor — blocked on **C7's Region-from-path gap**, same as `SunkenBorder.cs`/`Axis.cs`'s two still-open `Region` sites) **and** call `ChartGraphics.Widen(graphicsPath, pen)` for hit-region hairline-widening (blocked on the documented **`GraphicsPath.Widen(Pen)`-has-no-cross-platform-equivalent risk**, §7). These three files are genuinely blocked on the same two open gaps as the existing holdouts, not independently convertible — confirmed by reading the actual code (`AreaChart.cs:121`, `:250`; `RangeChart.cs:207`, `:233`; `StackedAreaChart.cs` equivalents), not assumed.
    - `PieChart.cs`'s two `GetGradientBrush` call sites (`:1967`, `:2077`) only use `graph.Clip = new Region()` (a plain empty-region reset, not `new Region(path)`) — no `Widen` at all in the file. This one is **not** blocked by the same gaps; not yet attempted (its 3D-lighting brush-blend logic needs its own careful read before converting).
    - `AxisScaleSegment.cs`'s `GetChartFillBrush`/break-line-fill path (`:239-260`) has **no** `Region`-from-path or `Widen` calls anywhere in the file — its brush feeds a concrete `GraphicsPath` built via `AddPath`/`Reverse`/`CloseAllFigures`/`Dispose`, all already on `IGraphicsPath` per C7's complete inventory. This is the one genuinely convertible file among the five, but it has its own multi-step `GraphicsPath` field lifecycle (`breakLinePath`, `graphicsPath` disposal, `DrawPath` calls) that deserves a dedicated, careful pass rather than a quick edit — not started.
  - **Net finding:** the "bundles Brush+GraphicsPath" prediction from Milestone C actually undersold the entanglement — 3 of the next 5 candidates are blocked on the *same* GDI+-only primitives (`Region`-from-path, `Widen`) already flagged as open risks, not merely on B2 not having reached them yet. **This means the `Region`-from-path gap and the `Widen` gap are higher-leverage to resolve than initially scoped** — solving either one unblocks `CalloutAnnotation.cs`'s/`FunnelChart.cs`'s existing holdouts *and* 3 of these 5 files simultaneously. Next real increment, in priority order: (1) decide on `Widen`'s cross-platform strategy (hand-rolled stroke-outline vs. accept Windows-only hit-testing precision) since it single-handedly unblocks the most files; (2) `AxisScaleSegment.cs`'s break-line fill (no blockers, ready whenever picked up); (3) `PieChart.cs`'s brush-blend logic (no blockers found yet, unread in detail).

  **Converted `AxisScaleSegment.cs`'s break-line fill in full (2026-07-19) — the first genuinely non-mechanical per-method B2 conversion.** `PaintBreakLine`/`GetBreakLinePath`/`GetChartFillBrush` together build/fill/stroke a compound break-line path (wave, ragged, or straight styles) and are now fully interface-typed: `GetChartFillBrush` returns `IBrush` (via `Get*BrushResource`/`resourceFactory.CreateSolidBrush`); `GetBreakLinePath` (now takes a `ChartGraphics graph` parameter, previously didn't need one) returns `IGraphicsPath` (via `graph.ResourceFactory.CreatePath()`); `PaintBreakLine`'s `breakLinePath`/`graphicsPath`/`graphicsPath2` locals and `pen2` are now `IGraphicsPath`/`IPen`. **Real gap found and fixed:** `GetBreakLinePath`'s wave-style branch calls GDI+'s `GraphicsPath.AddCurve(points, offset, numberOfSegments, tension)` — the offset/count overload, distinct from the single `AddCurve(points, tension)` overload already on the interface — which was missing entirely. Added `IGraphicsPath.AddCurve(PointF[], int, int, float)`, implemented in `GdiGraphicsPath` (`NativePath.AddCurve(points, offset, numberOfSegments, tension)`), stubbed in `SkiaGraphicsPath`. One bridging point kept concrete by design: the shadow-trail sub-block builds its own local `graphicsPath3` (`new GraphicsPath()`) purely to `Transform(Matrix)`/`DrawPath(Pen, ...)` repeatedly in a loop with a concrete `System.Drawing.Drawing2D.Matrix` — left untouched since it's fully self-contained (never returned or stored), with its one dependency on the now-interface-typed `breakLinePath` bridged via `new GraphicsPath(breakLinePath.PathPoints, breakLinePath.PathTypes)`, the same pattern used earlier this session for `PolylineAnnotation.cs`.
  - Verified: build 0 errors (`--no-incremental`), all 5 visual regression tests pass, zero baseline changes. **Caveat, stated plainly:** none of the 5 committed baselines configure `Axis.ScaleBreakStyle`, so this pass doesn't exercise `PaintBreakLine` pixel-for-pixel — it only confirms the rest of the engine is unaffected and the new code compiles/type-checks correctly. A scale-break-specific visual regression test would be needed for full confidence; not added here (out of scope for this pass, flagged as a real gap in verification, not hidden).

  **Added interface-typed `FillPieCurve`/`DrawGraphicsPath` overloads to `ChartGraphics3D.cs` (2026-07-19) — port-only, `PieChart.cs`'s real callers not yet migrated.** `ChartGraphics3D.FillPieCurve(ChartArea, DataPoint, Brush, Pen, ...)` (14 args, 30 call sites all in `PieChart.cs`'s 3D pie/doughnut curve-drawing methods) has a local `graphicsPath` that only ever calls `AddLine`/`AddArc` — both already on `IGraphicsPath` — and both `HotRegionsList.AddHotRegion` overloads it calls already have interface-typed siblings from earlier this session. Added a true C# overload `FillPieCurve(ChartArea, DataPoint, IBrush, IPen, ...)` (differs by parameter type, so no name clash) that mirrors the original body exactly, using `((ChartGraphics)this).ResourceFactory.CreatePath()` for the local path. Also added a matching `DrawGraphicsPath(IPen, IGraphicsPath)` overload (the private helper `FillPieCurve` calls for border-drawing when `pen.Width >= 2f`, which manually flattens+walks the path drawing line segments — every member it touches, `Flatten`/`PathPoints`/`DrawLine`/`DrawPath`, is already interface-typed). **Initially miswrote this as an in-place signature change** (retyping the original method instead of adding a sibling) — caught before verifying, because it would have forced `PieChart.cs`'s 30 call sites (spanning 4 methods: `DrawPieCurves`, `DrawPieCurvesBigSlice`, `DrawDoughnutCurves`, `DrawDoughnutCurvesBigSlice`, each with their own `SolidBrush`/`Brush`/`Pen` parameters and further upstream callers) to convert in the same pass — reverted to the dual-overload pattern instead, consistent with how every other cluster in this doc has been handled.
  - **Deliberately not converted in this pass:** `PieChart.cs` itself — its 3D pie/doughnut rendering is a large, self-contained cluster (4 methods, `brush`/`pen` threaded from `DrawPieCurves`'s own `SolidBrush brushWithoutLight`/`Pen pen` parameters, which themselves come from further up the 3D pie-drawing call chain) that deserves its own dedicated read-and-convert pass, not a mechanical one. The new `FillPieCurve`/`DrawGraphicsPath` overloads exist and are ready whenever that pass happens.
  - Verified: build 0 errors (`--no-incremental`), all 5 visual regression tests pass, zero baseline changes (the new overloads are additive and unreachable until `PieChart.cs` is converted, so this only proves they compile and don't disturb the existing concrete path).

  **Converted `PieChart.cs`'s full 3D pie/doughnut rendering cluster (2026-07-19) — the dedicated pass flagged above, now done, with a real byte-for-byte pixel verification (not just "doesn't crash").** Traced the whole call chain from its single entry point `Draw3DPie` (private, called from exactly one site) down through `DrawPieCurves`/`DrawPieCurvesBigSlice`/`DrawDoughnutCurves`/`DrawDoughnutCurvesBigSlice`/`Draw3DOutsideLabels` (all private, each called only from within this chain) to `ChartGraphics3D`'s `FillPieSlice`/`FillDoughnutSlice`/`FillPieSides`/`FillPieCurve`. Since `Draw3DPie` has exactly one external caller (unlike `ChartGraphics3D`'s `internal` methods, which get true additive overloads out of caution), the whole PieChart.cs-side chain was converted **in place** — `solidBrush`/`pen`/`pen2`/`pen3` (originally `SolidBrush`/`Pen` locals in `Draw3DPie`) are now `ISolidBrush`/`IPen` via `graph.ResourceFactory.CreateSolidBrush`/`CreatePen`, and every method signature in the chain was retyped to match. Added matching interface-typed `FillPieSlice`/`FillDoughnutSlice`/`FillPieSides` overloads to `ChartGraphics3D.cs` (true additive overloads, since these 3 are `internal` and a codebase-wide grep found only `PieChart.cs` calling them today, but kept the safer dual pattern rather than assume that grep is exhaustive).
  - **Real gap found and fixed:** `IPen` had no `Clone()` — GDI+'s `Pen.Clone()` is used in `FillPieSlice`/`FillDoughnutSlice` to derive a border pen from the fill pen without mutating the shared original. Added `IPen Clone()` to the interface, implemented in both `GdiPen` (constructs a new `GdiPen` and copies all 5 mutable properties) and `SkiaPen` (same pattern).
  - **Real verification added, not just reused:** none of the 5 existing baselines render a `Pie`/`Doughnut` series or enable `Area3DStyle.Enable3D`, so (unlike everything else in this session) the existing suite could not have caught a regression here. Added `SampleCharts.RenderPie3DChart()`/`RenderDoughnut3DChart()` and two new tests (`Pie3DChart_MatchesBaseline`/`Doughnut3DChart_MatchesBaseline`). Generated the baselines the same way `RotatedLabelsChart.png` was: `git stash push` on just the 5 engine files (keeping the new test files, via `git stash push -- <paths>`) to render both charts through the **original, concrete, pre-conversion code**, visually inspected both PNGs (a normal 3D pie and a 3D doughnut, both with legends, both structurally correct), promoted them to `Baselines/`, then `git stash pop` to restore the conversion and re-ran the full suite.
  - Verified: build 0 errors (`--no-incremental`); **all 7 visual regression tests pass, including byte-for-byte pixel match on the two new 3D pie/doughnut baselines against the pre-conversion render** — the strongest verification of any conversion this session, since it's an actual pixel-identical proof rather than "the rest of the suite is unaffected."

  **Resolved the `Widen` half of the `Widen`/`Region`-from-path blocker (2026-07-19) — the `Region`-from-path half turned out to be a separate, bigger decision, not resolved today.** The two gaps were previously tracked together because they co-occur in the same few files, but they're structurally unrelated (stroke-to-fill geometry vs. clip-region construction) and needed to be untangled rather than solved as one unit.
  - **`Widen` — real fix, not a workaround:** `IGraphicsPath.Widen(IPen)` already existed on the interface and `GdiGraphicsPath` already implemented it correctly; the actual gap was `SkiaGraphicsPath.Widen` throwing `NotImplementedException`. Implemented it for real using `SKPaint.GetFillPath` — Skia's own stroke-to-fill primitive (used internally by Skia to rasterize strokes), not a hand-rolled geometry algorithm. Added `SkiaConvert.ToSKStrokeCap`/`ToSKStrokeJoin` to translate `IPen`'s `LineCap`/`LineJoin` into Skia's stroke settings. Added `ChartGraphics.Widen(IGraphicsPath, IPen)`, mirroring the original static helper's swallow-known-failures semantics (GDI+'s `Widen` throws `ArgumentException` on degenerate/empty paths, treated as "leave unwidened," not a hard failure).
  - **Two more missing `HotRegionsList.AddHotRegion` interface-typed overloads found and added** (the same `PathPoints`/`PathTypes` bridge pattern as the others): the `(ChartGraphics, path, bool, float[] coord, DataPoint, string, int)` arity (used by most of the converted files below) and the `(path, bool, ChartGraphics, ChartElementType, object)` arity (used by `Axis.cs`).
  - **Converted every real caller of `ChartGraphics.Widen` found by a codebase-wide grep (13 call sites, not the 6 originally estimated) — 9 of 13 were purely Widen-blocked and are now done:** `LineChart.cs`, `StepLineChart.cs` (2 sites), `KagiChart.cs`, `RadarChart.cs` (its `AddSelectionPath` needed a new `ChartGraphics graph` parameter threaded through its 3 call sites — small, contained, all in the same file, all already had `graph` in scope — to reach `resourceFactory`), `LineAnnotation.cs`, `PolylineAnnotation.cs` (this also let a `new GraphicsPath(graphicsPath.PathPoints, ...)` bridge added earlier this session collapse into a direct `graphicsPath2.AddPath(graphicsPath, ...)`, since both sides are now `IGraphicsPath`), `Axis.cs`'s `DrawCircularLine`, `AreaChart.cs` and `StackedAreaChart.cs`'s **hit-region-only** `Widen` blocks (each file's *other*, Region-entangled `DrawLine` logic was left concrete — the two blocks use independent local `GraphicsPath` variables, confirmed by reading the full method before touching anything, not assumed). `LineAnnotation.cs`/`PolylineAnnotation.cs` needed one more trick: their pen is cloned from the shared `graphics.pen` field (deliberately left concrete all session), so rather than touch that field, each constructs a fresh local `IPen` via `resourceFactory.CreatePen(graphics.pen.Color, graphics.pen.Width + 2f)` — same "local resource, not the shared field" pattern used for `DrawPathAbs`/`DrawCircleAbs` earlier.
  - **4 of 13 sites remain genuinely blocked, each on something deeper than `Widen` itself, confirmed by reading the full enclosing method, not assumed:**
    - `CalloutAnnotation.cs` (2 sites) — its local `graphicsPath2`/`graphicsPath3` get `AddPath`-ed into the method's main `graphicsPath`, which is the exact object `Paint()` later runs through `GraphicsPathIterator` — the same structural blocker already documented for this file, unrelated to `Widen`. Converting just these two sub-blocks in isolation would leave the file in a confusing half-converted state without actually unblocking it, so left alone.
    - `ChartGraphics3D.cs`'s `Draw3DSurface`-style method and `RangeChart.cs`'s equivalent — both mix the `Widen` call with real concrete rendering (`FillPath`/`DrawPath` calls using the *same* `graphicsPath`/`pen` that's also widened, plus — in `ChartGraphics3D.cs`'s case — the persistent `frontLinePen` field spanning multiple calls). This is the exact entanglement already found and reverted earlier this session (see C3 notes) — confirmed still true, not re-attempted.
  - **`Region`-from-path is NOT resolved and turned out bigger than scoped:** `AreaChart.cs`/`RangeChart.cs`/`StackedAreaChart.cs`'s shadow-drawing blocks don't just need `new Region(path)` (which already has a working bridge — `IDrawingResourceFactory.CreateRegion(IGraphicsPath)` — from Milestone C1) — they read and reassign `graph.Clip`, a `public Region Clip { get; set; }` property on `ChartRenderingEngine` (the common base of `ChartGraphics3D`/`ChartGraphics`), backed directly by the live `IChartRenderingEngine`'s GDI+-only `RenderingObject`. Retyping that property would ripple across the whole engine's public surface, not just these 3 files — it's Milestone-D-adjacent work (the surface/rendering-context boundary), not a per-file fix. `SunkenBorder.cs`/`EmbossBorder.cs`/`Axis.cs`'s two simpler `new Region(path)`-only sites (no `Clip` involved) are separately still open too, blocked only on whatever produces their source path still being concrete — smaller, real future work, not attempted in this pass.
  - Verified: build 0 errors (`--no-incremental`), all 7 visual regression tests pass. **Important caveat, stated plainly:** hit-region/`Widen` changes affect invisible hit-testing metadata (tooltips, click-through maps), not rendered pixels — so passing visual regression tests here proves "compiles, runs, doesn't throw, and rendering is unaffected," not "the widened hit-region shape is pixel-correct." No test harness exists for hit-region correctness (noted as a real gap earlier this session too); the strongest guarantee available is that `GdiGraphicsPath.Widen(IPen)` still calls the exact same `GraphicsPath.Widen(Pen)` GDI+ API with equivalent values, so Windows behavior is unchanged by construction, not just by testing.

  **Resolved the `Region`-from-path/`Clip` half too (2026-07-19, same day) — real per-caller migration, not just the port.** The interface-typed alternative to the concrete `ChartRenderingEngine.Clip`/`IsClipEmpty` property already existed (`IChartRenderingEngine.GetClipRegion()`/`SetClipRegion(IClipRegion)`, from Milestone C1) — so, exactly like `Pen`/`Brush`, the property itself never needed retyping; only real callers needed migrating from `.Clip`/`new Region(...)` to `.GetClipRegion()`/`.SetClipRegion()`/`resourceFactory.CreateRegion(...)`.
  - **Extended `IClipRegion` with 4 real, previously-missing members**, found by reading every real caller rather than assumed: `Translate(float, float)`, `Clone()` (returns `IClipRegion`), `IsInfinite(IChartRenderingEngine)`, and `Complement(IGraphicsPath)` (GDI+'s `Region.Complement(GraphicsPath)` — "this = argument − this", used by the Emboss/Sunken border shadow-clipping trick). All four implemented in `GdiClipRegion` (thin passthroughs to `System.Drawing.Region`) and stubbed in `SkiaClipRegion`.
  - **Converted the simple "save/reset-to-infinite/restore" pattern** (`Region clip = graph.Clip; graph.Clip = new Region(); ...; graph.Clip = clip;`) across 8 call sites in 5 files: `StackedBarChart.cs`, `StackedColumnChart.cs` (×2), `RangeColumnChart.cs` (×2), `PieChart.cs`, `StackedAreaChart.cs`. `PieChart.cs`'s site has a pre-existing early-return-before-restore quirk (leaves the clip in its reset state if `labelText` is empty) — preserved exactly as-is; not a bug introduced or fixed here, since the goal is a type-preserving transformation, not a behavior change.
  - **Converted 3 more structurally-elaborate sites**, each read in full before touching:
    - `AreaChart.cs`'s shadow-drawing block — `new Region(graphicsPath)` (bridged via `graphicsPath.PathPoints`/`PathTypes` since the main draw path stays concrete, shared with the real `FillPath(brush, graphicsPath)` call elsewhere in the method), plus `Clip`/`Clone`/`Translate`/`IsInfinite`. Also found `new Pen(brush2, 1f)` — a brush-based `Pen` constructor `IPen` doesn't support — but since `brush2` here is always a plain solid color (never a gradient), replaced with `resourceFactory.CreatePen(shadowFillColor, 1f)` using the same source `Color` both the brush and pen were built from — verified equivalent, not a behavior change.
    - `Axis.cs`'s `DrawRadialLine` — the `Clip`-from-polygon-path save/restore, plus its own independent hit-region `Widen` block (missed in the earlier `Widen` pass since this method wasn't in the original 13-site grep's immediate context — found while reading the file for this pass).
    - `Borders3D/EmbossBorder.cs` and `Borders3D/SunkenBorder.cs` — the `Complement`-based (and, in `SunkenBorder.cs`, also `Intersect`-based) clip-shadow tricks that motivated adding `Complement` to the interface. These were the two sites flagged as blocked back in Milestone C1 ("needs C7") — C7 has been complete for a while; the real blocker turned out to be `IClipRegion` missing `Complement`, not `IGraphicsPath` itself.
  - **`RangeChart.cs`'s equivalent shadow block investigated and left concrete, for a reason beyond just `Region`:** unlike `AreaChart.cs`'s version, its `graphicsPath`/`brush` are the *same* objects used by the main `FillPath(brush, graphicsPath)` call later in the method (not an independent local), and it *also* reads/writes `graph.Transform` (a second concrete GDI+ property, distinct from `Clip`, though it has the same kind of already-existing interface-typed alternative — `GetTransform()`/`SetTransform(Matrix3x2)` — from Milestone C2). Converting it properly means converting the whole method's `Brush`/`Pen`/`GraphicsPath`/`Transform` surface together, not just its `Region` calls — real, larger future work, consistent with `RangeChart.cs` already being flagged as one of the bigger pending per-file B2 conversions.
  - **Verification — added 3 new smoke-test sample charts, generated from pre-conversion code the same `git stash` way as the pie/doughnut baselines, since none of the 7 existing baselines touch borders or area shadows:** `RenderEmbossBorderChart`/`RenderSunkenBorderChart` (`BorderSkin.SkinStyle`) and `RenderAreaChartWithShadow` (an `Area` series with `ShadowOffset` set). All three rendered without exceptions under the pre-conversion code, were visually inspected (structurally correct — bars, borders, and a filled area, nothing broken), promoted to baselines, and the post-conversion render matches all three byte-for-byte.
  - Verified: build 0 errors (`--no-incremental`); **all 10 visual regression tests pass**, including byte-for-byte pixel match on the 3 new baselines — real proof this conversion preserves behavior, not just "compiles and doesn't crash."
  - **What's left, precisely:** `RangeChart.cs`'s shadow block (needs a full `Brush`/`Pen`/`GraphicsPath`/`Transform` method conversion, not just `Region`); `ChartGraphics3D.cs`'s `Draw3DSurface`-style method (already-documented `frontLinePen`-field entanglement, unrelated to `Clip`); `ChartImage.cs`'s one `Clip` site (the already-documented Windows-only EMF/metafile export path, out of scope by design); and `Gauge.WebForms`/`Map.WebForms`'s many `Clip` sites (separate engines, not in this migration's scope). The `Clip`/`IsClipEmpty` property itself was never touched — it still coexists with `GetClipRegion()`/`SetClipRegion()`, exactly like `Pen`/`Brush`/`GraphicsPath` throughout this whole effort.

  - **`RangeChart.cs`'s `DrawLine` converted in full (2026-07-19).** Confirmed via a fresh read that the method genuinely needed a whole-method conversion (`IBrush`, `IPen`, `IGraphicsPath`, `IClipRegion`, transform), as previously flagged — not a quick fix. Surveyed the interface surface first and found almost everything already existed: `GetHatchBrushResource`/`GetTextureBrushResource` (siblings of `GetHatchBrush`/`GetTextureBrush`), `resourceFactory.CreateSolidBrush`/`CreatePath`/`CreateRegion(IGraphicsPath)`, `graph.GetClipRegion()`/`SetClipRegion()`/`.Translate()`/`.Clone()`/`.IsInfinite(graph)` (extended for the earlier `Clip` migration), `graph.FillRegion(IBrush, IClipRegion)`, `graph.DrawLine`/`DrawCurve`(`IPen`, ...)`, `graph.FillPath(IBrush, IGraphicsPath)`, and `graph.Save()`/`TranslateTransform(dx,dy)`/`Restore(GraphicsState)` (the pattern `AreaChart.cs` already uses for its shadow-offset translate, reused here instead of `Matrix.Clone()/Translate()`). **One real gap found and filled:** GDI+'s `new Pen(Brush, float)` ("stroke with an arbitrary brush") had no interface equivalent — added `IDrawingResourceFactory.CreatePen(IBrush, float)`, implemented for `GdiResourceFactory` (unwraps to the concrete `Brush` via a `NativeBrush(IBrush)` switch, mirroring `GdiGraphics.Native(IBrush)`) and added a matching `GdiPen(Brush, float)` constructor; stubbed as `NotImplementedException` in the still-spike-scoped `SkiaResourceFactory`, consistent with its other unimplemented brush kinds. Converted `RangeChart.cs`'s shadow block (`Region`-from-path, `Save`/`TranslateTransform`/`Restore` instead of `Matrix.Clone()`/`graph.Transform =`, `FillRegion`, the shadow hairline pen), the main fill block (`FillPath`, the brush-backed hairline pen incl. the hatch-brush color-override via `IHatchBrush.ForegroundColor`), the `brush is SolidBrush` → `brush is ISolidBrush` check, and the hot-region `graphicsPath3` (already had a matching `AddHotRegion(ChartGraphics, IGraphicsPath, bool, float[], DataPoint, string, int)` overload). `areaPath`/`areaBottomPath` (class-level fields used by `FillLastSeriesGradient`, a separate method) deliberately left concrete — untouched, no interop conflict since only plain `PointF`s cross between them and the new interface-typed locals. Added 3 new sample charts + regression tests (`RangeChartWithShadow`, `RangeChartWithHatch`, `RangeChartWithGradient` — no prior `RangeChart` coverage existed at all), each verified byte-for-byte against pre-conversion GDI+ output via the `git stash` technique (stashing only `RangeChart.cs`, since the new `CreatePen(IBrush, float)` infrastructure is purely additive and doesn't affect any other already-converted call site). Build 0 errors, all 25 tests across the solution (24 `VisualRegressionTests` + 1 `Chart.Rdl.Tests`) pass. **This resolves `RangeChart.cs`'s brush-stroke gap — one of the three remaining flagged architectural gaps.**

  - **`CalloutAnnotation.cs`'s `GraphicsPathIterator` gap resolved (2026-07-19), the second of the three remaining flagged architectural gaps — bigger than `RangeChart.cs`, not smaller.** A fresh read confirmed the real scope: the iterator only matters for hot-region splitting, but the paths it walks are built by 5 separate shape-drawing methods (`DrawRoundedRectCallout`, `DrawRectangleCallout`, `DrawCloudCallout`, `DrawPerspectiveCallout`, `DrawRectangleLineCallout`, ~600 lines total), 3 of which call `GraphicsPath.SetMarkers()` to merge sub-shapes (a perspective wedge, a widened anchor-line stroke) into the main outline for later splitting. Converted all 5 to `IGraphicsPath`/`IBrush`/`IPen` — almost every needed piece already existed (`DrawPathAbs(IGraphicsPath, ...)` overloads from the earlier B2 rename pass, `AddHotRegion(ChartGraphics, IGraphicsPath, bool, string, string, string, object, ChartElementType)`, `ChartGraphics.Widen(IGraphicsPath, IPen)` — resolving 2 of the 4 previously-blocked `Widen` callers noted in the §7 risk table — and `IGraphicsPath.SetMarkers()`/`AddPath`/`PointCount`/`AddLines` etc.). `GetCloudPath`/`GetCloudOutlinePath` (static, cached, `Matrix.Translate`/`Scale`-based geometry helpers with no Pen/Brush coupling) were deliberately left concrete internally — converting their `Matrix`-chaining to `Matrix3x2` correctly would require re-deriving GDI+'s prepend-order semantics with no independent way to verify the math, so instead their already-correct concrete output is bridged to `IGraphicsPath` right at the call site via `CreatePath(PathPoints, PathTypes)`, the same established bridging pattern used throughout this migration for exactly this kind of judgment call.

    **The real new work: replacing `GraphicsPathIterator.NextMarker` itself**, since it's a GDI+-only type with no interface-side equivalent anywhere in the port. Added a private `SplitAtMarkers(ChartGraphics, IGraphicsPath)` helper that reads `IGraphicsPath.PathTypes` directly (already exposes the same `PathPointType` flag bytes GDI+ uses internally, including the `PathMarker` 0x20 bit `SetMarkers()` sets) and slices the point/type arrays at each marker boundary — reproducing `NextMarker`'s behavior (marker-to-marker segments, with a final unmarked remainder segment covering the rest of the path) without depending on the GDI+-only iterator type at all.

    **Verification split into two kinds, matching what each part of the change actually affects:** the 5 converted shape-drawing methods produce real pixels, so got 7 new PNG regression tests (one per `CalloutStyle`), each verified byte-for-byte against pre-conversion GDI+ output via `git stash`. But `SplitAtMarkers`/`AddHotRegion` produce no visible pixels at all — a PNG diff can't validate hot-region correctness — so also added 4 new tests in a separate `CalloutAnnotationHitTestTests.cs` that call the public `Chart.HitTest(x, y)` API (which exercises the real `ProcessModeRegions`/`AddHotRegion` path) against a `Perspective` callout positioned so both the top and left wedges get merged into the main path via `SetMarkers`/`AddPath` — confirming hit-tests land correctly inside the main rectangle, inside each of the two split-out wedge triangles (independently hand-computed centroids), and correctly miss a point far from all three shapes. Build 0 errors, all 36 tests across the solution pass (31 `VisualRegressionTests` — up from 24, +7 Callout PNG tests — + 4 new hit-test tests + 1 `Chart.Rdl.Tests`).

  - **`ChartGraphics3D.cs`'s `frontLinePen` entanglement resolved (2026-07-19), the third and last of the three flagged architectural gaps.** A fresh read of `Draw3DPolygon` (lines 688-781) and `Draw3DSurface` (lines 943-1282, ~340 lines) confirmed the two methods are large and share the persistent `internal Pen frontLinePen;` field across calls (surfaces are drawn one at a time, so a segment's front-facing edge is deferred and drawn on the *next* segment's call, or left for a final flush) — `Draw3DSurface` additionally entangles `ChartGraphics.Widen(graphicsPath, pen3)` with a `graphicsPath` that's incrementally built across many conditional branches and returned to ~7 external `ChartTypes/*.cs` callers, so retyping either method's return value or most of its internals would have meant either an even-larger conversion or the same kind of `Widen`+concrete-path risk already flagged for `RangeChart.cs`/`CalloutAnnotation.cs`.

    **Reframed the fix to match what's actually needed:** `frontLinePen`'s only real consumer is `((ChartGraphics)this).DrawLine(frontLinePen, ...)` (which already has a matching `IPen`-typed overload) — every other `Pen`/`Brush`/`GraphicsPath` in both methods stays concrete and untouched, exactly like `RangeChart.cs`'s own `Draw3DSurface` override was deliberately left concrete earlier in this migration. Retyped just the field (`Pen` → `IPen`) and added a small `BridgeFrontLinePen(Pen concretePen)` helper that reads the concrete pen's *current* property values (`Color`, `Width`, `StartCap`, `EndCap`, `DashStyle`, `LineJoin`, `Alignment`) at the exact moment of assignment — important because `Draw3DSurface` mutates `pen3.EndCap` *after* constructing it but *before* the `frontLinePen` assignment, so re-deriving from scratch would have silently dropped that mutation. This is the same bridge-at-the-sink pattern used for Font/StringFormat (C5/C6) and the concrete `GetCloudPath`/`GetCloudOutlinePath` output (`CalloutAnnotation.cs`, above) — read properties off the concrete object, construct the interface-typed resource from those values, right where it's needed. Confirmed the field's 3 external write sites (`LineChart.cs`, `KagiChart.cs`, `StepLineChart.cs`) only ever assign `null` (a reset between series/segments), which compiles identically regardless of the field's static type — zero changes needed there.

    Added `RenderLineChart3D` (a 3D `SeriesChartType.Line` series with the default `Area3DStyle.Perspective == 0` and 4 points, so the carry-over path executes more than once), verified byte-for-byte against pre-conversion GDI+ output via `git stash`. Build 0 errors, all 37 tests across the solution pass. **This completes all three flagged architectural gaps (`RangeChart.cs`, `CalloutAnnotation.cs`, `ChartGraphics3D.cs`) found during the earlier B2 survey.**

  **`StockChart.cs` converted too (2026-07-19), while surveying remaining `ChartTypes/*.cs` files for more real-caller candidates.** Its Triangle-open-close-mark drawing (2D `DrawOpenCloseMark` and its 3D `Draw3DOpenCloseMark` counterpart) builds a fully self-contained local `GraphicsPath` + `new SolidBrush(point.Color)`, fed straight into `FillPath` — no `Region`/`Widen`/shared-field entanglement, so it converted cleanly via `resourceFactory.CreatePath()`/`CreateSolidBrush()` + the already-existing `FillPath(IBrush, IGraphicsPath)` overload. Not previously exercised by any regression baseline (no sample chart used `SeriesChartType.Stock`) — added `RenderStockChartWithTriangleMarks` (sets `series["OpenCloseStyle"] = "Triangle"` to select this code path over the default Candlestick rendering) and generated its baseline from the pre-conversion code via the same `git stash` technique; verified byte-for-byte. Build 0 errors, all 11 visual regression tests pass.

  **`FastPointChart.cs` converted too (2026-07-19), same survey pass.** `DrawMarker` (its whole `Brush brush, Pen borderPen` parameter pair, plus the caller's `SolidBrush`/`Pen` locals in `ProcessChartType`) converted in place — `protected virtual` but confirmed via grep to have zero subclasses anywhere in the codebase, so no dual-overload was needed. `FillPolygon`/`DrawPolygon`/`FillEllipse`/`DrawEllipse`/`FillRectangle`/`DrawRectangle` all already had matching `IBrush`/`IPen` overloads from Milestone A3, so the method body needed no further changes beyond the signature and the two locals. A scratch-local `Matrix` used purely for a 45°-rotation of `Cross`-marker points stays concrete — it's never passed to any `IChartRenderingEngine` method, just used to precompute `PointF[]` coordinates, so it's outside this conversion's scope entirely (not a Milestone-C blocker, just an unrelated local calculation). Not previously exercised by any baseline (`SeriesChartType.FastPoint` unused) — added `RenderFastPointChartWithMarkers` (two series: bordered `Diamond` and bordered `Cross`, to hit two different branches of `DrawMarker`'s marker-style switch) and generated its baseline via the same `git stash` technique; verified byte-for-byte. Build 0 errors, all 12 visual regression tests pass.

  **`FastLineChart.cs` converted too (2026-07-19), same survey pass.** `DrawLine`'s `Pen pen` parameter (`public virtual`, confirmed zero subclasses via grep) converted to `IPen`, along with the caller's `pen`/`pen2` locals in `ProcessChartType` and the method's own hit-region `GraphicsPath` local (used only for `GetBounds()`/`AddHotRegion` — never fed to `FillPath`/`DrawPath`, so no entanglement). `HotRegionsList.AddHotRegion(IGraphicsPath, bool, ChartGraphics, DataPoint, string, int)` already existed as a matching overload, so the hit-region call needed no changes beyond the local's type. Not previously exercised by any baseline (`SeriesChartType.FastLine` unused) — added `RenderFastLineChart` and generated its baseline via the same `git stash` technique; verified byte-for-byte. Build 0 errors, all 13 visual regression tests pass.

  **`LineChart.cs`'s shadow-line block converted too (2026-07-19), same survey pass.** `DrawLine`'s local `Pen pen` (built only when `series.ShadowOffset != 0`, independent of the shared `linePen` field left concrete per B1b) converted to `IPen`. Its only extra wrinkle: both the new local `pen` and the shared concrete `linePen` field are passed to the same `private DrawTruncatedLine(ChartGraphics, Pen, PointF, PointF)` overflow-fallback helper — added a true second overload `DrawTruncatedLine(ChartGraphics, IPen, PointF, PointF)` (private, same file, both callers now resolve to their matching overload) rather than touch `linePen`. The block's `graph.Transform`/`graph.Save()`+`graph.Restore(GraphicsState)` calls stay concrete and untouched — `GraphicsState` (an opaque, whole-graphics-state snapshot token) has no interface-typed equivalent anywhere in the port, a distinct, unrelated gap from `Pen`, not attempted here. Not previously exercised by any baseline (no `Line`-series sample used `ShadowOffset`) — added `RenderLineChartWithShadow` and generated its baseline via the same `git stash` technique; verified byte-for-byte. Build 0 errors, all 14 visual regression tests pass.

  **`Grid.cs`/`TickMark.cs` converted (2026-07-19), continuing the B2 real-caller sweep beyond `ChartTypes/*.cs`.** Both files' only remaining `GraphicsPath` usage was hit-region-only (`ProcessModeRegions`-gated, never drawn) — `Grid.cs`'s `DrawGrid`/`PaintCustom` build a small closed-figure path around a gridline purely to feed `HotRegionsList.AddHotRegion(IGraphicsPath, bool, ChartGraphics, ChartElementType, object)` (an overload already added earlier this session); `TickMark.cs`'s circular-chart-area branch does the same but also calls `.Transform(graph.Transform)` first, which bridged cleanly to the already-existing `IGraphicsPath.Transform(Matrix3x2)` + `graph.GetTransform()` pair from Milestone C2. No new interface gaps found — both conversions were pure drop-in `new GraphicsPath()` → `graph.ResourceFactory.CreatePath()` swaps (added `using Microsoft.Reporting.Chart.WebForms.Rendering;` to each file). These paths never render pixels, so there's no new baseline to add — verified via the existing 36-test suite (unaffected, as expected) plus a build check that `ProcessModeRegions` code compiles and type-checks correctly. Build 0 errors, all 36 tests pass.

  **Survey conclusion: the remaining `ChartTypes/*.cs` files with concrete GDI+ construction (`TreeMapChart.cs`, `SunburstChart.cs`, `BarChart.cs`, `PointChart.cs`, `ErrorBarChart.cs`, `BoxPlotChart.cs`) all reduce to the exact same pattern — `graph.DrawPointLabelStringRel(..., new SolidBrush(point.FontColor), ...)`, where `DrawPointLabelStringRel`/`DrawStringRel` take concrete `Font`/`Brush`/`StringFormat` with no interface-typed overload anywhere in `ChartGraphics.cs`.** This isn't a per-file gap — it's the C5/C6 (`ITextFormat`/`IChartFont`) real-caller migration, already flagged in Appendix A.2/A.3 as the other XL-sized item alongside `IGraphicsPath` (31-file spread). Converting even one call site requires adding real `DrawStringRel`/`DrawPointLabelStringRel` overloads accepting `IChartFont`/`ITextFormat`/`IBrush` and verifying text-measurement/layout parity — a milestone-sized piece of new work, not a quick continuation of this file-by-file sweep. The self-contained-block vein this pass has been mining (`StockChart.cs`, `FastPointChart.cs`, `FastLineChart.cs`, `LineChart.cs`) is now exhausted; every other real remaining Pen/Brush/GraphicsPath call site is either already documented as blocked (`RangeChart.cs`'s brush-stroke gap, `ChartGraphics3D.cs`'s `frontLinePen`, `CalloutAnnotation.cs`'s `GraphicsPathIterator`) or is this Font/TextFormat cluster.

### Milestone C — Migrate resource types (per type, parallelizable)

Ordered smallest-blast-radius first so patterns settle before the big ones:

> **Strategic finding after C1-C3 (revisit before sinking more time into "C4 alone"):** per-type migration cleanly works for `Region`/`Matrix` (mostly self-contained) but degrades fast for the rest, because real painter code almost never uses one resource type in isolation — `DrawStringRel(text, Font, Brush, ..., StringFormat, ...)` bundles C4+C5+C6 in a single call; `DrawPath`/`FillPath` bundle C3/C4 with C7. Treating C4-C8 as independently-closeable checkboxes undersells this — most remaining value requires converting a **whole method's** resource surface at once (Font+Brush+StringFormat+Path together), which is really **B2's job**, not six separate per-type passes. Revised plan: finish the interface/adapter groundwork for each type first (cheap — mostly already drafted in A1/A2), then do the real conversion **per ChartGraphics method** (B2) and **per painter file** rather than per-type. C4-C8's checkboxes below now track "the port + adapter exists and is complete," not "all call sites converted" — call-site conversion is B2/B1b's job once every type it needs is ready.

- [x] **C1. `Region` → `IClipRegion`** (17 occ / 6 files) 🟢 S
  **Done for the independent usage; two sites intentionally left, matching B1b's precedent.** Added `Xor`, `Transform(Matrix3x2)`, `IsEmpty`/`GetBounds` to `IClipRegion` (the latter two retargeted from a nonexistent-in-context `IRenderSurface` parameter to `IChartRenderingEngine`, since `ChartGraphics` only ever holds the live drawing context, never an owned `IRenderSurface` — that type models Milestone D's encodable output surface, a different thing). Implemented in `GdiClipRegion`/stubbed in `SkiaClipRegion`. Migrated [ChartGraphics.cs:1793-1892](../Microsoft.ReportViewer.DataVisualization/Microsoft.Reporting.Chart.WebForms/ChartGraphics.cs#L1793-L1892) (`FillRectangleShadowAbs`'s save/restore-clip block) to `IClipRegion` via `GetClipRegion()`/`SetClipRegion()`/`resourceFactory.CreateRegion()`. **Update after C2 landed:** the axis-label rotation clip (originally `ChartGraphics.cs:985-1013`) needed both C1's `IClipRegion.Transform(Matrix3x2)` and C2's `myMatrix` retyping — both now landed, so it migrated too (`resourceFactory.CreateRegion(rect)`, `region.Transform(myMatrix)`, `region.IsEmpty(this)`/`GetBounds(this)`). Two remaining `Region` sites are still genuinely blocked, not skipped, now solely on C7: [ChartGraphics.cs:2289-2290](../Microsoft.ReportViewer.DataVisualization/Microsoft.Reporting.Chart.WebForms/ChartGraphics.cs#L2289-L2290) constructs `new Region(path)` from a still-concrete `GraphicsPath` (needs **C7**); [Axis.cs:1172-1181](../Microsoft.ReportViewer.DataVisualization/Microsoft.Reporting.Chart.WebForms/Axis.cs#L1172-L1181) constructs a region from `GetPolygonCirclePath(...)` (concrete `GraphicsPath`, needs **C7**) — its Transform usage was migrated separately as part of C2. `ChartImage.cs:101`'s `Region` use is in the Windows-only EMF/metafile export path (separate from `IChartRenderingEngine` entirely) — out of scope by design, not a gap. Verified: build 0 errors, all 5 visual regression + spike tests pass, baselines unchanged (zero behavior change on Windows).
- [x] **C2. `Matrix` → `ITransform`** (58 / 11) — use `System.Numerics.Matrix3x2` (Appendix A.6: identity-only ctor, 5 ops); verify rotation direction + transform-order parity with GDI+. 🟡 M
  **Done for all `Matrix` usage independent of `GraphicsPath.Transform`; the rest bridges via a helper pending C7.** Before touching any call site, verified GDI+'s default `MatrixOrder.Prepend` composition (`RotateAt`/`Translate`) point-for-point against `Matrix3x2` (`rot * matrix`, `Vector2.Transform`) with a standalone scratch console app across several angles/centers/offsets — exact match. Added [`Rendering/Matrix3x2Extensions.cs`](../Microsoft.ReportViewer.DataVisualization/Microsoft.Reporting.Chart.WebForms/Rendering/Matrix3x2Extensions.cs): `RotateAt`/`Translate`/`Scale`/`TransformPoints` (mirroring GDI+'s `Matrix` API shape) plus a `ToGdiMatrix()` bridge for call sites still feeding a concrete `GraphicsPath`/`Region`'s own `.Transform(Matrix)` overload. Migrated `ChartGraphics.cs` (`myMatrix` field retyped to `Matrix3x2`, driven by `GetTransform()`/`SetTransform()` instead of the legacy `Transform` property), `Axis.cs`, `Title.cs`, `Label.cs`, `ChartArea.cs` — all `base.Transform`/local-`Matrix` bookkeeping now uses the interface-typed pair; the few `GraphicsPath.Transform(matrix)` sites use `.ToGdiMatrix()` as a documented, temporary bridge. **Remaining concrete `Matrix` usage (26 occ / genuinely blocked, not skipped):** `ArrowAnnotation.cs`, `AxisScaleSegment.cs`, `CalloutAnnotation.cs` (4 sites) all rotate/translate a matrix solely to feed a concrete `GraphicsPath.Transform(Matrix)` — needs **C7** first, since bridging every remaining site individually would just be C7's work done piecemeal. Added a new visual-regression test (`RotatedLabelsChart_MatchesBaseline`, baseline generated from the pre-migration code via a temporary `git stash`) that exercises rotated axis labels and a rotated title — the two paths this migration touches most — and it passes byte-for-byte against the pre-migration render, on top of the existing 4 tests. Build: 0 errors on both passes.
- [x] **C3. `Pen` → `IPen`** (127 / 16) — ~5 properties, `(color, width)` ctor (Appendix A.4). 🟠 L
  **Done for the independent usage only — this milestone turned out far more entangled with C7 than its L sizing assumed.** Added the missing `IPen.Alignment` property (Appendix A.4 undercounted it — the doc's own ⚠ caveat on ambiguous member counts held here) and exposed `ChartGraphics.ResourceFactory` (internal, read-only) so the ~70 painter classes outside `ChartGraphics` can construct `IPen` too. Migrated the fully-independent sites (`AxisScrollBar.cs` — now 0 `Pen` occurrences; `SmartLabels.cs`'s `DrawRectangle`/`DrawLine` path; three local `Pen`s in `ChartGraphics.cs` that only ever call `DrawRectangle`/`DrawLine`/`DrawLines`). **Two new blocking discoveries, not just the expected `DrawPath`/`GraphicsPath` coupling:** (1) `ChartGraphics.Widen(GraphicsPath, Pen)` — a thin wrapper over GDI+'s `GraphicsPath.Widen(Pen)` — is called from `CalloutAnnotation.cs`, `LineAnnotation.cs`, `PolylineAnnotation.cs`, `TextAnnotation.cs`, `Title.cs`, and `Axis.cs`, and is inherently GDI+-only (stroke-to-fill geometry) with no direct `IGraphicsPath` equivalent — this is a **new risk**, not merely a C7 dependency (see §7 risk table). (2) `ChartGraphics3D.cs`'s `frontLinePen` field and its two big polygon-drawing methods looked independent (`DrawLine`/`DrawPolygon` only) until a `ChartGraphics.Widen(graphicsPath, pen3)` call turned up 40 lines further into the second method — reverted that attempt (confirmed via `git diff --stat` showing zero net change) rather than leave a half-converted, silently-wrong field. **Remaining 116 occurrences across 12 files are genuinely blocked**, either on C7 (`DrawPath`/`DrawCircleAbs`/`Widen`) or on the `AdjustableArrowCap`/`CustomStartCap` gap (`SmartLabels.cs`, one site — reverted, no cross-platform equivalent exists for custom arrow caps). Verified: build 0 errors, all 5 tests pass, baselines unchanged.
- [x] **C4. Brushes → `IBrush` family** (~200 / ~16) — `ISolidBrush` first (~90% of usage); Hatch/Gradient/Texture follow (Appendix A.5). 🟠 L
  **Port + adapter complete** (per the strategic finding above — call-site conversion moved to B2). Found and fixed a real gap: `PathGradientBrush` (16 occ / 2 files, all in shadow-rendering) was **missing entirely** from the original Appendix A.5 inventory. Added `IPathGradientBrush` (`CenterColor`/`SurroundColors`/`CenterPoint`/`FocusScales`) + `GdiPathGradientBrush` + `IDrawingResourceFactory.CreatePathGradientBrush(IGraphicsPath)` (Skia side stubbed, spike scope). `ISolidBrush`/`ILinearGradientBrush`/`ITextureBrush`/`IHatchBrush` were already complete from A1/A2. Confirmed via grep that most real call sites (`DrawStringRel`, `FillPath`) bundle `Brush` with `Font`/`StringFormat`/`GraphicsPath` in the same call — not independently convertible, hence the strategy pivot.
- [x] **C5. `StringFormat` → `ITextFormat`** (145 / 19) — contract is tiny (4 members + 2 presets, Appendix A.3); L is purely file spread. 🟠 L
  **Port + adapter complete** (`ITextFormat`/`GdiTextFormat`, drafted in A1/A2, verified complete — no gaps found). Call-site conversion deferred to B2.
- [x] **C6. `Font` → `IChartFont`** (232 / 31) — most spread; small API (Appendix A.2). Route text **measurement** through `IChartRenderingEngine.MeasureString`, not the font. 🔴 XL
  **Port + adapter complete** (`IChartFont`/`GdiChartFont`, drafted in A1/A2, verified complete). Call-site conversion deferred to B2.

  **B2 call-site conversion started (2026-07-19).** Key finding first: `Series.Font`/`DataPoint.Font`/`StringFormat` locals built by painter code are **not candidates for direct retyping** — `Font` is a public property on the widely-used data model (`DataPointAttributes.Font`, set directly by chart consumers via `series.Font = new Font(...)`), so it must stay `System.Drawing.Font` at the object-model boundary regardless of the rendering backend; only the *rendering call itself* can move to `IChartFont`. Confirmed `IChartRenderingEngine.DrawString`/`MeasureString` already had full `IChartFont`/`IBrush`/`ITextFormat` overloads (Milestone A3, unused until now) — so the real gap was one level up, in `ChartGraphics`'s helper methods (`DrawStringRel`×4, `DrawStringAbs`, `DrawPointLabelStringRel`×2, `MeasureStringRel`×2), which only had `Font`/`Brush`/`StringFormat`-typed signatures. Added matching interface-typed dual overloads for the subset actually reached by `DrawPointLabelStringRel` (the real call sites in every chart-type file), mirroring each concrete method's body exactly. `DrawPointLabelBackground` (the label-background/border painter) needed no changes — it never took `Font`/`Brush`/`StringFormat` in the first place.
  **Bridging pattern (applies at every real call site, not just this one):** the concrete `Font font`/`StringFormat format` locals stay concrete for the whole method (since position-calculation helpers like `PointChart.GetLabelPosition`/`ChartArea.smartLabels.AdjustSmartLabelPosition` are themselves concrete-typed and out of scope here) — only at the final `DrawPointLabelStringRel` call are they bridged: `graph.ResourceFactory.CreateFont(font.FontFamily.Name, font.Size, font.Style, font.Unit)` for the font, a `resourceFactory.CreateTextFormat()` with its 4 properties copied from the concrete `format` for the text format, and `resourceFactory.CreateSolidBrush(point.FontColor)` in place of `new SolidBrush(...)` for the brush. This is the same shape as the `GraphicsPath`→`IGraphicsPath` `PathPoints`/`PathTypes` bridge used throughout B2 — read properties off the concrete object, construct the interface-typed resource from those values, right at the sink.
  **Converted one real caller as proof:** `PointChart.cs`'s private `DrawLabels` (confirmed private, single file, no override concerns). Not previously exercised by any baseline (`SeriesChartType.Point` with labels enabled unused) — added `RenderPointChartWithLabels` and generated its baseline via the same `git stash` technique; verified byte-for-byte. Build 0 errors, all 15 visual regression tests pass.
  **Remaining scope, precisely:** every other `DrawPointLabelStringRel` call site found during the earlier survey (`TreeMapChart.cs`, `SunburstChart.cs`, `BarChart.cs`, `ErrorBarChart.cs`, `BoxPlotChart.cs`, plus `StockChart.cs`'s and others' own label methods) follows the *identical* bridging pattern proven here — each is a small, mechanical, low-risk per-file conversion now that the `ChartGraphics` dual-overload infrastructure exists, but each still needs its own read-the-full-method-first pass (some, like `StockChart.cs`'s `DrawLabel` and `PointChart.cs`'s `DrawLabels` itself for `SmartLabels.Enabled`, thread `format` through a `ref StringFormat` parameter on `AdjustSmartLabelPosition` — harmless to leave concrete, but worth confirming per file) and its own baseline. Not attempted further in this pass — this establishes the pattern and proves it end-to-end, matching this session's practice of proving a technique on one real site before repeating it broadly.

  **All remaining `DrawPointLabelStringRel`/`DrawStringRel` call sites from the survey converted (2026-07-19), same day.** `BarChart.cs` (both `DrawLabelsAndMarkers`'s 2D `RectangleF`-position call and `DrawLabels3D`'s `PointF`-position call — the latter needed no new `ChartGraphics` overload since the `PointF` variant was already added for `PointChart.cs`), `ErrorBarChart.cs`, `BoxPlotChart.cs`, and `StockChart.cs`'s `DrawLabel` (confirmed its one subclass, `CandleStickChart.cs`, doesn't override `DrawLabel`) all followed the exact same read-the-method-first-then-bridge-at-the-sink pattern, each confirmed `private`/`protected virtual`-with-no-overriding-subclass first. `TreeMapChart.cs` needed both its `DrawStringRel` (series label, `PointF` position) and `DrawPointLabelStringRel` (data-point label, `PointF` position) call sites converted — both `private static` methods, no override concerns. `SunburstChart.cs` also converted (its `RenderLabel` uses a `private static StringFormat format` field shared across the whole class — read from, not mutated, by the bridge, so no behavior change to the field's lifecycle) but **could not be visually verified**: constructing any chart with `SeriesChartType.Sunburst` throws `InvalidOperationException("TreeMap chart cannot be combined...")` today, because `SunburstChart.Name` (SunburstChart.cs:15) incorrectly returns `"TreeMap"` instead of `"Sunburst"` — a pre-existing, unrelated bug (confirmed by reading `ChartTypeNames.cs`/`Chart.cs`'s registration, which correctly registers it under the key `"Sunburst"`) that makes the chart type non-functional independent of this migration. Not fixed here — flagged as a separate, unrelated defect for the user to decide on, out of scope for a GDI+-abstraction pass. Added 6 new sample charts + regression tests (`BarChartWithLabels`, `BarChart3DWithLabels`, `ErrorBarChartWithLabels`, `BoxPlotChartWithLabels`, `StockChartWithLabels`, `TreeMapChartWithLabels`) covering the 6 verifiable conversions, each generated against pre-conversion code via the same `git stash` technique (stashing only the 6 `ChartTypes/*.cs` files, keeping `ChartGraphics.cs`'s new overloads so `PointChart.cs`'s already-converted call site still compiled) and confirmed byte-for-byte. Build 0 errors, all 21 visual regression tests pass. **This completes the C5/C6 real-caller migration for every `DrawPointLabelStringRel`/`DrawStringRel` site found in the Milestone B2 file survey.**

  **`SunburstChart.Name` bug fixed (2026-07-19).** Changed `SunburstChart.cs:16` from `"TreeMap"` to `"Sunburst"`, matching its actual registration key (`Chart.cs:994`, `ChartTypeNames.cs:77`). This eliminates the `InvalidOperationException` from the combine-check that fired for every chart built with `SeriesChartType.Sunburst`.

  **Follow-up investigation: why a direct `Chart`/`Series`/`DataPoint` construction still rendered blank, and what the real fix looks like.** `SunburstChart.Paint` reads its hierarchy from `ChartArea.CategoryNodes` (`SunburstChart.cs:109`) rather than building it from `Series`/`Points` the way `TreeMapChart.BuildTreeNodes` does. Searching the whole solution (not just `Microsoft.ReportViewer.DataVisualization`) found the real populator: `Microsoft.ReportViewer.Common/Microsoft.ReportingServices.OnDemandReportRendering/ChartMapper.cs`'s `RenderCategoryGrouping` family (~line 4073) walks the RDL report's `ChartCategoryHierarchy`/`ChartMember` tree and builds a `CategoryNodeCollection` from it — this is the only call site anywhere that ever assigns `ChartArea.CategoryNodes`, and it's gated on `VisualizeCategoryGrouping` finding a `SeriesChartType.Sunburst` series in the chart area. Since this project renders RDLC reports, that's the real, working, intended path for Sunburst charts — it's just unreachable from a `Chart` object built directly in C# (which is what the DataVisualization `VisualRegressionTests` project does, and why the first attempt at a regression test there rendered blank). `CategoryNode`/`CategoryNodeCollection` being `internal sealed class` with no public constructor is consistent with this: they're only ever meant to be built by `ChartMapper` (which has the necessary access via `InternalsVisibleTo`), not by a direct API consumer.

  **Added a proper RDL-driven regression test in a new project, `tests/Microsoft.ReportViewer.Chart.Rdl.Tests`**, since the existing `VisualRegressionTests` project structurally cannot exercise this path (it never touches RDL/`ChartMapper`). The test (`SunburstChartRdlTests.SunburstChartWithCategoryHierarchy_MatchesBaseline`) loads `Reports/SunburstReport.rdlc` — a hand-authored 2016/01-namespace RDL with a 2-level `ChartCategoryHierarchy` (`Category` → `SubCategory`, each a dynamic `Group`/`GroupExpression` off a `Data` dataset) driving a single `Type=Shape`/`Subtype=Sunburst` series with `Y=Sum(Fields!Value.Value)` — via `LocalReport.LoadReportDefinition` + `ReportDataSource("Data", rows)`, renders to PNG via `Render("IMAGE", "<DeviceInfo><OutputFormat>PNG</OutputFormat></DeviceInfo>")`, and compares against a new baseline (reusing the same `ImageComparer` pattern as `VisualRegressionTests`). Getting the RDL right took a few iterations (RDL schema requires `<ReportSections><ReportSection>` wrapping `Body`/`Page`, a `Label` is mandatory even on the single static series-hierarchy `ChartMember`, and the series type is `Type=Shape`/`Subtype=Sunburst`, not `Type=Sunburst` — confirmed via `Validator.cs:1215`/`ValidateChartSeriesSubtype`). The resulting render shows two correctly nested rings (inner = Category, outer = SubCategory) with correctly proportioned wedges and labels on both levels — visually confirmed before promoting to baseline. Build 0 errors, all 22 tests across the solution (21 `VisualRegressionTests` + 1 new) pass. **Sunburst is confirmed working end-to-end for its real (RDL) use case; the `Name` bug is fully fixed and now has regression coverage.**
- [x] **C7. `GraphicsPath` → `IGraphicsPath`** (290 / 24) — largest; full method set enumerated in Appendix A.1 (~24 methods, no hidden surface). 🔴 XL
  **Port + adapter complete** (`IGraphicsPath`/`GdiGraphicsPath`, drafted in A1/A2 — all ~24 methods implemented, not just the spike-scoped subset `SkiaGraphicsPath` has). `Widen(IPen)` is on the interface already; the Skia side of that (stroke-to-fill geometry) is real future work, not an interface gap (see §7 risk table). Call-site conversion deferred to B2 — this was going to be the true bottleneck for per-type conversion (`DrawPath`/`FillPath` touch `GraphicsPath` together with `Pen`/`Brush` at nearly every real site).
- [x] **C8. `ImageAttributes` → `IImageDrawOptions`** (25 / 6); coordinate with the existing `IImageProvider` background-image work in `chart-image-abstraction-analysis.md`. 🟡 M
  **Port + adapter complete** (`IImageDrawOptions`/`GdiImageDrawOptions`, drafted in A1/A2, verified complete). Call-site conversion deferred to B2.

### Milestone D — Surface boundary & backend selection

- [x] **D1. `IRenderSurfaceFactory`** replacing `new Bitmap` + `Graphics.FromImage` ([ChartPicture.cs:733-734](../Microsoft.ReportViewer.DataVisualization/Microsoft.Reporting.Chart.WebForms/ChartPicture.cs#L733-L734)) and encode at [Chart.cs:1313](../Microsoft.ReportViewer.DataVisualization/Microsoft.Reporting.Chart.WebForms/Chart.cs#L1313). Guard the EMF/`Metafile` path ([Chart.cs:1281-1293](../Microsoft.ReportViewer.DataVisualization/Microsoft.Reporting.Chart.WebForms/Chart.cs#L1281-L1293)) as Windows-only. 🟠 L
  **Narrower scope than originally framed — done for that scope, 2026-07-19.** Investigated first: the real production path is `Chart.Save` → `ChartImage.GetImage(float)` (a `public Bitmap`-returning method; `ChartPicture` doesn't itself define `GetImage` — `ChartImage : ChartPicture` does, and `Chart.chartPicture` is typed `ChartImage`) → `image.Save(stream, format)`; the doc's original line numbers were stale from an earlier layout. Found and converted all 4 real `new Bitmap(...)` + `Graphics.FromImage(...)` construction sites to `IRenderSurfaceFactory.CreateRasterSurface(w, h, dpi)` (backed by the already-drafted `GdiRenderSurfaceFactory`/`GdiRenderSurface`): `ChartImage.GetImage` (the public bitmap-returning render entry point — downcasts to `GdiRenderSurface` to keep returning a concrete `Bitmap`, since that's still this method's public contract), `ChartImage.GetSvgImage`, `ChartPicture.Select`'s hit-test bitmap, and `ChartPicture.Paint`'s SVG-border special-case bitmap. Added `internal IRenderSurfaceFactory renderSurfaceFactory = new GdiRenderSurfaceFactory()` to `ChartPicture`, mirroring the existing `ChartGraphics.resourceFactory` default-to-Gdi pattern — this is the seam D2 will later assign a Skia factory into. Left `ChartImage.SaveIntoMetafile`'s `new Bitmap`+`Graphics.FromImage` **fully untouched, deliberately**: that pair exists solely to obtain a Windows `IntPtr` HDC (`graphics.GetHdc()`) to construct a `System.Drawing.Imaging.Metafile` — intrinsically Windows-only, no `IRenderSurface` abstraction attempted (matches the already-documented EMF-is-Windows-only scope boundary). **What D1 does *not* yet do** (this was investigated and explicitly scoped out, not missed): `ChartPicture.Paint(Graphics graph, ...)`'s two public overloads still take a concrete `System.Drawing.Graphics` parameter, and `ChartGraphics.Graphics` is still a settable concrete-`Graphics` property — the actual seam a non-GDI backend would need to plug into at the top-level paint entry point. That is real, separate work (effectively D2/E1's job, already correctly gated in §6 on B1b/B2/C1-C8 substantially completing first) — D1 here only removes the *direct* `new Bitmap`/`Graphics.FromImage` construction calls from behind an interface, it doesn't make the paint entry point backend-agnostic. Verified: build 0 errors; all 10 visual regression tests pass byte-for-byte (the harness renders via `chart.Save(stream, ChartImageFormat.Png)`, the exact path converted) — zero behavior change on Windows.
- [ ] **D2. Backend factory selection by platform** (Windows → GDI+ adapters, else → Skia), mirroring the Excel `ImageProviderFactory` pattern. 🟢 S
- [ ] **D3. Remove the temporary `System.Drawing`-typed `IChartRenderingEngine` overloads** from A3 once all callers use interfaces. 🟡 M

### Milestone E — Skia adapter & verification

- [x] **E0. Visual regression harness** (pixel-baseline scaffolding, ahead of the Skia work it will eventually verify). 🟢 S
  **Done** in [`tests/Microsoft.ReportViewer.DataVisualization.VisualRegressionTests/`](../tests/Microsoft.ReportViewer.DataVisualization.VisualRegressionTests/) — MSTest project with `InternalsVisibleTo` access into `Microsoft.ReportViewer.DataVisualization` (new entry added to [AssemblyInfo.cs](../Microsoft.ReportViewer.DataVisualization/Properties/AssemblyInfo.cs#L17), signed with the shared `ReportViewerCore.snk`). `SampleCharts.cs` builds a `Chart` directly against the internal engine API (no `.rdlc`/host needed) for a simple bar chart and line chart; `ImageComparer.cs` does a per-pixel compare (tolerance 2/channel, for encoder rounding only — rendering is otherwise deterministic, confirmed via repeated runs) against a committed PNG in `Baselines/`, writing the actual output and a red/transparent diff PNG to `Results/` (gitignored, under `bin/`) on any mismatch or missing baseline. Two baselines committed (`SimpleBarChart.png`, `SimpleLineChart.png`); both currently pass. This is the mechanism Phase 0's "render one simple bar chart, compare pixels" step and Milestone E2 will both run through — E2 still needs the Skia adapter (E1) and a broader report corpus; this only proves the harness/baseline mechanics work today, against GDI+ output only.
- [ ] **E1. Implement the Skia adapter set** (`SkiaResourceFactory`, `SkiaPen/Brush/Font/Path/…` over `SKPaint`/`SKPath`/`SKFont`). 🔴 XL
  **Phase 0 spike done** (see `chart-cross-platform-implementation.md` Spike Report, 2026-07-18) — a partial, spike-scoped `Rendering/Skia/` adapter set + [`SkiaChartGraphics`](../Microsoft.ReportViewer.DataVisualization/Microsoft.Reporting.Chart.WebForms/SkiaChartGraphics.cs) already exist and render a hand-built scene correctly on both Windows and Linux, proving the port design works. **This milestone is not shortened by that, though** — the spike also found GDI+ can't even construct on Linux today (any `System.Drawing` object, not just via the rendering seam), which means **B1b/B2/C1-C8 must be substantially complete before E1's adapters are reachable from a real `Chart`** (today `ChartGraphics` still calls the GDI+-typed `IChartRenderingEngine` overloads exclusively, which the spike's `SkiaChartGraphics` deliberately leaves as unreachable stubs). Treat B1b/B2/C1-C8 as blocking prerequisites for E1, not parallel/independent work.
- [ ] **E2. Visual regression** across a report corpus (Windows GDI+ vs Linux Skia), per Phase 6 of the parent plan. Extend the E0 harness with more chart types/gauges and a second (Skia) render path once E1 lands. 🟠 L

---

## 6. Sequencing & dependencies

```
A1 → A2 → A3 → B1 → B2 → (C1…C8 in parallel) → D1 → D2 → D3 → E1 → E2
                                   │
        C6 (Font) also unblocks cross-platform text measurement
        C7 (GraphicsPath) is the critical-path long pole
```

- **Milestones A–D deliver a fully GDI+-backed engine that still behaves identically on Windows** but is now expressed in interfaces. That's a safe, mergeable state with zero cross-platform code yet.
- **Milestone E** adds the first non-GDI+ backend. The design is validated only when E2 passes.

---

## 7. Risks

| Risk | Impact | Mitigation |
|------|--------|-----------|
| **Text metrics differ** (GDI+ `MeasureString` vs Skia `SKFont`) — C6 | Layout drift | Route measurement through `IChartRenderingEngine.MeasureString`; tolerance-based visual diffs |
| **`GraphicsPath` surface is large** — C7 | Long pole | ✅ Surface now fully inventoried (Appendix A.1): ~24 methods, no hidden API. Risk downgraded to "large but bounded." |
| **`Matrix` semantics** (order, transform direction) — C2 | Subtle geometry bugs | Unit-test transform parity before/after |
| **Big-bang signature change** on `IChartRenderingEngine` | Compilation churn | A3 keeps old overloads; remove only in D3 |
| **Gauge engine is separate** | Charts fixed, gauges still GDI+ | Repeat pattern for `GaugeContainer` (parent plan Phase 5) |
| **Effort genuinely large** (C6+C7 ≈ 520 occ / 40+ files) | Schedule | Phase 0 spike sizes C7/C6 against one real Skia primitive before committing |
| **GDI+ cannot construct on Linux at all** (any `System.Drawing` object, not just via rendering) — confirmed in the Phase 0 spike, holds even with `libgdiplus` installed | E1 can't be exercised by a real `Chart` until B1b/B2/C1-C8 are done, not just started | Treat B1b/B2/C1-C8 as a hard blocking prerequisite for E1 in scheduling, not parallelizable "nice architecture" work |
| ~~`GraphicsPath.Widen(Pen)` has no cross-platform equivalent~~ — **resolved 2026-07-19** | ~~Stroke-to-fill geometry is GDI+-only~~ | `IGraphicsPath.Widen(IPen)` now has a real `SkiaGraphicsPath` implementation via `SKPaint.GetFillPath` (Skia's own stroke-to-fill primitive — no hand-rolled algorithm needed). 9 of 13 real callers converted; the remaining 4 are blocked on unrelated, deeper issues (`GraphicsPathIterator` in `CalloutAnnotation.cs`; concrete-rendering entanglement in `ChartGraphics3D.cs`/`RangeChart.cs`'s `Draw3DSurface`-style methods) — see chart-gdi-type-abstraction.md Milestone C progress notes |
| ~~`ChartRenderingEngine.Clip` is a `public Region` property, not just a `new Region(path)` construction gap~~ — **resolved 2026-07-19** | ~~Retyping it would ripple across the whole engine's public surface~~ | Turned out **not to need retyping at all** — `IChartRenderingEngine.GetClipRegion()`/`SetClipRegion(IClipRegion)` already existed (Milestone C1), so this was a real-caller migration, same as `Pen`/`Brush`. Extended `IClipRegion` with 4 missing members (`Translate`, `Clone`, `IsInfinite`, `Complement`) and converted 11 real call sites across 7 files; verified with 3 new pixel-exact regression tests. `RangeChart.cs`'s equivalent block stays concrete (shares its path/brush with the main fill, and also touches `graph.Transform`) — see Milestone C progress notes |
| **`Pen.CustomStartCap`/`AdjustableArrowCap` has no cross-platform equivalent** — discovered during C3, one site (`SmartLabels.cs`) | That one site (custom arrow-shaped smart-label callout line caps) can't move off concrete `Pen` | Low priority — single site, cosmetic; leave GDI+-only (Windows-only feature) or approximate with a fixed `LineCap` on the Skia backend later |

---

## 8. Open questions for the spike (Phase 0)

1. Interfaces vs descriptor records for Pen/Brush/Font/StringFormat (§3) — decide against one real Skia implementation.
2. ~~`System.Numerics.Matrix3x2` sufficient for `ITransform`?~~ — **Yes** (Appendix A.6). Remaining check: rotation direction / transform-order parity with GDI+.
3. ~~Exact `GraphicsPath` method surface actually used~~ — **inventoried** (Appendix A.1).
4. Can the GDI+ adapters be introduced with **zero** rendered-output change on Windows? (Must be yes before merging Milestones A–D.)
5. **Text-metric parity** (GDI+ `MeasureString` vs `SKFont`) — the one unknown only a running prototype can settle.

> **Port surface is now fully specified.** Appendix A enumerates every member of all resource interfaces (`IGraphicsPath`, `IChartFont`, `ITextFormat`, `IPen`, `IBrush` family, `ITransform`). The `IDrawingResourceFactory` can be authored directly from it. The remaining spike work is behavioral parity (#4, #5), not API discovery.

---

## Appendix A — Measured member surface (C5/C6/C7)

Counts from `grep -rohE` across the Chart engine. **Caveat:** counts for generic member names (`.Name`, `.Size`, `.Height`, `.Clone`, `.Dispose`, `.Style`) are **inflated by ambiguity** — they match many types, not just the one in question. Those are marked ⚠️ and should be treated as "used, count unreliable." The distinctive members and constructor patterns are reliable and are what drive the interface shape.

### A.1 `IGraphicsPath` (C7) — the surface is bounded and fully enumerable

Constructors: `new GraphicsPath()` ×92, `new GraphicsPath(points, types)` ×4.

| Member | Hits | Member | Hits | Member | Hits |
|--------|------|--------|------|--------|------|
| `AddLine` | 114 | `AddArc` | 29 | `CloseAllFigures` | 31 |
| `AddBezier` | 36 | `AddEllipse` | 15 | `IsVisible` ⚠️ | 26 |
| `AddLines` | 13 | `AddPolygon` | 12 | `Transform(matrix)` ⚠️ | 16 |
| `AddRectangle` | 11 | `AddPath` | 10 | `GetBounds` ⚠️ | 16 |
| `AddCurve` | 2 | `AddClosedCurve` | 2 | `Reset` ⚠️ | 16 |
| `AddPie` | 2 | `AddString` | 2 | `Reverse` | 10 |
| `StartFigure` | 9 | `CloseFigure` | 4 | `SetMarkers` | 6 |
| `Flatten` | 8 | `Widen` | 8 | | |
| **props:** `PathPoints` | 13 | `PointCount` | 13 | `PathTypes` | 3 |

→ **~24 methods + 3 properties + 2 constructors.** Large but finite. This retires the "hidden method surface" risk from §7: `IGraphicsPath` can be specified completely up front.

### A.2 `IChartFont` (C6) — small distinct surface, wide spread (31 files)

Reliable constructor patterns to support on the factory:
- `Create(familyName, sizeInPoints)` — most common (~19 sites)
- `Create(familyName, size, FontStyle)`
- `Create(familyName, size, FontStyle, GraphicsUnit)`
- `Derive(existingFont, newStyle)` and `Derive(existingFont.FontFamily, newSize, style, unit)` — re-style/re-size an existing font

Reliable members: `FontFamily` (16), `SizeInPoints` (11), `Unit` (3), `Style` ⚠️(42), `Name` ⚠️(218), `Size` ⚠️(107).
**Text measurement is NOT on the font** — it flows through `IChartRenderingEngine.MeasureString` (route to `SKFont` in the Skia adapter). The XL sizing is from the 31-file spread, not API complexity.

### A.3 `ITextFormat` (C5) — cleanest of the three

Constructors: `new StringFormat()` ×19, `StringFormat.GenericTypographic` preset ×3, `StringFormat.GenericDefault` preset ×1.
Members: `Alignment` (95), `LineAlignment` (60), `FormatFlags` (38), `Trimming` (11).
→ **4 members + 2 presets.** L sizing is purely the 19-file spread; the contract is tiny and can be a `readonly record struct` behind the interface.

### A.4 `IPen` (C3) — small clean surface, driven by spread

Constructor: overwhelmingly `new Pen(color, width)` (a few via `Color.FromArgb`). No brush-based pens.
Reliable distinctive members: `DashStyle` (18), `StartCap` (33), `EndCap` (23), `LineJoin` (2). Ambiguous ⚠: `Width` (1155 — mostly Rectangle/element widths, **not** `Pen.Width`), `Color` (47), `Alignment` (95 — mostly `StringFormat`).
Not used (0 hits): `DashPattern`, `DashCap`, `MiterLimit`, `DashOffset`, `Pen.Brush`.
→ **~5 properties + 1 constructor.** L sizing is the 16-file spread, not API size.

### A.5 `IBrush` family (C4) — `SolidBrush` dominates

Construction counts: `SolidBrush` ×68, `LinearGradientBrush` ×7, `TextureBrush` ×3, `HatchBrush` ×1.
- `ISolidBrush` — `new SolidBrush(color)`; **~90% of all brush usage.**
- `IGradientBrush` — plus `Blend` (2), `InterpolationColors` (3), `WrapMode` (2).
- `ITextureBrush` — image fill + `WrapMode`.
- `IHatchBrush` — style + fore/back color (rare; 1 construction site).
→ Prioritize `ISolidBrush`; the other three are low-frequency and can follow.

### A.6 `ITransform` (C2) — confirms `System.Numerics.Matrix3x2` is viable

Construction: **only** `new Matrix()` (identity) ×14 — the 6-float affine constructor is **not used**.
Members: `TransformPoints` (46, the workhorse), `RotateAt` (22), `Translate` (14), `Reset` ⚠(16, shared with `GraphicsPath.Reset`), `Scale` (2). Not used: `Multiply`, `Invert`, `TransformVectors`, `Elements`, `Shear`.
→ Tiny op set. `System.Numerics.Matrix3x2` covers all of it: `CreateTranslation`/`CreateScale`/`CreateRotation` + `Vector2.Transform` for points; `RotateAt` composes as translate→rotate→translate. **Spike question #2 is effectively answered: yes.** Only caveat to verify — rotation direction / append-vs-prepend order parity with GDI+.
