# RichText text-shaping cross-platform abstraction (PDF's real blocker)

**Status (2026-07-26): the real RichText/Uniscribe production pipeline is now wired into PDF rendering end to end ("Option B"), not just the base-14/embedded-font MVP.** See "Option B: real TextBox.RenderParagraph pipeline wired into PDF" below for what changed and what's verified. This supersedes the "P4" line item in `pdf-render-callstack-analysis.md`/`pdf-quick-reference.md`. The MVP section below (and the phased plan after it) is now historical context for how this was reached, not the current state of PDF text rendering.

**Production wiring started (2026-07-26): the real RichText pipeline (`FontCache`/`TextRun`/`Paragraph`/`TextLine`) now has working cross-platform branches for itemization, line-break analysis, shaping, and font metrics — item (a) from "What real production wiring still needs beyond these prototypes" below.** See "Production wiring, increment 1" further down for what's done, what's tested, and what's still missing before this reaches parity with the base-14/embedded-font MVP bypass above (which remains what real cross-platform PDF renders actually use today — this increment is not yet reachable from any production render call).

**P4 progress (2026-07-26): the shaping/itemization/line-break prototypes are now wired into PDFWriter's cross-platform text path, for measurement only - not for drawing.** `DrawWrappedText`/`DrawWrappedRichText` now decide word-wrap points and decoration-rectangle widths using real shaped glyph widths and soft-break opportunities (`ShapedTextMetrics`/`ShapedTextWrapper`/`ShapedStyledTextWrapper`, `Microsoft.ReportingServices.Rendering.ImageRenderer/ShapedTextLayout.cs`) instead of the MVP's per-character-class approximation table. This does NOT touch the RichText/Uniscribe production pipeline (`FontCache`/`TextRun`/`LineBreaker`/`Paragraph` still exclusively Win32) - that remains the real, not-yet-attempted "full fidelity" wiring described in "Recommended phased plan" below.

**Real font embedding, same day:** drawing itself was extended past base-14/WinAnsi - see "Real font embedding for the cross-platform path" below. This reverses the "font embedding deferred" note above; base-14 is now a fallback (`EmbedFonts == FontEmbedding.None`), not the only option.

## MVP: base-14 fonts + approximate word-wrap (done 2026-07-26)

Tracing `PDFWriter`'s own "P3" font-metrics call sites (`ProcessDrawStringFont`/`WriteFont`/`WriteEmbeddedFont`/`ProcessFontForFontEmbedding`) down to their actual inputs found they were never independently reachable: `run.CachedFont.Font`/`.Hfont`/`.TextMetric` all come from a live `RichText.CachedFont`, constructed by the RichText pipeline *before* `PDFWriter.DrawTextRun` is ever invoked (it's a callback from inside `TextBox.Render`). So "P3 is bounded, independent of P4" (the original 2026-07-24 scoping) was itself wrong — P3's call sites are only reachable through P4's blocked pipeline. There is no way to reach them on Linux at all, regardless of how portable PDFWriter's own code is.

The unlock: PDF's standard 14 fonts (Helvetica/Times/Courier families) need **no** embedded font data and **no** `/Widths` array — real PDF readers already know their metrics natively (`WriteFont`'s existing `InternalFont` branch confirms this: it emits only `/Subtype /Type1 /BaseFont /Helvetica /Encoding /WinAnsiEncoding`, nothing else). `PDFWriter.cs`'s existing `m_internalFonts` dictionary already maps common Windows family names (`"Arial"`, `"Times New Roman"`, `"Courier New"`, with `,Bold`/`,Italic`/`,BoldItalic` suffixes) to these base-14 names - the exact same map `ProcessDrawStringFont` uses for its own fast path. And since the PDF reader lays out `Tj` text itself using the font's real metrics, `PDFWriter` never needs pixel/point-exact glyph widths for *positioning* - only an approximate width for its *own* word-wrap decisions (where imprecision costs a slightly early/late line break, not a spec violation).

This adds up to a real, if reduced-fidelity, path requiring **zero** GDI+/Win32/RichText/SkiaSharp/HarfBuzzSharp dependency:

- `CrossPlatformTextLayout.cs`: `ApproximateTextMetrics` (rough per-character-class em-width table) and `SimpleTextWrapper` (greedy word-wrap on explicit line breaks + spaces).
- `WriterBase.DrawWrappedText(RectangleF, PointF, string, ITextRunProps)`: new virtual hook, no-op by default (`ImageWriter`/EMF path unaffected).
- `PDFWriter.GetOrCreateBase14Font`/`DrawWrappedText`: resolves family/bold/italic to a base-14 font via the existing `m_internalFonts` map (unknown families fall back to the Helvetica family), reuses `WriteFont`/`ProcessFontForFontEmbedding`'s existing `m_fonts`/`ReserveObjectId`/`m_fontsUsedInCurrentPage` bookkeeping so the synthetic `PDFFont` (constructed with `CachedFont: null`, `InternalFont: true`, `IsComposite: false`) gets written out by the *existing, unmodified* font-dictionary code - which already short-circuits before touching `CachedFont`/HDC for that combination (verified by reading both methods). Emits `BT/Tf/color/Td/Tj/T*/ET` directly via `WriteText(null, ...)`, which was confirmed by reading `WriteText`/`MapGlyphToUnicodeChar` to never dereference its `TextRun` parameter on the `InternalFont`-non-composite branch.
- `Renderer.ProcessSimpleTextBox`: branches on `!OperatingSystem.IsWindows()` to call `Writer.DrawWrappedText(...)` instead of constructing `RichText.Paragraph`/`TextBox`/calling `richTextBox.ScriptItemize()`/`LineBreaker.Flow`/`TextBox.Render` at all. `ProcessRichTextBox` (multi-run rich text) is unchanged - a documented next-increment gap, below.

**Verified (2026-07-26):** built and ran a minimal RDLC (`tests/Microsoft.ReportViewer.Chart.Rdl.Tests/Reports/SimpleTextboxReport.rdlc`) with the cross-platform branch temporarily forced (a one-line, reverted-after-verification edit to `ProcessSimpleTextBox`'s condition) and rendered it to PDF with `HumanReadablePdf=true`; the output PDF opened correctly with the expected text, correctly word-wrapped at the textbox width, in Helvetica. Also added a permanent test (`SimpleTextboxRdlTests.SimpleTextbox_RendersToPdf_ContainsExpectedText`, using the same report) that runs the *Windows* path (this dev environment can't run Linux directly) and asserts the PDF is well-formed and contains the expected text - full test suite passing (155 tests) with zero behavior change on Windows.

**Extended to rich (multi-run) text boxes (2026-07-26, same day):** `Renderer.ProcessRichTextBox` now has the same platform branch, calling a new `WriterBase.DrawWrappedRichText`/`PDFWriter` implementation. `StyledTextWrapper.WrapParagraph` (`CrossPlatformTextLayout.cs`) word-wraps a paragraph's runs *across* run boundaries (a run can split mid-word-wrap the same as plain text), merging adjacent same-style fragments; each paragraph is wrapped independently so paragraph breaks are preserved as forced line breaks. Drawing relies on the PDF reader's own post-`Tj` text-position advance (driven by the *actual* font metrics for whichever base-14 font is current) to place successive same-line fragments, so `PDFWriter` never computes per-fragment x-positions itself - only `Tf`/color changes between fragments. All runs in a text box share one line-height/baseline sized to the box's largest font (documented approximation for badly mixed sizes, not exact per-run baseline alignment). Verified the same way as the simple case: forced the branch locally, rendered a report with a bold-prefix + normal-suffix run pair (word-wrapped correctly across the run boundary) plus a second, separately-styled italic paragraph, confirmed correct in the rendered PDF, reverted. Permanent test: `RichTextboxRdlTests.RichTextbox_RendersToPdf_ContainsExpectedText` (runs the Windows path, same automated-testing-gap caveat as `SimpleTextboxRdlTests`). Full suite: 156 tests passing.

**Extended with center/right text alignment (2026-07-26, same day):** both `DrawWrappedText` and `DrawWrappedRichText` now take a resolved `RPLFormat.TextAlignments` (resolved from `RPLFormat.TextAlignments.General` against the text box's `DefaultAlignment`/`Direction` by a new `Renderer.ResolveTextAlignment` helper, mirroring `RichText.TextBox.RenderParagraph`'s identical resolution). Each wrapped line's approximate width (the same `ApproximateTextMetrics` used for word-wrap) determines that line's start-x for center/right; line-to-line movement switched from `TL`/`T*` to an explicit relative `Td (dx, dy)` per line, since different lines can need different start-x under center/right alignment (a fixed `TL` leading only supports a single shared x). This was deliberately deferred in the original MVP ("an unverified per-line-position scheme was judged riskier...") but proved straightforward once implemented, since it only affects the *start* x of each already-wrapped line - drawing within a line is still handled by the PDF reader's own per-fragment advance. Verified the same way (forced locally, rendered a centered title and a right-aligned line, both visually correct in the output PDF, reverted). Permanent test: `AlignedTextboxRdlTests`. Full suite: 157 tests passing.

**Extended with underline/strikethrough (2026-07-26, same day):** `ITextRunProps.TextDecoration` (`RPLFormat.TextDecorations`: None/Underline/Overline/LineThrough - a single value per run, not flags) is now read on both paths. PDF text objects (`BT`...`ET`) may only contain text-showing/text-state operators - path-painting operators like `re f` are not legal inside one - so decorations can't be emitted inline with the `Tj` calls. Instead, a new `PDFWriter.AppendDecorationRectangle` helper builds a separate content-stream fragment (filled rectangle, same `WriteColor`/`WriteRectangle`/`re f` pattern `FillRectangle` already uses elsewhere in this file) appended as a second `m_pageContentsSection` entry right after the `BT`...`ET` block, so it's drawn on top of the text it decorates. `DrawWrappedText` computes one rectangle per wrapped line (whole-line decoration, since the box has one style); `DrawWrappedRichText` computes one rectangle per styled fragment, tracking each fragment's start-x by accumulating `ApproximateTextMetrics` widths itself (the PDF reader's own post-`Tj` advance still positions the actual glyphs; this running estimate is only used to size/place the decoration rectangle, so the same approximate-width caveat as word-wrap applies - a slight over/under-run in rare cases, not a correctness risk). Underline sits `0.15em` below baseline, strikethrough `0.3em` above (roughly text mid-height); both `~0.06em` thick, minimum `0.5pt`. Overline is not handled (not requested; a one-line addition to `AppendDecorationRectangle`'s switch if needed later). Verified the same way as prior increments: forced the cross-platform branch locally, rendered a report with a whole-line-underlined textbox, a whole-line-strikethrough textbox, and a rich-text box mixing plain/underlined/struck-through runs in one paragraph - all four decorations landed correctly positioned in the output PDF, reverted. Permanent test: `DecorationRdlTests`. Full suite: 158 tests passing.

**Known gaps in this MVP, honestly scoped rather than silently missing:**
- **Font family/embedding gap superseded (2026-07-26)** - see "Real font embedding for the cross-platform path" below. This bullet described the state before that increment; base-14/WinAnsi is now only what's used when `EmbedFonts == FontEmbedding.None`.
- **Word-wrap widths are approximate**, not exact glyph metrics - cosmetic imprecision only (see above), not a correctness risk, but real Adobe AFM-exact wrapping would look better. The same approximation now also sizes/places underline/strikethrough rectangles in rich text (see above).
- **No overline, no vertical/rotated text, no highlight runs** on this path - underline/strikethrough are done (above); the rest of `RPLTextBoxProps`' styling beyond family/size/bold/italic/color/decoration is not read yet.
- ~~No automated test exercises the cross-platform branch itself~~ **Resolved (2026-07-26):** `Microsoft.ReportViewer.Common/Properties/AssemblyInfo.cs` now grants `InternalsVisibleTo` to `Microsoft.ReportViewer.Chart.Rdl.Tests` - not a new trust boundary, since that test project is already signed with the same `ReportViewerCore.snk` key as `Microsoft.ReportViewer.Common` itself (verified: same `AssemblyOriginatorKeyFile` in both `.csproj`s). `CrossPlatformPdfTextTests.cs` calls `PDFWriter.DrawWrappedText`/`DrawWrappedRichText` directly (constructing a minimal `Renderer`/`PDFWriter`/`MemoryStream` and reading the emitted content-stream operators back out) - this runs the actual cross-platform code on every platform/CI run, not just when someone manually forces the branch. It doesn't need `EndReport()` (which requires a fully-processed `RplReport` for document metadata) - `EndPage()` already flushes the page's content stream to the underlying `Stream`, which is all these tests assert on.

If this MVP needs richer fidelity later (real embedded fonts, exact metrics, arbitrary Unicode), the SkiaSharp/HarfBuzzSharp path below is still the way to get there - but it's no longer a prerequisite for "simple text boxes render on Linux," which is done.

## Real shaped-width wrapping wired into the MVP (done 2026-07-26)

The MVP above wrapped/measured text with `ApproximateTextMetrics` (a fixed per-character-class em-width table) and `SimpleTextWrapper`/`StyledTextWrapper` (split-on-space word-wrap). Once the P4 shaping/itemization/line-break prototypes existed and composed correctly (`UnicodeParagraphShaper`), they were wired into the MVP's word-wrap and decoration-sizing decisions in place of that approximation - `ShapedTextLayout.cs` (`ShapedFontCache`/`ShapedTextMetrics`/`ShapedTextWrapper`/`ShapedStyledTextWrapper`) replaced `CrossPlatformTextLayout.cs`'s approximate classes (deleted; `StyledLineFragment` is the only piece still needed and was kept). `PDFWriter.DrawWrappedText`/`DrawWrappedRichText` now call these instead.

What changed in practice: word-wrap breaks only at real Unicode soft-break opportunities (after whitespace or a hyphen, per `UnicodeLineBreakAnalyzer`) using each run's actual shaped glyph widths (`HarfBuzzTextShaper`, via whatever font `SkiaCachedFont`/`SKTypeface.FromFamilyName` resolves for that family on the host) rather than a fixed per-character-class table. An unbreakable run of characters wider than the box is allowed to overflow rather than force a mid-word split - same fallback the approximate wrapper had, since this codebase still has no hyphenation logic.

**What did NOT change (at the time):** actual glyph drawing - `PDFWriter` still resolved the requested family to a base-14 font and emitted plain-text `Tj`. See "Real font embedding for the cross-platform path" below - that gap is now closed for the common case.

Verified via `ShapedTextLayoutTests.cs` (13 tests: per-character width/break-opportunity arrays, wrap-preserves-all-characters invariants across run boundaries, no-mid-word-split behavior, explicit-newline forced breaks, same-style fragment merging) plus the existing `CrossPlatformPdfTextTests.cs` (unchanged - still passes, since it calls `PDFWriter.DrawWrappedText`/`DrawWrappedRichText` directly and asserts on emitted operators, which this change doesn't alter the shape of). Full suite: 205 tests (137 + 53 + 15), 0 new warnings.

## Real font embedding for the cross-platform path (done 2026-07-26, same day)

**Why this reverses an earlier decision:** base-14 fonts only support `/Encoding /WinAnsiEncoding` (cp1252, Western European) - no Hebrew/Arabic/Cyrillic/Greek/CJK code points at all. An attempt to add bidi/RTL reordering on top of the base-14 path (to extend `DrawWrappedText`/`DrawWrappedRichText` further, per the original plan for this increment) hit that wall immediately: reordering RTL runs correctly doesn't help when the glyphs themselves can't be drawn on that path regardless of order. Real non-Latin script support requires real font embedding - so the user chose to take on that increment now rather than stop at the wall.

**What was built:** `PDFWriter` can now draw through a real, embedded, SkiaSharp-backed Type0/CIDFontType2 composite font instead of always falling back to base-14/WinAnsi:
- `PDFFont.SkiaTypeface` (nullable `SKTypeface`) marks a font as this new cross-platform composite kind - `CachedFont` stays null, so `WriteFont`/`WriteEmbeddedFont`/`ProcessFontForFontEmbedding` branch around their Win32-HDC code for it (`WriteSkiaCompositeFont`/`WriteSkiaEmbeddedFont`, both in `PDFWriter.cs`).
- `PDFWriter.GetOrCreateEmbeddedFont` resolves (family, bold, italic) to one of these fonts, cached per report the same way `GetOrCreateBase14Font` is.
- `PDFWriter.WriteCompositeText` re-shapes each wrapped line/fragment via `UnicodeParagraphShaper` (the same P4 pipeline `ShapedTextMetrics` already uses for wrap decisions) to get real font glyph ids, registers each unique glyph's width (in the CID font's standard 1000-units-per-em space) via `PDFFont.AddUniqueGlyph`, and writes `<hex glyph codes> Tj` - `CIDToGIDMap /Identity`, since HarfBuzz's glyph ids already are this `SKTypeface`'s real glyph indices, no indirection needed.
- The embedded font file itself comes straight from `SKTypeface.OpenStream`/`SKData` (no HDC, no `FontPackage.Generate`) via the existing `WriteFontBuffer` helper.
- **Gating:** this path is used whenever `EmbedFonts == FontEmbedding.Subset` (already the default in `PDFRenderer`, both on Windows and cross-platform) - `FontEmbedding.None` still gets the exact old base-14/WinAnsi behavior. Existing tests that construct `PDFWriter` directly (not through `PDFRenderer`) are unaffected: `EmbedFonts` defaults to `None` on a bare `PDFWriter`, so nothing changes for them unless a test opts in.

**Known gaps, honestly scoped:**
- ~~No glyph subsetting on this path~~ **Resolved (2026-07-26):** see "Glyph subsetting for the cross-platform composite path" below.
- ~~No embedding-rights check~~ **Resolved (2026-07-26):** see "Embedding-rights check for the cross-platform composite path" below.
- ~~No bidi/RTL reordering~~ **Resolved (2026-07-26):** see "Bidi/RTL run reordering for the cross-platform composite path" below - `UnicodeParagraphShaper.Shape` now returns runs in visual order via `BidiRunReorderer`, and `WriteCompositeText` draws them as received.
- **Approximate FontDescriptor metrics** - ascent/descent/bbox/italic angle come from `SKFontMetrics`/`SKTypeface.FontStyle`, not real font-table parsing (no OS/2 table read for `fsType`/weight class, no post-table italic angle) - the same class of approximation the Win32 composite path's own `OUTLINETEXTMETRIC`-derived values already are.
- **The real RichText/Uniscribe production pipeline is still completely untouched** - this is still PDFWriter's own parallel path, not `FontCache`/`TextRun`/`LineBreaker`/`Paragraph`.

Verified: `EmbeddedFontPdfTextTests.cs` (5 tests: composite hex Tj vs. base-14 literal Tj, Type0/CIDFontType2/Identity-H/CIDToGIDMap dictionary shape, `/FontFile2` presence, rich-text fragment coverage, and that `EmbedFonts == FontEmbedding.None` is unaffected) plus a manual end-to-end render (forced the cross-platform branch locally the same way prior increments were verified, rendered `SimpleTextboxReport.rdlc`, confirmed a real ~1MB Arial font embedded via `/FontFile2`/`/Length1`, non-trivial/varied glyph ids in the `Tj` hex strings including a consistent space-glyph id, reverted). Full suite: 210 tests (58 + 137 + 15), 0 new warnings.

## Bidi/RTL run reordering for the cross-platform composite path (done 2026-07-26, same day)

**Why now:** user asked what the original (Windows-only) product supported for Hebrew/Arabic/Cyrillic/Greek/CJK in PDF export, since they'd only ever used en-US with this library. Research (web + a codebase-history spike into the original Uniscribe path) found the original `TextLine.ScriptLayout` call did real bidi/RTL visual reordering unconditionally, for every render - a capability the new cross-platform composite-font path (above) explicitly lacked. Of the five scripts, only Hebrew/Arabic need reordering to render correctly at all (Cyrillic/Greek/CJK are LTR); this was prioritized as the correctness gap, ahead of glyph subsetting/embedding-rights (efficiency/compliance, not correctness).

**What was built:** `BidiRunReorderer` (`Microsoft.ReportingServices.Rendering.RichText/BidiRunReorderer.cs`) - a generic, standalone run-level reordering pass implementing the run-granularity form of the Unicode bidi algorithm's L2 rule (UAX #9), scoped to the two-level case `UnicodeTextItemizer` actually produces (each run is either the paragraph's base direction or the opposite one - no directional-formatting-character nesting is itemized). Base paragraph direction is taken from the first item's direction (mirrors UAX #9 P2/P3's "first strong character" heuristic - there's no textbox/report-level direction override wired into itemization today). `UnicodeParagraphShaper.Shape` now calls it on its result list before returning, so its output contract changed from "runs in logical/storage order" to "runs in visual (left-to-right draw) order." `WriteCompositeText` (`PDFWriter.cs`) needed no change beyond a doc-comment update - it already just draws `Shape`'s returned list in order.

**Why no glyph-level reversal was needed:** each run's own glyphs are already in visual order courtesy of HarfBuzz/SKShaper - HarfBuzz's documented contract is that shaped output is always in left-to-right paint order regardless of the run's script direction (it reverses RTL clusters internally during shaping). Verified this holds for this codebase's usage by reasoning through `HarfBuzzTextShaper.Shape`'s call to `SKShaper.Shape` (auto-detects direction per call, one call per itemized run) - confirmed adequate by the fact that `ShapedTextMetrics.Measure` (word-wrap/decoration sizing) already worked correctly for RTL text before this change, since it re-indexes by absolute `CharPos` rather than by list order. So the only real gap was run *order*, not glyph order within a run.

**Known gaps, honestly scoped:**
- **Only two direction levels are modeled** (matches what `UnicodeTextItemizer` itemizes) - genuinely nested bidi (an LTR word inside an RTL phrase inside an LTR paragraph, requiring 3+ levels) is not handled; two-level cases (a lone RTL/LTR island, or several same-direction islands in a row) are.
- **No explicit directional-formatting-character support** (LRE/RLE/PDF/LRI/RLI/etc. control characters) - base direction is inferred from the first strong character only, same simplification the itemizer already made for script detection.
- **`UnicodeTextItemizer`'s `Other` bucket** (Thai/Devanagari/Hangul/etc.) is still treated as LTR (a pre-existing, separate gap - not attempted here, since none of those scripts are RTL).
- **Digits/numbers within an RTL run** are not given special "European/Arabic number" contextual handling (UAX #9's N-rules) - they stay in the RTL run's reading-order position, which is usually but not always what a mixed Hebrew/Arabic-plus-numbers line should visually show.

Verified: `BidiRunReordererTests.cs` (7 tests exercising the algorithm directly via plain `(label, isRtl)` tuples - single-run no-ops, an all-LTR paragraph, a lone RTL island keeping its position, two adjacent RTL runs reversing relative to each other, and an RTL-base paragraph with a trailing LTR word reversing the whole sequence) plus three new cases added to `UnicodeParagraphShaperTests.cs` (pure-Hebrew text marked RTL, a lone embedded Hebrew word keeping its logical slot, and an RTL-base "שלום world" string where the returned list's Latin run now precedes the Hebrew run). Full suite: 68 tests in this test project, 0 failures.

## Embedding-rights check for the cross-platform composite path (done 2026-07-26)

**Why now:** with bidi/RTL reordering done, the remaining PDF gaps were glyph subsetting (efficiency) and this embedding-rights check (compliance) - the user picked the smaller, bounded one first.

**What was built:** `SkiaFontEmbeddingRights` (`Microsoft.ReportingServices.Rendering.ImageRenderer/SkiaFontEmbeddingRights.cs`) reads the OS/2 table's `fsType` field directly off an `SKTypeface` via `SKTypeface.GetTableData(uint tag)` - confirmed against a real Arial font (`fsType` at byte offset 8-9 of the OS/2 table, big-endian, matching `FontPackage`'s existing Win32 OS/2-table-offset constants) that this needs no HFONT/HDC at all, closing the exact gap the prior entry above described as "possible later." `ProcessFontForFontEmbedding` (`PDFWriter.cs`) now has a `pdfFont.SkiaTypeface != null` branch mirroring the existing Win32 `FontPackage.CheckEmbeddingRights` branch - when a font's `fsType` sets the "Restricted License embedding" bit (`0x0002`, the only bit that forbids embedding outright per the OpenType spec - Preview&Print/Editable/no-subsetting/bitmap-only are usage restrictions for the consuming viewer, not embedding prohibitions), `pdfFont.EmbeddedFont` is left `null` the same way the Win32 path already leaves it null for a non-embeddable font - every call site already checks `EmbeddedFont != null` before referencing it, so no other code needed to change.

**Known gaps, honestly scoped:**
- **A missing/malformed OS/2 table is treated as unrestricted** (some non-TrueType/OpenType fonts have none) - matches the common case (virtually all real fonts on Windows/Linux ship one) but isn't a byte-for-byte port of `TTGetEmbeddingType`'s own fallback behavior for that edge case.
- **No end-to-end "unembeddable font" integration test** - there's no restricted-license font file vendored in this repo to exercise the negative path through a real `PDFWriter` render (same "no font file vendored, no reproducible fixture" constraint noted elsewhere in this doc). Covered instead by direct unit tests of the bit-check logic (`SkiaFontEmbeddingRightsTests.cs`) plus an integration test that reads a real resolved `SKTypeface`'s actual `fsType` and asserts `CanEmbed` agrees with it.

Verified: `SkiaFontEmbeddingRightsTests.cs` (7 tests: the fsType bit-check in isolation for Installable/Restricted/Preview&Print/Editable/combined-bits, a null-typeface case, and a real-typeface integration case). Full suite: 75 tests in this test project (68 + 7), `ReportViewerCore.LinuxRenderers.Tests` unaffected (15/15), 0 regressions.

## Glyph subsetting for the cross-platform composite path (done 2026-07-26)

**Why now:** the last of the two smaller PDF gaps identified after bidi/RTL reordering (the other, the embedding-rights check, was done first since it was even smaller/more bounded). This closes the file-size/efficiency gap; only the much larger real RichText/Uniscribe pipeline port remains.

**What was built:** `TrueTypeGlyphSubsetter` (`Microsoft.ReportingServices.Rendering.ImageRenderer/TrueTypeGlyphSubsetter.cs`) parses a font's `glyf`/`loca`/`head`/`maxp` sfnt tables directly from the raw bytes `SKTypeface.OpenStream` already returns, then rewrites `glyf`/`loca` to zero out the outline data of every glyph id not actually used in the document (`EmbeddedFont.GetGlyphIdArray()` — aggregated across every `PDFFont` sharing that embedded font object). Composite glyphs (accented Latin letters etc.) have their component glyph ids resolved and kept automatically by walking the composite glyph record chain, so a kept glyph's dependencies are never dropped. Critically, **glyph ids are never renumbered** — every glyph keeps its original id/slot, so nothing else already emitted (the glyph-indexed `Tj` hex strings, `CIDToGIDMap /Identity`, the `/W` widths array built from `hmtx`) needs to change. This is a smaller, lower-risk win than a full font subsetter (which would renumber glyph ids into a dense range and requires rewriting every glyph-id-dependent table), while still capturing most of the size reduction for the common case (a report using a handful of glyphs out of a large multi-script font's thousands). Every other table (`hmtx`, `cmap`, `name`, `post`, `OS/2`, etc.) is left completely untouched. The whole-file checksum (`head.checkSumAdjustment`) and each table's own checksum are recomputed per the OpenType spec so the rewritten font stays a well-formed, spec-valid sfnt binary. Wired into `WriteSkiaEmbeddedFont` (`PDFWriter.cs`): calls `TrueTypeGlyphSubsetter.TrySubset` before `WriteFontBuffer`, using the subsetted bytes when it succeeds.

**Why this is the same class of technique, scoped down:** a full glyph-id-renumbering subsetter (what the Win32 path's `FontPackage.Generate`/Windows Font Subsetter API does) is real, non-trivial binary-format work across every glyph-id-indexed table. This increment gets most of the practical benefit (smaller `glyf`, the table that's disproportionately large in a full-Unicode-coverage font) without that renumbering complexity or its correctness risk.

**Known gaps, honestly scoped:**
- ~~Only TrueType-outline (`glyf`/`loca`) fonts are subsetted; CFF-flavored OpenType fonts fall back to whole-file embedding~~ **Resolved (2026-07-27):** see "CFF glyph subsetting + correct CIDFontType0/FontFile3 declaration" below.
- **Only `glyf` shrinks - `hmtx`/`cmap`/`name`/`post`/etc. are untouched**, so the size win is smaller than a full renumbering subset would achieve (those tables are usually much smaller than `glyf` for a large multi-script font, so this is a minor gap in practice).
- **Any parse surprise falls back to whole-file embedding** (`TrySubset` catches all exceptions) rather than risk emitting a corrupt embedded font - matches this repo's "don't force an abstraction whose semantics can't be verified end-to-end" convention.
- **TrueType Collection (`'ttcf'`) fonts remain out of scope** for both the TrueType and CFF subsetters (would need member-font selection/remapping) - still fall back to whole-file embedding unchanged.

Verified: `TrueTypeGlyphSubsetterTests.cs` (8 tests: a real font shrinks when only a few glyphs are kept, the result reloads as a valid `SKTypeface` with an unchanged glyph count, advance widths for *every* glyph id - kept or not - are unchanged (since `hmtx` isn't touched), a composite glyph's (`É`) outline survives subsetting when only its own glyph id is passed as "used" (proving component-glyph discovery works), plus `OTTO`/`ttcf`/too-short-data/missing-required-tables all safely decline without throwing). Full suite: 83 tests in this test project (75 + 8), `ReportViewerCore.LinuxRenderers.Tests` unaffected (15/15), 0 regressions.

## CFF glyph subsetting + correct CIDFontType0/FontFile3 declaration (done 2026-07-27)

**Why now:** the last remaining honestly-scoped gap in the font-embedding work above — CFF-flavored ('OTTO') fonts fell back to whole-file embedding, unlike TrueType's already-subsetted path.

**A pre-existing correctness bug found along the way, not just a size/efficiency gap:** `PDFWriter.WriteSkiaCompositeFont` unconditionally declared every cross-platform embedded composite font as `/Subtype /CIDFontType2` with `/FontFile2`, regardless of the font's real outline format. Per PDF spec 9.7.4/9.9, `CIDFontType2`/`FontFile2` is valid only for TrueType (`glyf`-outline) fonts; a CFF-flavored font must be declared `CIDFontType0` with `FontFile3` (`/Subtype /OpenType`, since the embedded bytes are the whole OTF file, not a bare CFF table) — and `CIDToGIDMap` doesn't apply to `CIDFontType0` at all (a non-CID-keyed CFF program uses CIDs directly as GIDs per spec 9.7.4.2). This was latent regardless of subsetting: any report using a CFF-flavored font (common on Linux, and not rare on Windows either — many commercial/OTF fonts are CFF-flavored) was emitting a spec-invalid font dictionary. Fixed alongside the subsetter, since correctly detecting CFF-vs-TrueType was needed for both.

**What was built:**
- `SfntBinaryUtils` (`Microsoft.ReportingServices.Rendering.ImageRenderer/SfntBinaryUtils.cs`) — low-level sfnt helpers shared by both subsetters: `DetectOutlineFormat` (peeks the version tag: `'OTTO'` → CFF, `0x00010000`/`'true'` → TrueType, anything else including `'ttcf'` → Unsupported), `TryReadTableDirectory`, and `RebuildFont`/checksum/byte helpers previously private to `TrueTypeGlyphSubsetter` (pure refactor extracting shared plumbing — `TrueTypeGlyphSubsetter`'s own subsetting logic and test behavior are unchanged).
- `CffGlyphSubsetter` (`Microsoft.ReportingServices.Rendering.ImageRenderer/CffGlyphSubsetter.cs`) — same "zero the unused glyph data, never renumber" technique as `TrueTypeGlyphSubsetter`, applied to a CFF font's CharStrings INDEX (which has the same offset-array-plus-data shape as `loca`+`glyf`). The real complication CFF has that TrueType doesn't: CharStrings isn't the last structure in the file — Private DICT (+ Local Subrs) normally follows it — so shrinking CharStrings requires patching Private/charset/Encoding's absolute-offset operands in the Top DICT by the size delta. Done via a minimal DICT tokenizer that locates those operands' exact byte positions/encoding widths, patching in place (reusing the original 3-byte/5-byte integer encoding form) rather than re-serializing the whole Top DICT — avoids any cascading re-layout of everything before CharStrings.
- `PDFWriter.WriteSkiaEmbeddedFont` now detects the font's real outline format and routes to whichever subsetter matches (`TrueTypeGlyphSubsetter` for TrueType, `CffGlyphSubsetter` for CFF); `WriteSkiaCompositeFont`/`WriteFontBuffer` now declare `CIDFontType0`/`FontFile3`+`/Subtype /OpenType` for CFF fonts and keep the existing `CIDFontType2`/`FontFile2`/`CIDToGIDMap /Identity` declaration for TrueType, decided independently of whether subsetting actually happened (a CFF font that fell back to whole-file embedding still needs the CFF-shaped declaration).

**Known gaps, honestly scoped:**
- **Only non-CID-keyed CFF is supported** — CID-keyed CFF (`ROS`/`FDArray`/`FDSelect` operators present) has a materially different structure (per-glyph FDSelect, multiple Private DICTs) this class doesn't attempt; falls back to whole-file embedding, same as before this increment.
- **No seac-style composite-glyph dependency resolution** — Type2 charstrings have a deprecated `endchar`-with-4-args accented-character mechanism referencing two other glyphs by StandardEncoding code (unlike TrueType's glyf composites, which name component glyph ids directly); not tracked here. Modern font tooling doesn't emit it (accented glyphs are precomposed outlines), so this is a low-probability gap, not a silently-dropped common case.
- **Charset/Encoding/Private offset operands are only patched when encoded in the 3-byte or 5-byte DICT integer form** — the short 1-2 byte forms only encode small values a real file offset is never seen using in practice; if one is, this bails rather than risk misencoding it.
- ~~TrueType Collection (`'ttcf'`) fonts remain out of scope for both subsetters~~ **Resolved (2026-07-27):** see "TrueType Collection ('ttcf') face extraction" below.
- **No real CFF/OTF font is vendored in this repo, and none was found installed on the dev box that built this increment** (all `C:\Windows\Fonts` entries are `.ttf`) — same "no reproducible fixture" constraint noted elsewhere in this project (e.g. the embedding-rights check's negative-path gap). `CffGlyphSubsetterTests.cs` hand-builds a minimal, spec-valid synthetic CFF font (3 glyphs, Private DICT deliberately placed after CharStrings so the offset-patching path is actually exercised) rather than skipping real-format verification entirely.

Verified: `CffGlyphSubsetterTests.cs` (6 tests: dropping a glyph shrinks the CharStrings INDEX and correctly patches the Private DICT's offset, the result's sfnt table directory still parses and is detected as CFF, keeping all glyphs leaves the CharStrings INDEX byte-identical in size, a non-OTTO font/too-short data/CID-keyed CFF all safely decline without throwing). `TrueTypeGlyphSubsetterTests.cs` unchanged and still passing (confirms the `SfntBinaryUtils` extraction was behavior-preserving). Full suite: `Microsoft.ReportViewer.Chart.Rdl.Tests` 112/112, `ReportViewerCore.LinuxRenderers.Tests` 15/15, 0 regressions, full-solution `dotnet build` 0 errors.

## TrueType Collection ('ttcf') face extraction (done 2026-07-27)

**Why now:** the last remaining honestly-scoped gap in the glyph-subsetting work above. TrueType Collection files bundle multiple font faces (e.g. distinct weights, or a text face plus a companion math/symbol face) sharing common tables, in one physical file — common for CJK system fonts (`simsun.ttc`, `msyh.ttc`, `mingliub.ttc`) and for some Latin-script families too (`cambria.ttc` bundles Cambria + Cambria Math).

**A pre-existing correctness bug found along the way, same class as the CIDFontType0/FontFile3 bug the CFF increment found:** `SfntBinaryUtils.DetectOutlineFormat` correctly returns `Unsupported` for a raw `'ttcf'` blob (it isn't itself a single-format font program) — but `PDFWriter.WriteSkiaCompositeFont`'s `isCffOutline` check treated `Unsupported` the same as "not CFF," so a TTC-backed font fell through to declaring `CIDFontType2`/`FontFile2` and then `WriteSkiaEmbeddedFont` wrote the *entire multi-face container* into that `FontFile2` stream unchanged. Per PDF spec 9.9, `FontFile2` must hold a single TrueType (`glyf`-outline) font program, not a multi-face collection — any report resolving to a `.ttc`-backed family (common for CJK reports on both Windows and Linux) was emitting a spec-invalid, and needlessly enormous (the whole `.ttc`, often 10-20MB), embedded font stream.

**What was built:**
- `SfntBinaryUtils.IsTtc`/`TryExtractTtcFace` (`SfntBinaryUtils.cs`) - detects the `'ttcf'` version tag and extracts one face (by index) into a standalone, single-face sfnt binary. A TTC face's own per-face table directory already uses the identical 12-byte-header-plus-table-records shape as an ordinary standalone sfnt (per spec), and every table record's offset is absolute into the whole container regardless of which face it's attached to (tables can be shared between faces, e.g. `glyf` across weights of one family) - so extraction is exactly `RebuildFontFromDirectory`'s existing table-copy-and-recompute-checksums logic, just started from that face's directory offset instead of 0. `RebuildFont` (the sole method both `TrueTypeGlyphSubsetter`/`CffGlyphSubsetter` call) was generalized into a thin wrapper over a new `RebuildFontFromDirectory(original, directoryOffset, numTables, replacements)` — a pure refactor with `directoryOffset: 0` for the existing callers, verified behavior-preserving by both subsetters' existing tests still passing unchanged.
- `PDFWriter.TryGetStandaloneFontBytes(SKTypeface, out byte[], out SfntOutlineFormat)` - the one seam both `WriteSkiaEmbeddedFont` and `WriteSkiaCompositeFont`'s `DetectSkiaTypefaceOutlineFormat` now go through: reads the typeface's raw bytes plus SkiaSharp's own resolved `ttcIndex` (`SKTypeface.OpenStream(out int ttcIndex)` — the face index SkiaSharp itself picked when it matched the family/style request that produced this typeface), and calls `TryExtractTtcFace` with that index when the raw bytes are a TTC. Everything downstream (`TrueTypeGlyphSubsetter`/`CffGlyphSubsetter`/the FontFile2-vs-FontFile3 declaration) then operates on the extracted single-face bytes exactly as it already did for a plain `.ttf`/`.otf` font - no changes needed to either subsetter.
- On extraction failure (malformed container - not expected for any real font, but the same defensive posture as everywhere else in this pipeline), `WriteSkiaEmbeddedFont` throws into its existing catch block, which nulls `EmbeddedFont` the same way an embedding-rights refusal or a corrupt-font parse failure already does, rather than risk writing an invalid `FontFile2` stream. `DetectSkiaTypefaceOutlineFormat` (used even when `EmbeddedFont` is null, to describe the real typeface for readers that substitute by name) falls back to `TrueType` on extraction failure - the far more common outline format, and the same default the pre-fix code effectively had for the `Unsupported` case.

**Known gaps, honestly scoped:**
- **Assumes the extracted face's own outline format is TrueType or CFF** (the normal cases) - a TTC bundling a CFF-flavored face is spec-legal but rarer; `CffGlyphSubsetter`'s existing non-CID-keyed-only scope applies unchanged to an extracted CFF face.
- **No test exercises a genuinely corrupt/truncated real TTC's extraction failure path end-to-end through `PDFWriter`** - covered instead by `SfntTtcExtractionTests.cs`'s direct malformed-input cases (out-of-range face index, non-TTC data, truncated offset table) at the `SfntBinaryUtils` level.

Verified with **real** `.ttc` fixtures (unlike the CFF increment, which had none available — `cambria.ttc`/`simsun.ttc`/`msyh.ttc`/`mingliub.ttc`/`msgothic.ttc` are all installed on this dev box): `SfntTtcExtractionTests.cs` (9 tests - extracting Cambria's own face vs. its bundled Cambria Math face by their real, distinct, SkiaSharp-resolved `ttcIndex` values produces two genuinely different reloadable faces; an extracted face is itself subsettable by the unmodified `TrueTypeGlyphSubsetter`; malformed-input cases). `TrueTypeCollectionPdfTextTests.cs` (3 tests, same harness as `EmbeddedFontPdfTextTests.cs` but with `FontFamily = "SimSun"`): confirms the fixed `CIDFontType2`/`FontFile2`/`CIDToGIDMap /Identity` declaration, hex-glyph `Tj` drawing, and - the concrete proof the bug is fixed - that the embedded `/Length1` byte count is both smaller than the whole `~18MB` `simsun.ttc` container and less than half the size of the extracted-but-unsubsetted single face, confirming real end-to-end extraction-then-subsetting rather than a fallback to whole-container (or whole-face) embedding. Full suite: `Microsoft.ReportViewer.Chart.Rdl.Tests` 125/125 (112 + 13), `ReportViewerCore.LinuxRenderers.Tests` 15/15, 0 regressions, full-solution `dotnet build` 0 errors.

## Production wiring, increment 1: itemize/break/shape/measure (done 2026-07-26)

**What was built:** the four real production classes named in "What real production wiring still needs beyond these prototypes" (below) now branch on `!OperatingSystem.IsWindows()` to use the P4 prototypes instead of Win32/GDI+, each exposing its cross-platform body as its own `internal` method (not just an inline branch) so it can be called directly in tests regardless of host OS — the same seam this doc's MVP section already used for `PDFWriter.DrawWrappedText`/`DrawWrappedRichText`:

- `FontCache.CreateFont` (private) branches to construct a `SkiaCachedFont` instead of a GDI+ `Font` + Win32 `HFONT` — this is the literal blocker TODO.md called out ("constructs live GDI+ Font objects unconditionally for every text run, in every script"). A new `FontCache.GetFontCrossPlatform(ITextRunProps)` does the same resolve-and-cache dance as `GetFont` but always via the Skia path (unconditionally, not gated) — `TextRun.ShapeAndPlaceCrossPlatform` calls this directly rather than through the platform-gated `GetFont`/`CreateFont`, so it's exercisable in tests on Windows without relying on the runtime OS check.
- `CachedFont` gained a nullable `SkiaFont` slot alongside its existing `Font`/`Hfont` fields; `GetHeight`/`GetAscent`/`GetDescent`/`GetLeading` each check it first and return `SkiaCachedFont`'s metrics directly (no HDC, no `GetTextMetrics`) when set.
- `TextRun.ShapeAndPlaceCrossPlatform` shapes via `HarfBuzzTextShaper.Shape` (using the font resolved above) instead of Win32 `ScriptShape`/`ScriptPlace`. Deliberately skips `GetCharset()`/`IsComplex` entirely — both route through `ScriptProperties`, whose static constructor P/Invokes `Win32.ScriptGetProperties` and would crash on first touch on a non-Windows host; charset is irrelevant to Skia's family/style-based typeface resolution anyway. No fallback-font-on-missing-glyph retry (matches `HarfBuzzTextShaper`'s own documented scope). Explicitly sets `GlyphData.NeedGlyphPlaceData = false` after shaping — without this, a later `TextRun.LoadGlyphData` call (e.g. from a second `GetWidth`/`GetHeight` call on the same run) would try to re-invoke Win32 `ScriptPlace` via `TextScriptPlace`, since `GlyphData`'s `GlyphShapeData`-taking constructor sets that flag `true` unconditionally regardless of platform.
- `Paragraph.ScriptItemizeCrossPlatform` itemizes via `UnicodeTextItemizer.Itemize` and computes line-break attributes once for the whole paragraph via `UnicodeLineBreakAnalyzer.Analyze`, instead of Win32 `ScriptItemize`/(per-item) `ScriptBreak`. `ExtractRuns`/`AnalyzeForBreakPositions` gained a precomputed-`SCRIPT_LOGATTR[]` code path (`AssignBreakPositions`) that slices the paragraph-wide array by absolute character position instead of calling `Win32.ScriptBreak` on each item's text — mirrors the exact pattern `UnicodeParagraphShaper.Shape` already used for the PDF composite-font path. Does not reproduce `CanMergeItemizedRuns`' Windows-parity merge heuristic — relies on `UnicodeTextItemizer` already merging "Common"-script characters into neighboring runs itself, which is a different (simpler, coarser) merge policy.
- `TextLine.ScriptLayoutCrossPlatform` reorders `m_logicalRuns` into `m_visualRuns` via `BidiRunReorderer.ReorderToVisualOrder` (the same run-level UAX #9 L2 algorithm already used by the PDF composite-font path's `WriteCompositeText`) instead of Win32 `ScriptLayout`. The shared underline-height computation tail was extracted into its own `internal ApplyUnderlineHeights` method so both branches (and tests) can reuse it.

**Why each cross-platform method is its own directly-callable `internal` method, not just an inline branch:** this dev/test box is Windows, so calling the public gated entry points (`TextRun.ShapeAndPlace`, `Paragraph.ScriptItemize`, `TextLine.ScriptLayout`) always takes the real Win32 branch regardless of what's being tested — there is no way to make `OperatingSystem.IsWindows()` return `false` from a unit test. Exposing `ShapeAndPlaceCrossPlatform`/`ScriptItemizeCrossPlatform`/`ScriptLayoutCrossPlatform`/`GetFontCrossPlatform`/`ApplyUnderlineHeights` as `internal` lets tests call the cross-platform body directly and exercise real behavior on any host OS — the same convention `CrossPlatformPdfTextTests.cs` already established for `PDFWriter`'s platform-gated methods.

**Verified:** `RichTextCrossPlatformTests.cs` (10 tests: real `SkiaCachedFont`/no-GDI+-`Font` after shaping, positive width/height/ascent/descent queryable with a zero HDC, `CachedFont` reuse across identically-styled runs, per-character logical widths summing to the run's total shaped width plus the empty-text edge case, single-vs-multi-item itemization with correct break positions, RTL/LTR item flagging and full character-count preservation across a mixed Hebrew+Latin paragraph, LTR-only run order preserved, an RTL-base paragraph with a trailing LTR run reversing the same way `BidiRunReordererTests` already proved the algorithm does, and underline-height computed from real font metrics with no HDC). Full suite: `Microsoft.ReportViewer.Chart.Rdl.Tests` 93/93 (83 + 10), `ReportViewerCore.LinuxRenderers.Tests` 15/15, 0 regressions.

**Known gaps, honestly scoped — this does NOT yet make any real render path work end-to-end on Linux:**
- ~~Not wired to any production call site~~ **Resolved (2026-07-26, same day):** see "Option B: real TextBox.RenderParagraph pipeline wired into PDF" below — `Renderer.ProcessSimpleTextBox`/`ProcessRichTextBox` now route to this real pipeline for PDF specifically, drawing through `PDFWriter.DrawTextRun`'s existing glyph-level content-stream writer (not `SkiaTextRunRasterizer`, which turned out unnecessary for PDF — see that section).
- ~~`TextRun.GetLogicalWidths` is still Win32-only, unconditionally~~ **Resolved (2026-07-26, same day):** `GetLogicalWidths` now branches to a new `GetLogicalWidthsCrossPlatform(GlyphData)`, which converts `HarfBuzzTextShaper`'s per-glyph advances back into per-character widths using `GlyphShapeData.Clusters` (character index → owning glyph index): a 1-char-per-glyph cluster (the common case) gets that glyph's exact advance; a multi-character cluster (ligature or combining-mark sequence) splits the glyph's advance evenly across its characters, with the integer-division remainder added to the cluster's last character - an approximation, not a byte-for-byte port of Uniscribe's own splitting rule, but one that preserves the invariant callers actually need (summing the array over any prefix equals that prefix's real shaped width, verified by `GetLogicalWidthsCrossPlatform_PlainLatinText_SumsToTotalRunWidth`). This closes `LineBreaker`'s character-level line-fold path (`FindWidthToBreakPosition`/`FindFoldTextPosition_CharacterTrim`).
- ~~No actual glyph drawing~~ **Resolved for the drawing *primitive* (2026-07-26, same day):** see "Cross-platform glyph rasterizer" below - `TextBox.Render`'s full paragraph/line orchestration (indent/alignment, underline/strikethrough rectangles, highlighted-run color inversion, prefix/bullet drawing) is still not ported, and `RichTextRenderer.cs`'s hit-testing (`ScriptXtoCP`/`ScriptCPtoX`) remains untouched.
- **No vertical-font, `UseEmSquare`, or fallback-font support** on the Skia branch - `FontCache.GetFontCrossPlatform` always resolves via family/bold/italic only, same documented scope as `HarfBuzzTextShaper`/`SkiaCachedFont` already had.
- **`FontCache.CreateFont`'s own `!OperatingSystem.IsWindows()` branch is currently dead code in production** - `TextRun.ShapeAndPlaceCrossPlatform` calls the new `GetFontCrossPlatform` instead (for the direct-testability reason above), and `CreateFont`'s only other caller, `GetFallbackFont`, is only ever reached from within `TextRun.ShapeAndPlace`'s Windows-only fallback-font logic. Left in place since it's correct and harmless if a future caller reaches it, but it's not exercised by any real path today.

## Cross-platform glyph rasterizer (done 2026-07-26, same day)

**What was built:** `SkiaTextRunRasterizer.Draw(SKCanvas canvas, TextRun run, float x, float baselineY)` (`Microsoft.ReportingServices.Rendering.RichText/SkiaTextRunRasterizer.cs`) - the cross-platform counterpart to `TextBox.DrawTextRun`'s Win32 `ScriptTextOut` call, and the first actual pixel-producing step in this whole cross-platform pipeline (everything before this only measured/laid out; nothing drew).

Draws exactly the glyphs shaping already computed, the same "draw what was measured" invariant `ScriptTextOut` itself preserves (it draws `TextRun.GlyphData`'s glyph array, never the original string) - via `SKTextBlobBuilder.AllocatePositionedRun`, which takes explicit glyph ids and per-glyph pen positions rather than re-shaping `TextRun.Text` through Skia's own string-drawing entry points (`SKCanvas.DrawText(string, ...)`, what `SkiaChartGraphics` uses for Chart's simpler text needs). Per-glyph positions are built by walking `GlyphData.Advances` (already real HarfBuzz-shaped advances) and `GlyphData.GOffsets` (currently always zero per `HarfBuzzTextShaper`'s documented scope, but read anyway so a future non-zero GOFFSET producer needs no rasterizer change) - the same accumulate-pen-position loop `ScriptTextOut`'s own glyph/advance/offset arrays imply. Text color comes from `TextRunProperties.Color` converted straight to `SKColor`.

**Scope, honestly - this is the per-run glyph-drawing primitive only, not a port of `TextBox.Render`/`RenderParagraph`:**
- No paragraph/line orchestration (indent, alignment, prefix/bullet runs, spacing) - callers position each run themselves via `x`/`baselineY`, same as `TextBox.DrawTextRun`'s own parameters.
- No underline/strikethrough rectangles, no highlighted-run color inversion/clipping (`RenderHighlightedTextRun`'s logic) - `TextRun.UnderlineHeight`/`HighlightColor` are computed by the measurement pipeline already wired in but not consumed here.
- Requires the run to already be shaped (`ShapeAndPlaceCrossPlatform` already called, directly or via `GetWidth`/`GetGlyphData`) - throws `InvalidOperationException` otherwise rather than silently shaping on demand, so a caller can't accidentally draw against stale/mismatched measurement.
- **Resolved (2026-07-26, same day): not needed for PDF.** See "Option B: real TextBox.RenderParagraph pipeline wired into PDF" below - `PDFWriter.DrawTextRun` already had its own real, working glyph-level PDF content-stream writer (`WriteText`/`WriteGlyph`, reading straight from `TextRun.GlyphData`), so PDF's real pipeline draws through that, not this rasterizer. This primitive remains useful groundwork for a future `SKCanvas`-based writer (the WinForms viewer/`ImageWriter`, which need real pixels, not PDF operators) - no caller wired yet.

**Verified:** `SkiaTextRunRasterizerTests.cs` (4 tests, behavioral ink-presence checks rather than a golden-image PNG diff - same rationale `ReportViewerCore.LinuxRenderers.Tests`' own `TextRasterAssertions` already documented: `SKTypeface` family resolution isn't guaranteed identical between this Windows dev box and a fontconfig-less Linux CI image, so a pixel-exact baseline wouldn't be reproducible there). Covers: real text produces ink somewhere in the bitmap; each non-whitespace character of a multi-character run produces ink specifically within its own shaped column (computed from `GetLogicalWidthsCrossPlatform`, so this test would have caught the earlier Chart-migration class of bug - a glyph silently drawn blank or off-position without throwing); an empty run draws nothing; drawing an unshaped run throws rather than silently misbehaving. Full suite: `Microsoft.ReportViewer.Chart.Rdl.Tests` 97/97 (93 + 4), `ReportViewerCore.LinuxRenderers.Tests` 15/15, 0 regressions.

## Option B: real TextBox.RenderParagraph pipeline wired into PDF (done 2026-07-26, same day)

**The decision:** given the choice between (A) keeping PDF permanently on the simpler base-14/embedded-font `DrawWrappedText`/`DrawWrappedRichText` MVP bypass, or (B) finishing the wiring so the real Windows-parity `LineBreaker`/`TextBox`/`FontCache`/`TextRun` pipeline (itemize → layout → shape → measure → draw) drives PDF text on non-Windows too, the user chose (B) for higher fidelity — real bidi reordering, real HarfBuzz shaping, and the same code path Windows always used, rather than a permanently-separate simpler PDF-only path.

**First finding: `SkiaTextRunRasterizer` (previous section) turned out not to be needed for PDF.** Investigating what `TextBox.RenderParagraph`'s drawing hook (`ITextBoxProps.DrawTextRun` → `PDFWriter.DrawTextRun`) actually does found it already writes real PDF content-stream `Tj` operators straight from `TextRun.GlyphData` (glyph ids/advances) — `WriteText`/`WriteGlyph`/`MapGlyphToUnicodeChar` never touched a GDI+ `Font` or HDC to begin with. `SkiaTextRunRasterizer` draws to an `SKCanvas`, which PDF's content-stream writer has no use for — it remains useful groundwork for a future `SKCanvas`-based writer (e.g. `ImageWriter`), just not this increment.

**What was actually blocking the real pipeline from running on PDF, found by tracing every Win32/GDI+ call site in the chain:**

1. **`LineBreaker.Flow(Win32DCSafeHandle hdc, ...)` and `TextBox.Render(..., Win32DCSafeHandle hdc, ...)`** unconditionally called `Win32.SelectObject`/`Win32.SetTextAlign`/`Win32.SetBkMode` to save/restore GDI text state around the real HDC they assumed they'd been given. Both now guard those calls with `OperatingSystem.IsWindows()` — safe to call with `Win32DCSafeHandle.Zero` on non-Windows, since there's no real device context to save/restore around.
2. **`TextBox.RenderParagraph`/`RenderHighlightedTextRun`'s highlighted-run background fill** called `Graphics.FromHdc`/`new SolidBrush(...)`/`g.FillRectangle` — GDI+ object construction, which per `GraphicsBase.cs`'s own constructor comment can't happen at all on non-Windows. Now skipped when no real `Graphics` exists (`graphics` stays `null`, `RenderHighlightedTextRun` null-checks before filling) — **documented gap:** a highlighted run's *text* still draws correctly on this path (`DrawTextRun`/`DrawClippedTextRun` calls are unconditional), only the highlight background rectangle itself does not yet on non-Windows.
3. **The substantive fix — `PDFWriter.ProcessDrawStringFont`:** this was the one genuinely Win32-only piece in the whole chain. It read `run.CachedFont.Font` (a live GDI+ `Font`, always `null` on the cross-platform-shaped path since `ShapeAndPlaceCrossPlatform` only ever sets `CachedFont.SkiaFont`) and called `m_commonGraphics.GetHdc()`/`Win32.SelectObject`/`FontPackage.CheckSimulatedFontStyles` to resolve simulated-bold/italic and font metrics — an immediate `NullReferenceException` on the cross-platform path, before even reaching Win32. New `PDFWriter.ProcessDrawStringFontCrossPlatform(TextRun run)` resolves the same (family, bold, italic) style via `GetOrCreateEmbeddedFont`/`GetOrCreateBase14Font` — the *exact same* helpers `DrawWrappedText` already used and already proven correct (font caching, embedding-rights check, glyph subsetting all apply unchanged) — keyed off `run.CachedFont.SkiaFont` instead of a GDI+ `Font`.
4. **`DrawTextRun` needed a real dispatch point**, not an internal `OperatingSystem.IsWindows()` check inside `ProcessDrawStringFont` (that shape would make the cross-platform branch untestable from this Windows dev box — see "Why each cross-platform method is its own directly-callable `internal` method" above, same reasoning applies here). Refactored: the shared body moved to a new `private DrawTextRunCore(..., bool useWindowsFontResolution)`; `DrawTextRun` (the real `WriterBase` override) calls it with `OperatingSystem.IsWindows()`; a new `internal DrawTextRunCrossPlatform(...)` calls it with `useWindowsFontResolution: false` unconditionally, for direct testing.
5. **A capability flag was needed so only PDF opts in.** New `WriterBase.SupportsCrossPlatformRichTextPipeline` (`false` by default, `true` in `PDFWriter`) — `ImageWriter`'s GDI+/EMF output still needs a real HDC end to end and can't use this path yet. `Renderer.ProcessSimpleTextBox`/`ProcessRichTextBox`'s gate changed from `if (!OperatingSystem.IsWindows())` to `if (!OperatingSystem.IsWindows() && !Writer.SupportsCrossPlatformRichTextPipeline)`, so only writers that opt in fall through to the real pipeline; others keep the MVP bypass.
6. **The HDC acquisition itself had to change, not just be skipped.** `Renderer.cs`'s existing Windows-only body called `Writer.GetHdc(m_beginPage, out hdc, out dpiX)`, whose underlying `GraphicsBase.CacheHdc`/`GetHdc` constructs a real `System.Drawing.Bitmap`/`Graphics` (`EnsureGraphics()`) — impossible on non-Windows per `GraphicsBase.cs`'s own constructor comment, so this call can never run cross-platform regardless of what calls it. The non-Windows branch (both `ProcessSimpleTextBox` and `ProcessRichTextBox`) now skips `Writer.GetHdc`/`ReleaseHdc` entirely, reading `dpiX` from `Writer.CommonGraphics.DpiX` (a plain `int` field, no GDI+ involved) and threading `Win32DCSafeHandle.Zero` through `LineBreaker.Flow`/`TextBox.Render` — both made bracket-skip-safe in step 1. `Writer.ClipTextboxRectangle`/`UnClipTextboxRectangle` needed no change — `PDFWriter`'s overrides already only write PDF clip operators, never touching `hdc`.

**Why `LineBreaker.Flow`/`TextBox.Render`'s bracket-skip changes aren't independently unit-tested, unlike everything else in this doc:** every other cross-platform addition in this doc exposed its own directly-callable `internal` method (`ShapeAndPlaceCrossPlatform`, `ScriptItemizeCrossPlatform`, etc.) specifically so a test running on this Windows dev box could bypass the `OperatingSystem.IsWindows()` check and exercise the cross-platform body directly. That pattern doesn't apply to `LineBreaker.Flow`/`TextBox.Render`: their guarded Win32 calls are only a few lines of save/restore bracketing around a large shared loop that already, unconditionally, calls straight into `TextRun.GetWidth`/`GetLogicalWidths`/`ShapeAndPlace` — each of which has its *own* `OperatingSystem.IsWindows()` check baked in. Calling `Flow`/`Render` directly on this Windows box, even with `Win32DCSafeHandle.Zero`, would still resolve every one of those nested checks to the Windows branch (since they check the real OS, not a test double), defeating the purpose. `PDFWriter.DrawTextRun` was different: it had exactly one Win32-only decision point (font resolution), cleanly extractable into its own bypassable method. Given this, the mechanical, low-risk bracket-skip in `Flow`/`Render` is covered by build/full-suite verification only (below), not a dedicated behavioral test — a real gap, but an honest one, not a silent one.

**Verified:** 3 new tests in `CrossPlatformPdfTextTests.cs` calling `DrawTextRunCrossPlatform` directly (bypassing the OS check, same convention as `ShapeAndPlaceCrossPlatform`) — base-14 path emits literal `(Hello) Tj`; `FontEmbedding.Subset` emits composite glyph-hex `<...> Tj` (never the literal-text branch); two runs sharing style reuse the same cached `/F<id>` font object. Full suite: `Microsoft.ReportViewer.Chart.Rdl.Tests` 100/100 (97 + 3), `ReportViewerCore.LinuxRenderers.Tests` 15/15, 0 regressions, `dotnet build --no-incremental` on the whole solution: 0 errors.

**Known gaps, honestly scoped:**
- ~~Highlighted-run background rectangles don't draw on non-Windows~~ **Resolved (2026-07-26, next day):** see "Highlighted-run background fill, closing the first known gap" below.
- ~~Vertical-writing-mode text-box height measurement remains Windows-only~~ **Investigated and re-scoped (2026-07-26, next day):** confirmed out of scope for PDF rendering specifically — see "Vertical-text measurement gap: traced to the pagination phase, not PDF rendering" below.
- **`RichTextRenderer.cs`'s hit-testing** (`ScriptXtoCP`/`ScriptCPtoX`, used by the WinForms viewer for caret/selection placement) remains untouched — unrelated to PDF, not exercised by this wiring.
- **No fallback-font retry, no vertical-font/`UseEmSquare` support** on the Skia font-resolution path — same pre-existing scope limit `FontCache.GetFontCrossPlatform`/`HarfBuzzTextShaper` already had, unchanged by this increment.

## Highlighted-run background fill, closing the first known gap (2026-07-26, next day)

`TextBox.RenderHighlightedTextRun`'s GDI+ `g.FillRectangle` call only ran when a real `Graphics` existed (Windows). The non-Windows branch needed a way to fill a rectangle without GDI+ `Graphics`/`Brush` construction (impossible per `GraphicsBase.cs`) — but `PDFWriter` already had exactly this primitive: `WriterBase.FillRectangle(Color, RectangleF)`, an existing virtual member already used for page/item background fills (`Renderer.cs`'s `FillRectangle(cachedReportColorStyle, bounds)`/`FillRectangle(backgroundColor, pageBounds)`), overridden in `PDFWriter` to emit a plain `re f` content-stream fill — no GDI+ involved at all.

The fix: a new `ITextBoxProps.FillHighlightRectangle(FontCache, Rectangle, Color)` — `rect` is already fully resolved to device pixels (same convention as `DrawTextRun`'s `position`/`layoutRectangle`, i.e. `layoutRectangle.X + x + highlightStart` etc., already computed by the caller). `ReportTextBox` (the one real implementer, shared by `PDFWriter`/`ImageWriter`) converts `rect` to millimeters via `SharedRenderer.ConvertToMillimeters(_, fontCache.Dpi)` — confirmed to match `PDFWriter`'s coordinate convention by tracing `DrawTextRunCore`'s `pointF`/`ConvertToPDFUnits` math and `ReportTextBox.DrawTextRun`'s existing `ActionInfo` position computation, both of which build the identical `ConvertToMillimeters(layoutRectangle.X) + ConvertToMillimeters(x)` value — then forwards to `m_writer.FillRectangle(color, rectMM)`. `TextBox.cs`'s `RenderHighlightedTextRun` now calls this new hook in an `else` branch alongside the existing `if (g != null)` GDI+ path, so Windows/`ImageWriter` behavior is completely unchanged (bit-for-bit the same code as before).

The other 3 `ITextBoxProps` implementers needed the new interface member too: `SPBProcessing.TextBox`/`HPBProcessing.TextBox` (pagination-only stubs — their `DrawTextRun`/`DrawClippedTextRun` were already no-ops) got a matching no-op; `RenderingTextBox` (the WinForms live viewer, Windows-only, always has a real `Graphics` in `RenderHighlightedTextRun` so this hook is unreachable from it) got a `NotSupportedException`, matching this codebase's existing `NotReachable()`-style convention for provably-unreachable interface members.

Verified: new `FillHighlightRectangle_EmitsFilledRectangleOperatorInDeviceRectPosition` test (`CrossPlatformPdfTextTests.cs`) constructs a `ReportTextBox` directly and calls the new method (same "call the internal method directly, bypass the OS-gated caller" convention as `DrawTextRunCrossPlatform`'s tests — `FillHighlightRectangle` itself has no OS check to bypass, since the OS-sensitivity lives entirely in its caller), asserting the emitted `re f` operator. Full suite: `Chart.Rdl.Tests` 105/105 (+1 vs. the WebRequest migration's 105), 0 regressions, solution builds clean.

## Vertical-text measurement gap: traced to the pagination phase, not PDF rendering (2026-07-26, next day)

Investigating `TextBox.MeasureFullHeight`/`LineBreaker.FlowVertical`'s callers (to see whether closing this gap belonged to the PDF-rendering work) found they're called from exactly two places: `SPBProcessing.PageContext.MeasureFullTextBoxHeight` and `HPBProcessing.PageContext.MeasureFullTextBoxHeight` — both in the **pagination** phase (computing page breaks/text-box heights before any renderer draws anything), never from `Renderer.cs`/`PDFWriter` (the render phase this whole Option B effort targets). Both `PageContext` classes construct their own real GDI+ `Bitmap`+`Graphics` unconditionally (`CreateGraphics()`, `Graphics.FromImage(new Bitmap(2, 2))`) purely for measurement, and — more broadly than just vertical text — also use it for plain `MeasureStringGDI`/the `Graphics`-taking `LineBreaker.Flow` overload for *all* text-box height measurement during pagination, not only vertical-writing-mode boxes.

This means: pagination itself (not just vertical-text measurement) still requires a real Windows GDI+ `Graphics` object, entirely independent of and prior to PDF's render phase. That's architecturally out of scope for "PDF cross-platform rendering," the same way Chart/Gauge's GDI+ abstraction never touched report *processing* either — it's a different subsystem (`SPBProcessing`/`HPBProcessing` pagination engines, shared by every renderer per `TODO.md`) that would need its own SkiaSharp-based measurement path (a `SKPaint`/font-metrics equivalent to `Graphics.MeasureString`) to become cross-platform, a separate, larger effort with its own scoping — not a narrow follow-up to Option B. No code changed here; this narrows and confirms the existing "known gap" bullet rather than closing it, and flags that the gap is bigger than "vertical text" alone.

## Original phased plan (superseded for the simple-textbox case by the MVP above; still the path for rich text / full fidelity)

## Why this exists as its own doc

`PDFWriter` itself has no Metafile/GDI+-drawing dependency, but its one remaining hard item — Uniscribe-based text shaping — is not a bounded, RTL/CJK-only concern: it's this engine's *only* text-shaping backend, for every script, and it also constructs live GDI+ `Font` objects upstream of shaping (an independently-blocked-on-Linux wall). Full reasoning and evidence: `docs/decisions.md`'s "Correction: P4 ... understated the real scope" entry (2026-07-26).

## Full P/Invoke call-site inventory (verified 2026-07-26)

| Function | Call site | Purpose |
|---|---|---|
| `ScriptItemize` | `Paragraph.cs:217` (via `LineBreaker.cs:635`) | Splits mixed-direction/mixed-script text into runs (bidi/script segmentation) |
| `ScriptBreak` | `Paragraph.cs:179` (used throughout `LineBreaker.cs`, e.g. `:440,539,572`) | Per-character `SCRIPT_LOGATTR` soft-break/word-break flags — the actual data `LineBreaker` folds lines on |
| `ScriptShape` | `TextRun.cs:375,380,387,398,422,438` | Text → glyph indices + cluster map + visual attributes |
| `ScriptPlace` | `TextRun.cs:459,463,467` | Glyph indices → advance widths (`GOFFSET`/`ABC`) |
| `ScriptGetFontProperties` | `TextRun.cs:409` | Fallback/default-glyph detection for CJK/font fallback |
| `ScriptGetLogicalWidths` | `TextRun.cs:273` | Per-character logical widths for caret/selection |
| `ScriptTextOut` | `TextBox.cs:541,599` | Actual glyph drawing |
| `ScriptXtoCP`/`ScriptCPtoX` | `RichTextRenderer.cs:460,900` | Hit-testing/caret (WinForms viewer only, not PDF) |
| `ScriptFreeCache` | `ScriptCacheSafeHandle.cs:16` | Cache cleanup |
| `ScriptGetProperties` | `ScriptProperties.cs:29` | Script property table (`TextRun.IsComplex`, `TextRun.cs:35,506`) |
| `ScriptLayout` | `TextLine.cs:256` | Visual↔logical reordering for bidi/RTL |
| `CreateFont`/`GetTextMetrics` | `FontCache.cs:221`, `CachedFont.cs:105` | Real Win32 `HFONT` + `TEXTMETRIC`, not GDI+ |
| `new Font(...)` | `FontCache.cs:333,352,372,387,391` | GDI+ font object, independently blocked on Linux (Phase 0 spike) |

## Blast radius (this is not PDF-scoped work)

`LineBreaker.Flow`/`TextBox.Render`/`TextBox.MeasureFullHeight` are called from:
- `Microsoft.ReportingServices.Rendering.ImageRenderer/Renderer.cs:1145,1147,1246,1250` — shared by `PDFWriter` **and** the Windows-only `ImageWriter` (EMF/TIFF) renderer.
- `RichText/RichTextRenderer.cs:1118` and `Microsoft.Reporting.WinForms/RenderingTextBox.cs:385` — the WinForms on-screen viewer's own rendering.
- `Rendering.HPBProcessing/PageContext.cs:434,444` and `Rendering.SPBProcessing/PageContext.cs:548` — the shared pagination/page-break engines used by *every* renderer, not just PDF.

`HtmlRenderer` and `ExcelRenderer` do **not** depend on this pipeline (confirmed via grep — no references to `LineBreaker`/`RichText.TextBox`). Fixing this benefits PDF, Image/EMF, the WinForms viewer, and pagination correctness together; there is no PDF-only shortcut to route around it.

## What a replacement needs to produce

`LineBreaker.Flow(TextBox, Win32DCSafeHandle hdc, float dpiX, FontCache, FlowContext, bool keepLines, out float height) : List<Paragraph>` (line-break offsets/`TextLine`s) and `TextBox.Render(...)`/`MeasureFullHeight(...)` (glyph draw + height) are the two entry points `PDFWriter`/`Renderer.cs` call. Internally they need, per run: glyph indices (`GlyphShapeData.Glyphs`), clusters, `SCRIPT_VISATTR`, advance widths (`GlyphData.Advances`/`ABC`), `GOFFSET` glyph offsets, and `SCRIPT_LOGATTR` break flags — i.e. a shaper producing glyph ids + advances + break opportunities + a bidi-reordered run list, matching the shapes already defined in `TexRunShapeData.cs`/`GlyphShapeData.cs`/`GOFFSET.cs`.

## Recommended phased plan

Given the size (this is new shaping/line-breaking logic, not a resource-type port like Chart/Gauge's migration), do not attempt this as one pass. Status (2026-07-26): steps 1-3 have working prototypes; step 4 (verification harness) is done; production wiring (step 3's real goal) has not started.

1. **Spike — done.** `tests/ReportViewerCore.LinuxRenderers.Tests/TextShapingSpikeTests.cs` proved HarfBuzzSharp can shape plain Latin text into real glyph ids + advances, with no `System.Drawing`/Win32 in the call path.
2. **Font layer — done.** `SkiaCachedFont.cs` mirrors `CachedFont`'s metrics surface (`GetHeight`/`GetAscent`/`GetDescent`/`GetLeading`, `ScaleFactor`) via `SKTypeface`/`SKFont`/`SKFontMetrics`. Scaffolding only — nothing calls it in production yet (5 tests, `SkiaCachedFontTests.cs`).
3. **Shaping layer — prototypes done, production wiring not started.** Three composable prototypes exist, each translating a Uniscribe concept into a HarfBuzz/Unicode-range equivalent:
   - `HarfBuzzTextShaper.cs`: text → `GlyphData`/`GlyphShapeData`/`ABC`/`GOFFSET` (the same shapes `TextRun.ShapeAndPlace` builds from `ScriptShape`/`ScriptPlace`), via `SkiaCachedFont.Shaper` (`SkiaSharp.HarfBuzz.SKShaper` — see `docs/decisions.md`'s `SKShaper` entry for why not a hand-rolled `HarfBuzzSharp.Face`/`Font`). 6 tests (`HarfBuzzTextShaperTests.cs`).
   - `UnicodeTextItemizer.cs`: text → script/direction runs with real `SCRIPT_ANALYSIS` values (via the existing `ScriptAnalysis`/`ScriptState` wrapper classes), using coarse Unicode code-point ranges instead of Uniscribe's script table. 7 tests (`UnicodeTextItemizerTests.cs`).
   - `UnicodeLineBreakAnalyzer.cs`: text → per-character `SCRIPT_LOGATTR` via a hand-rolled soft-break-after-whitespace-or-hyphen heuristic (not UAX #14-compliant). 6 tests (`UnicodeLineBreakAnalyzerTests.cs`).
   - `UnicodeParagraphShaper.cs` composes all three into one per-paragraph pipeline (itemize → shape each item with its slice of line-break attributes). Composing them (not just unit-testing each in isolation) surfaced a real native crash, fixed by switching to `SKShaper` — see `docs/decisions.md`. Full suite after composition: 192 tests, 0 new warnings.

   Honest scope, all four files: plain LTR non-reordering text only (Latin/Cyrillic/Greek/numbers/punctuation). `SCRIPT_VISATTR` is zeroed (unread by this codebase). No fallback-font-on-missing-glyph retry. `UnicodeTextItemizer`'s `Other` bucket (Thai/Devanagari/Hangul/etc.) is treated as LTR, which is wrong for those scripts' real shaping rules.

   **What real production wiring still needs beyond these prototypes:** (a) a platform/backend selection point inside `TextRun.ShapeAndPlace` (or a new `TextRun` subclass/strategy), (b) real Uniscribe script IDs in place of the coarse Unicode-range buckets for non-Latin scripts, (c) UAX #14 line-breaking (or an ICU4N dependency) in place of the heuristic, (d) the fallback-font/east-asian-glyph retry logic `TextRun.ShapeAndPlace` already has for Windows. Recommend scoping each as its own bounded increment — lean on the verification harness below (`ShapedTextRasterizer`/`TextRasterAssertions`) for each, since wrong cluster/line-break/reading-order bugs don't throw.
4. **Verification infrastructure — done.** Deliberately not a golden-image PNG diff (`AGENTS.md`'s Testing Philosophy prefers behavioral tests; `SKTypeface.Default` isn't guaranteed identical across hosts, and no font file is vendored in-repo to pin a reproducible baseline against). Instead, `tests/ReportViewerCore.LinuxRenderers.Tests/ShapedTextRasterizer.cs`+`TextRasterAssertions.cs` shape and rasterize text, then verify per-glyph ink lands in its shaped column and LTR advance is monotonic — checked against the shaping data that produced the raster, not a stored reference. A meta-test proves the checker has real discriminating power (deliberately corrupts a glyph position and confirms the check fails). RTL is shaped but not yet checked for correct reordering (documented gap, `TextRenderingVisualTests.cs`). Reusable once step 3's shaper is wired into production — feed its output into `TextRasterAssertions` the same way.
5. Once a Skia/HarfBuzzSharp path renders a plain-Latin PDF page correctly on both Windows and Linux (byte-identical is not the bar, same as Chart's Skia-vs-its-own-baseline precedent), revisit whether P1-P3's `GraphicsBase`/HDC-metrics call sites (`PDFWriter.cs:1343,1972,2170,2338`) need their own `SKFont`-based port or can be bypassed for the Skia-backed `CachedFont` path.

## References

- `Microsoft.ReportViewer.Common/Microsoft.ReportingServices.Rendering.RichText/` — the whole subsystem (51 files, ~7,421 lines)
- `docs/decisions.md` — 2026-07-26 correction entry with full reasoning
- `docs/platform-support.md` — PDF (RDL engine) section, current state
- `docs/rendering-abstractions.md` — Chart/Gauge Ports & Adapters pattern this should follow
